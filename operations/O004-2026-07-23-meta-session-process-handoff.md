# O004 — Meta-session process handoff

- Date: 2026-07-23
- Classification: repository operation (not mathematical research)

## Scope

Distill the durable process decisions made across the meta-work conversation so
future work does not depend on retained chat context. This record changes no
mathematical result.

- Agents automatically enter fresh-context adversarial review when a proof or
  counterexample candidate appears, using `proofctl.py review`.
- The README dashboard is regenerated by `proofctl.py index` and displays work,
  claim, prior-proof provenance, and publication links.
- A proved result requires a TeX source and a Tectonic-compiled committed PDF;
  `proofctl.py typeset` produces and records that artifact.
- Repository-only work uses `operations/O###` and `proofctl.py operation`, not a
  problem session. It does not require a mathematical resolution estimate.
- The earlier dashboard and PDF records were moved from the graph dossier's
  research sessions to `O002` and `O003`; proof-review work remains recorded in
  the dossier because it directly assessed the theorem.

- `proofctl.py validate` passes.
- The integration suite covers the review lifecycle, dashboard freshness, and the
  operation command's non-interference with problem dashboards.
- The graph proof's PDF was built successfully by Tectonic 0.16.9 and is present
  in `papers/build/`.

## Dossier impact

None from this handoff record. The graph theorem's proof, review verdicts, and
`complete` / `proved` status were already recorded in the appropriate dossier
records before this operation.

## Handoff

- Result: this conversation's reusable process decisions are captured in durable
  repository records, not dependent on chat retention.
- Remaining operational follow-up: none.
- Files a later operator should read: `AGENTS.md`, `process/workflow.md`,
  `process/proof-standard.md`, `operations/README.md`, `O001`–`O003`, and
  `README.md`.
- Commit and push status: included in this operation's closure commit and push.
