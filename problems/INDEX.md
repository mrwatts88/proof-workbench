<!-- Generated by scripts/proofctl.py index. Do not edit by hand. -->
# Problem index

| ID | Problem | Work | Claim | Updated | Next action |
|---|---|---|---|---|---|
| P-001 | [Unique Common Neighbor](unique-common-neighbor/STATE.md) | `complete` | `proved` | 2026-07-23 | No required action; reference comparison is optional and freely permitted. |
| P-002 | [Erdős–Gyárfás Conjecture](erdos-gyarfas/STATE.md) | `active` | `open` | 2026-07-26 | TIER 1 (G015, case (5b)): PROVE (INT) — the no-gap conjecture — ON THE HAMILTONIAN STRATUM, using A026 T6's chord-exchange calculus. S030 re-aimed the tier and the reason is architectural, not tactical: L046 supplies the 2-connectivity the whole (F) frame assumes ONLY below order 36, so the (F) programme — even decided completely on BOTH strata — closes case (5b) for n0 <= 35 and CANNOT prove G015. The E028 ladder is therefore demoted to a source of floors; DO NOT restart it (stopped at order 30, S028) and DO NOT re-run E024 (harvested, C049). THE ORDER-UNBOUNDED SUCCESSOR IS (INT) AND (L-A) (A028 T9): (INT) says S(H,a,b) contains [8, max S] for a vertex-taut {C4,C8}-free exactly-two-profile pair; with (L-A) (max S >= 14) it forces 14 in S, which is poison by L048(iii), hence (F-S) AT EVERY ORDER — no window, no ladder, no C16 hypothesis. THE MOVE: on a pair with a Hamiltonian a-b path, take the chord-minimal cover of L052(i)/(ii) and ask whether the reachable savings set can skip a value once every chord spans >= 4 (spans 3 and 7 are already excluded by the span law, A026 T5). That is a hand question about span systems, not a search. If it cannot skip, (INT) follows on that stratum and 14 is forced at every order. KILL CONDITION: a span system whose savings set has a gap above 8 while satisfying the class constraints — that instance becomes a new test object and (INT) weakens to (INT-14), the form that only asks about 14, which C050 measures as having a five-degree-2-vertex margin to the nearest recorded refutation. ALTERNATIVE CONSIDERED AND DEFERRED: prove (L-A) instead (max S >= 14 for an in-window profile pair) — the other half of the same architecture, a LOWER bound on longest through-paths rather than an interpolation, deferred because (INT) has a validated engine (A026 T6 reproduces the top of S down to 10, including 14, on all eight profile objects) and a sharply calibrated conjecture in front of it. WHAT S030 SETTLED. (1) L053: the class-level hypotheses do NOT force a Hamiltonian through-path — a cubic non-Hamiltonian graph of girth >= 17 minus an edge is a 2-connected exactly-two-profile {C4,C8,C16}-free pair with none — so C049's 27/27 is a small-order pattern, NOT a lemma, and that route is retired; the object is CALIBRATION OBJECT #3 (any Hamiltonicity argument must fail on it). It is asymptotic: nothing is exhibited in [18,35] and no floor moves. (2) L055: the positive-savings theorem — EVERY off-path component of a longest through-path admits a bridge with savings >= 1, and >= 2 with three or more attachments (the Y-identity sigma(i,k) = sigma(i,j) + sigma(j,k) + 2*beta, maximality alone); A027 T5's zero-savings obstruction is GONE and L052(iii)'s reroute machinery transports from chords to bridges. But the stratum is cleared at NO order: A028 T8 names the two gaps (components are not thinnable edge by edge, so there is no component-atomic minimal-cover notion with a bound on the number of components; and the savings are per component, not per position). (3) L054: the bipartite exclusion dichotomy, with its three logical forms separated — as a dichotomy it is NOT a contradiction inside G015, which already assumes a counterexample exists; only its range form (C034, order <= 24) or an order comparison against n0 (L040) excludes anything. NEW PIVOT TRIGGER (replaces the retired saturation form): a profile object at ANY order with a hole in S at a value >= 8 refutes (INT) outright. C050 verified 24 recorded profile objects with every hole in {4,5,6}; the near-miss corpus opens its first hole at 8 only at degree-2 count 5 and its first at 14 only at count 7, so the margins are measured and any object narrowing them is worth recording. New structure from C050: a SECOND non-interval profile object exists, at order 20 (S = [3,19] minus {4,5}) — one order below where S027 first saw one. PROCESS: (INT) is a CONJECTURE and may not be cited as support for any proved row; 1,920 corpus rows refute it the moment the exactly-two profile is dropped, and those rows are its standing sanity check for any draft proof. See the dependency notes at the foot of CLAIMS.md for exactly how far L053-L055 and C050 may be cited. Do not reopen C037/L045/L038/C045; a non-decomposable cycle in a vertex-taut pair remains a SOUNDNESS ALARM against the reviewed L049, not a route pivot. |
