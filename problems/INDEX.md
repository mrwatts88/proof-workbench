<!-- Generated by scripts/proofctl.py index. Do not edit by hand. -->
# Problem index

| ID | Problem | Work | Claim | Updated | Next action |
|---|---|---|---|---|---|
| P-001 | [Unique Common Neighbor](unique-common-neighbor/STATE.md) | `complete` | `proved` | 2026-07-23 | No required action; reference comparison is optional and freely permitted. |
| P-002 | [Erdős–Gyárfás Conjecture](erdos-gyarfas/STATE.md) | `active` | `open` | 2026-07-24 | TIER 1 (G015): exclude case (5b) — after S019 the cubic reduction holds modulo ONE configuration (L041): a minimum-order tight 1-atom whose defect vertex is non-cut with both neighbours cubic and vertex-taut residue, i.e. the 2-path closure of a member of L039's congruence class G (through-set avoiding {2,6,14,...}; no s_max window — a congruence channel, not a pinch; the taut ladder's collapse machinery does not transfer, W1-T15). PRIMARY MOVE, decisive at one order: the ORDER-16 G-PROFILE SCAN — connected, C4-free, exactly two degree-2 vertices, all others >=3, power-free, S∩{2,6,14}=∅. C027 stops at 15 and C035 is pair-level only, so order 16 is genuinely open; power-freeness is decidable before any path enumeration, so this is far cheaper than E013's closed-pair scan. If empty: every tight 1-atom has order >=18 (improves L041's >=17). If a hit satisfies the S-condition: its 2-path closure is a tight 1-atom and STATEMENT 0.1 IS FALSE — run the disproof protocol (L025 R4). Prefilters from L042: closure 2-connected, non-bipartite, degree->=4 vertices independent, S meets both P and P-1. PROOF-SIDE COMPANION (alongside, not instead): the mod-4 congruence hunt against the L042 residual object (P-1≡3, P≡0, P-2≡2 mod 4; model = the parity proof W1-T13/L042; kill condition: a vertex-taut C4-free core realizing all three forced memberships with no mod-4 structure — then case (5b) reduces to search alone). Secondary: L042's chain-cancellation tension. Route R2 is CLOSED as a constant route (L038 delimitation: S15 caps the non-power counting at exactly 2/3, the proved constraints at 2/3+O(1/n), and no constant <1 delivers the reduction) — do NOT grind the density constant; the surviving R2 extension is the internally-disjoint longer-link descent (A020 W2-T8(c)), and the routes meet on the link graph (a one-defect subgraph of it is a tight 1-atom, W2-T8(d)). TIER 3 background (run, never select): the E015 order-23 bipartite leg was launched in S019 under PyPy and is still RUNNING — its harvest is a named follow-up, excluded from all ledgers until it finishes; then the odd-prime-gcd scan, order-17+ pinched legs, census mining. TIER 2 alternatives if Tier 1 stalls: bipartite EGC; the path-spectrum gcd theorem. PROCESS (O011): parallel legs run as worker subagents of one orchestrating session — workers on opus minimum (fable otherwise), audit worker output before integrating, single canonical checkpoint. |
