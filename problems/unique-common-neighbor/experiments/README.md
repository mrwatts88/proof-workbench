# Experiments

Each `E###-*/` contains a `README.md`, the smallest useful program or notebook,
inputs, and compact evidence. Put disposable or large output under `generated/`.
State whether the experiment is exploratory, exhaustive after a proved reduction,
or certificate-producing.

