# Attempts

Each `A###-*.md` records one coherent line of attack. Keep abandoned and failed
attempts, explain the decisive failure, and identify anything salvageable. Create a
record with:

```sh
python3 scripts/proofctl.py add <problem-slug> attempt "<approach>"
```

