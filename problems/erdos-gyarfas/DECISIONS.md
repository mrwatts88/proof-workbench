# Decision log

Append decisions that alter the statement, assumptions, conventions, proof
architecture, or disposition of a major approach.

| Date | Decision | Reason | Affected records |
|---|---|---|---|
| 2026-07-23 | Create statement version 0.1 for intake. | Initial dossier creation. | `STATEMENT.md`, `problem.json` |
| 2026-07-23 | Interpret the named conjecture as the finite simple undirected statement, with a simple cycle of length \(2^k\) for \(k\ge 2\). | This exposes the graph category and cycle convention without changing the standard substantive target; powers \(1\) and \(2\) cannot be simple-cycle lengths. | `STATEMENT.md`, `CLAIMS.md` |
| 2026-07-23 | Begin with an internal structural and exact-small-case attack; defer literature and current-status claims until sources are deliberately inspected. | This keeps imported facts separate from deductions and leaves provenance visibly unverified. | `STATEMENT.md`, `STATE.md`, `S001` |
| 2026-07-23 | After proving the order-\(9\) and order-\(10\) exclusions, stop using one-order-at-a-time enumeration as the main route. | The literature reports substantially stronger finite searches, while the order-\(11\) codegree budget already leaves twelve degree sequences; finite extension is evidence, not a scalable proof mechanism. | `A002`, `A003`, `E002`, `STATE.md` |
| 2026-07-23 | Import only precisely matched results from the inspected 2022–2026 primary papers, and keep the original 1997 source marked uninspected. | This distinguishes verified statement correspondence and usable external theorems from bibliographic or search-result reports. | `STATEMENT.md`, `CLAIMS.md`, `OBLIGATIONS.md`, `references/source-audit-2026-07-23.md` |
| 2026-07-23 | Stop the local induced-\(P_{13}\)/cubic-domination classification as a proposed \(C_{16}\)-forcing route. | `A004/L007` gives a connected cubic graph satisfying every structural conclusion used by the route while its girth excludes \(C_4,C_8,C_{16}\); further local enumeration would not repair the missing global input. | `A004`, `CLAIMS.md`, `OBLIGATIONS.md`, `STATE.md` |
| 2026-07-23 | Use edge-maximal power-cycle saturation as the primary global reframing, while retaining the Liu–Montgomery variable-length path method as the live alternative. | `L008` gives a certificate across every nonedge, `L009` shows that it is not shared by the prior bipartite cubic large-girth obstruction, and `L011` preserves the certificates inside the 2-connected leaf-block reduction. The route is not yet decisive because witness intersections and theta lengths remain uncontrolled. | `A005`, `CLAIMS.md`, `OBLIGATIONS.md`, `STATE.md` |
| 2026-07-23 | Do not treat one external ear and its three theta cycle lengths as the decisive saturation mechanism; retain the full Mersenne witness and its ordered intersections with the shortest odd cycle. | `L013` shows that an arbitrary ear is already forced by 2-connectivity, while `L014` gives infinite one-ear theta length triples with no power-of-two cycle. The extra information unique to saturation lies in the full \(2^k-1\)-edge witness. | `A005`, `CLAIMS.md`, `OBLIGATIONS.md`, `STATE.md` |
| 2026-07-23 | Retire single-witness saturation forcing entirely; repurpose `L008` as constraint supply, and make the order-\(11\) exhaustive search for saturated counterexamples the primary route, with the variable-length reframing kept as the live alternative. | `L015`–`L016` fire the recorded pivot trigger: full one- and two-excursion witness patterns both admit unconstrained infinite power-free families. Powers of two are exponentially sparse, so any bounded witness diagram with a free segment length admits power-free solutions; only unbounded witness coupling or an even-length interval mechanism can force. The trigger's recorded consequence named the variable-path reframing as the next primary; it is instead retained as the alternative because no small falsifiable first move for it at minimum degree \(3\) was identified, while at orders \(\le15\) saturation collapses to the finitely checkable condition "every nonedge has a path of length \(3\) or \(7\)", where an exhaustive order-\(11\) search is immediate, decisive either way, and informative for both routes. | `A005`, `CLAIMS.md`, `OBLIGATIONS.md`, `PROOF.md`, `STATE.md`, `problem.json` |
| 2026-07-23 | Drop the premise that `L008` saturation accelerates the finite search; run the frontier as a plain exhaustive \(\{C_4,C_8\}\)-free enumeration with saturation kept only as a survivor classifier. | The witness condition constrains completed graphs, not partial ones, so it cannot prune a generation tree; and at orders \(11\)–\(12\) the full \(\{C_4,C_8\}\)-free class proved empty, so the saturated subclass never arose and `L017` was proved without `L008`. Incremental \(C_8\) rejection, not saturation, is what makes the exhaustion cheap. | `A006`, `E004`, `CLAIMS.md`, `OBLIGATIONS.md`, `STATE.md` |
| 2026-07-23 | Stop the small-order exhaustion at thirteen; reach the published frontier by import, and make the novelty of the saturation line the deciding question for the next attack. | The user clarified the repository mission: lean on all existing verified knowledge to stand at the frontier instantly, and spend effort only on passing it; re-deriving reported computations is explicitly not a goal, and reported prior bounds already exceed the internal orders. The edge-maximal saturation reduction `L008` did not appear in any source audited in `S002`, so a deliberate literature check (`G011`) either makes strengthening it the frontier-passing target or imports its strongest published form. | `O008`, `OBLIGATIONS.md`, `STATE.md`, `problem.json`, `PROJECT_STATE.md`, `A006` |
| 2026-07-23 | Import the general finite bound at its primary-source strength — every counterexample has at least \(16\) vertices — and treat the circulating "at least 17" as an unsupported overread that must not be relied on. | Both primary sources agree: Royle's archived 2002 page says "I have checked this conjecture for graphs on up to 15 vertices," and Markström (2004) reports the same search as "all relevant graphs on less than 16 vertices." No inspectable source states an order-\(16\) search; Wikipedia and Hegde–Sandeep–Shashank repeat \(17\) without a matching primary statement. | `C012`, `references/source-audit-2026-07-23-S007.md`, `CLAIMS.md`, `STATE.md` |
| 2026-07-23 | Record the `G011` novelty verdict: the edge-maximal power-cycle saturation reduction appears nowhere in the swept literature, so strengthening the saturation line becomes the frontier-passing objective; the verdict carries a standing repeat-before-publishing caveat. | The recorded sweep (queries plus full-text inspection of every EGC-specific source held) found no saturation, edge-maximality, or Mersenne-witness formalism attached to this conjecture, and the cycle-saturation literature never treats the power-of-two family; one indexed sweep cannot prove absence, so the caveat stays attached to any external claim. | `G011`, `references/source-audit-2026-07-23-S007.md`, `STATE.md`, `problem.json` |
| 2026-07-23 | Verify leaned-on reported computations instead of trusting them: reproduce Markström's order-24 cubic census internally (E005) and re-derive the general orders 14-16 with an independent pipeline (E006) rather than importing blind; imported bounds whose reproduction would be expensive (cubic orders 26-28, Royle's degree-structured search) stay imported with source strength only. | The mission directs verification or refutation of merely reported results that the route will lean on; the 24-vertex graphs are the intended test material for the saturation line, and the general small orders gate the new bound, so their correctness must be internal; full re-derivation of everything else would be re-deriving known results for its own sake. | `E005`, `E006`, `CLAIMS.md`, `references/source-audit-2026-07-23-S007.md` |
| 2026-07-23 | Cap the exhaustive-search layer where it stops being cheap (about order 18 with the current pipeline) and add a named falsification-side route to the portfolio: the spectrum-density-versus-gap program — Bensmail-style confinement constructions (`C017`) against forced spectrum-density theorems (Bondy–Vince pairs; Sudakov–Verstraëte consecutive even lengths) inside the 2-connected block reduction, where confinement is impossible. | The user asked whether exhaustive checking is the likeliest path to settling the statement; it is not and was never priced as one — finite exclusions cannot touch the universal claim, and both deep routes are mechanistic. Erdős and Gyárfás themselves reportedly expected the conjecture to be false, Bensmail's constructions show exactly how bounded cycle spectra dodge sparse power sets and exactly why 1-connectivity is essential to the trick, so the decisive open tension is whether a 2-connected minimum-degree-3 spectrum can gap every power of two against the known density forcings. | `STATE.md`, `CLAIMS.md` (`C017`), `references/source-audit-2026-07-23-S007.md`, `S007` |
| 2026-07-23 | Amend the next action from positioning ("analyze test material, write a lemma target") to attempting: open the voltage-graph lift construction program on the falsification route with pre-named kill conditions, keeping the controlled-start interval lemma as the proof-side alternative and the order-18 census as capped support. | The user directed that missing tools be treated as internal research targets, now contract law via `O009`; the falsification route is generate-and-verify, the genre where this project's tooling is strongest, and voltage lifts give actual algebraic control of lifted cycle lengths, so a concrete construction attempt with kill conditions dominates pure target-sharpening on expected research value. The resolution estimate is unchanged — posture is not evidence. | `STATE.md`, `problem.json`, `S008` |
| 2026-07-23 | Close the cyclic/abelian arm of the voltage-lift program: its pre-named kill condition (a) fired, with the obstruction proved rather than merely observed. Retain the lift machinery (`L019`, `L020`, the walk-class sieve) and route the program's continuation to a fresh choice between the non-abelian sieve (odd-order groups 21, 27, where the obstruction vanishes at its root) and the proof-side interval lemma now fed by the `L021` length formula. | `L021`: commutator words are voltage-invisible over every abelian group and occur at power-of-two lengths on every tested base (8 dumbbell, 16 theta3/\(K_4\)/prism, 32 \(K_{3,3}\), 4 bouquet); `C020` makes the certificate verdict exhaustive over all \(m\ge2\) for the complete cycle-rank-2 base list plus \(K_4\), \(K_{3,3}\), prism, and `C021` verifies at truth level that the cyclic-lift universe of these bases contains no counterexample through order 30. Cyclicity was chosen for the no-wrap correspondence, but cyclic means abelian, and abelianization is precisely what blinds voltages to commutators — the leverage and the obstruction are the same fact. | `A008`, `E007`, `CLAIMS.md` (`L019`–`L021`, `C019`–`C021`), `OBLIGATIONS.md` (`G012`), `STATE.md`, `problem.json` |
| 2026-07-23 | Retire the voltage-certificate program as a counterexample generator for **all** finite groups, not only abelian ones, and continue it in-session past the original plan at the user's direction. The A009 route analysis replaced the recorded odd-order targets before running them: Feit–Thompson makes every odd-order group solvable, the solvable cascade (double commutators, power-twisted commutators) kills those predictably, so the decisive test was the perfect group \(A_5\) — which also died, exposing the true obstruction as the group-size collision wall rather than any group structure. Adopt proving the collision-wall lemma as the program's closing theorem and the refined `G012`; shift falsification-side weight toward the proof-side interval route, which inherits the wall's forcing heuristic. | `C022`: all assignments over orders 21, 27, 27, and 60 on all tested bases are certificate-dead by length 16, zero survivors — exactly the collision-wall prediction \(2\log_2\lvert\Gamma\rvert\) and independent of structure (solvable and perfect groups die identically). A lift must dodge powers up to \(n_B\lvert\Gamma\rvert\) while certificates die at \(\approx2\log_2\lvert\Gamma\rvert\): an exponential gap no group choice crosses. Structure only selects which of \(\{4,8,16\}\) bites first. | `A009`, `E008`, `CLAIMS.md` (`C022`), `OBLIGATIONS.md` (`G012` refined), `STATE.md`, `problem.json` |
| 2026-07-24 | Retire the unconditional walk-to-cycle transfer target on arrival and replace the interface with the atom/spread-doubling question: the recorded kill test ("unbounded walk/cycle separation in 2-connected min-degree-3 graphs") fires by construction, not by computation — Bondy–Vince's own 1998 ring of \(K_{3,3}-e\) copies (`C024`, imported this session) is 2-connected, minimum degree 3, carries the `L024` walk intervals, and has cycle-spectrum gaps of unbounded ratio. The correct conditional interface is `L025`'s contrapositive: statement 0.1 forces **spread-doubling** (\(s_{\max}\ge2\,s_{\min}\)) in every power-free two-terminal gadget, and forbids power-free graphs with a single sub-cubic vertex. Open `G013` for it; run the atom search as the failure-first arm. | The sweep found the degree-3 frontier is exactly Bondy–Vince strength (`C024`; `C025` needs degree \(\ge k+1\); sparse-interval technology needs large average degree), so no import supplies the transfer; and the ring mechanism shows any valid transfer must consume power-freeness. The atom reduction makes the disproof side finite-searchable for the first time since the lift program closed: one gadget of bounded order would settle the statement negatively, and its nonexistence at each order is exhaustively checkable. `E010`/`C027`: empty through order 15, with the profile showing the pinch is geometric­ally common and blocked exactly by \(C_8\) — the spread-doubling phenomenon is real. | `A011`, `E010`, `CLAIMS.md` (`C024`–`C027`, `L025`), `OBLIGATIONS.md` (`G013` new, `G007` rescoped), `PROOF.md`, `STATE.md`, `problem.json`, `references/source-audit-2026-07-24-S011.md` |
| 2026-07-24 | Close the voltage-lift program as a theorem: the collision-wall lemma is proved (`L024`, with the arc-digraph package `L023`), passing its delegated logic audit (`R001`); `G012` is resolved. The falsification side now has no live construction tool by design, and the portfolio's primary route becomes the proof-side walk-to-cycle question: convert the forced identity-balanced walk intervals of `L024` into cycle-length control at minimum degree \(3\). | The missing "bookkeeping" closed with a new branch-and-steer construction: one pigeonhole collision births a seed pair, deterministic exact-length steering in the strongly connected nb arc digraph (period \(\le2\), proved internally via the longest-path theta) replaces fiber pigeonhole, and a four-junction gluing telescopes to identity in any group. Both program arms are now closed by proof rather than exhaustion, so remaining falsification hope rests on mechanisms outside bounded-template algebra; meanwhile the theorem's trivial-group reading is the sharpest proof-side fact the project owns, making walk-to-cycle the highest-value open interface. | `A010`, `E009`, `R001`, `CLAIMS.md` (`L023`–`L024`), `OBLIGATIONS.md` (`G012` resolved, `G007` input), `PROOF.md`, `STATE.md`, `problem.json` |
| 2026-07-24 | Correct the bottom-rung program before executing it: the inherited rungs (\(s_{\min}\in\{1,2\}\), unrestricted) are proven conjecture-complete (`L026`), so they were never lemma-sized targets; re-base the rung ladder on taut gadgets via the lobe decomposition (`L027`), close the taut rungs \(s_{\min}\le2\) by hand with \(C_4\)-freeness alone (`L028`), and elevate the 1-atom question to the central open object of the disproof interface — the lobe content of every non-taut 2-atom and, by the new cubic reduction (`L029`), the sole obstruction to reducing statement 0.1 to cubic graphs. Statement unchanged. | The strategy audit's premise test fired at formulation time: pinched gadgets at \(s_{\min}\in\{1,2\}\) can be assembled from two disjoint copies of any hypothetical counterexample (bridge- and lobe-packing), so proving the unrestricted rungs equals proving the conjecture; tautness is exactly the condition outlawing the hiding mechanism, and under it the bottom rungs fall to short forcing arguments (unique common neighbor plus \(C_4\) counting). `E011` verifies both lemmas on every instance at orders 12–13 and finds every taut pinched gadget there sits at \(s_{\min}=6\), so the next rung (taut \(s_{\min}=3\)) is empty well past the orders in reach. | `A012`, `E011`, `CLAIMS.md` (`L026`–`L029`, `C028`), `OBLIGATIONS.md` (`G013` refined), `PROOF.md`, `STATE.md`, `problem.json` |
| 2026-07-24 | Close the taut \(s_{\min}=3\) rung as a theorem (`L030`) and correct S012's boundary prediction: \(C_4\)-freeness alone carries this rung too — the power-spectrum fight does not start at \(s_{\min}=3\). Re-scope the taut ladder accordingly: taut 2-atoms have \(s_{\min}\ge4\) and \(s_{\max}\ge6\), every 2-atom with \(s_{\min}\le3\) routes through the 1-atom question, and the genuine \(C_4\)-only/power boundary lies in \(s_{\min}\in\{4,5,6\}\) with the upper end pinned by `C028`'s witnesses. Make the taut \(s_{\min}=4\) rung the next action. Statement unchanged. | The failure-first construction attempt collapsed into forced structure at every step and terminated: tautness confines middles to distance one from the terminal neighborhoods (position analysis on length-\(\le5\) paths), forbidden length-6/7 paths eliminate the one-sided middle classes, force the rest into matched degree-3 triples, then empty the middle entirely; the surviving three-perfect-matchings structure dies in a dichotomy — a \(C_4\) block or a length-7 path. `E012` searched every admissible terminal pair (including degree-\(\ge3\) terminals, never examined before) at orders 6–14 and found the target empty; the endgame dichotomy verified exactly on all 218 small three-matchings structures. | `A013`, `E012`, `CLAIMS.md` (`L030`, `C029`), `OBLIGATIONS.md` (`G013` refined), `PROOF.md`, `STATE.md`, `problem.json` |
| 2026-07-24 | Reframe the taut half of the disproof interface onto **blocks**: adopt the pendant reduction and block extraction (`L031`/`L032`) as the primary frame — taut 2-atoms exist iff a power-free vertex-taut 2-connected core with \(s_{\max}\le2\,s_{\min}\) exists — and repair a record overstatement: earlier summaries cited `C029` for "\(s_{\min}\in\{4,5\}\) empty through order 14 over all terminal pairs", but `C029` certified \(S\subseteq\{3,4,5\}\) only; the emptiness is now genuinely established by `C030`. The session prioritized the witness-dissection alternative over the inherited depth-2 collapse after an explicit audit: the decisive evidence was the `C029` coverage gap (the inherited rung's search half had to be redone anyway) and the identical-spectra regularity of the five `C028` witnesses noticed at resume; the user's suggestion to consider the witnesses was input to, not the basis of, the selection. Statement unchanged. | The dissection found every known witness is a pendant lift: one cut vertex, terminal degrees (1,2), core at through-ratio exactly 2 — and the new all-band catalogue (`E013`) completed the picture: six strict witnesses (a new band-5 one at order 11 = the lift of Petersen minus an edge, plus the five known), five 2-connected cores at exact equality (Petersen\(-e\) at band 4; three at band 5; one at band 6 found at order 14), no strict block through 14, every \(C_8\) in the catalogue a two-through-path symmetric difference. The bridge composite (two Petersen\(-e\) blocks + bridge = strict taut core of order 20) refuted naive core-level spread-doubling in-session, fixing 2-connectedness as the right level before the frame was recorded. | `A014`, `E013`, `CLAIMS.md` (`L031`/`L032`, `C030`/`C031`), `OBLIGATIONS.md` (`G013` refined), `PROOF.md`, `STATE.md`, `problem.json` |
| 2026-07-24 | Reformulate the band-4 equality power rung as the **pencil endgame** and make it the primary target: prove that no vertex-taut 2-connected \(C_4\)-free core with \(d(x,y)=4\) and \(s_{\max}\le8\) has a pencilled 4-path system; success closes band 4 of the block question in the strengthened form "a \(C_8\) through both terminals" (no \(C_{16}\) alternative at any order). Demote the strict band-4 rung behind the endgame. Statement unchanged. | The dichotomy half of the inherited mechanism turned out to be a theorem stronger than hoped (`L033`): pairwise-intersecting 4-path systems are pencils in ALL graphs — the planned C4-dependent "triangle" anchor was machine-refuted during verification, removing the hypothesis — and \(C_4\)-freeness pins the pencil to a terminal neighborhood with rigid fan structure. The reformulation is exactly the complementary case of that dichotomy. Hypothesis-necessity is now settled (`C033`): with \(C_4\)s allowed, strict band-4 blocks with the rung shape and \(C_8\)-free band-4 equality cores both exist at orders 8–9, so no C4-agnostic proof of either rung exists, and the strict rung offers no shortcut. The endgame is exhaustively empty through order 15 (`C032`), and three hand construction attempts against it died to a forced \(C_8\), a forced 9-path, and forced \(C_4\)s (the cascade obstruction, recorded in `A015`). | `A015`, `E013` (order-15/16 extension), `E014`, `CLAIMS.md` (`L033`, `C032`, `C033`), `OBLIGATIONS.md` (`G013`), `PROOF.md`, `STATE.md`, `problem.json` |
| 2026-07-24 | Widen the assembly disproof interface from the pinched channel to the full sumset criterion (`L034`) after auditing a user-supplied external agent memo: a power-free (D)-gadget disproves 0.1 whenever ANY \(L\)-fold sumset of its through-set avoids the powers of two — parity (all-odd \(S\)), odd-prime gcd, and mod-4 channels join the pinch. Retract the three "sharp/entire-channel" glosses (`A011` R5 title, `G013`, `PROOF.md`); no proved row changes. Make the program ceiling explicit: closing all assembly channels plus the 1-atom question yields the cubic reduction, not 0.1. Open `G014` for the memo's literature/census leads (all unverified). Adopt the user's standing instruction that ALL research threads be documented and carried in parallel — the pencil endgame, order-16-derived pinched work, band-2 rung, 1-atom theory, and the six memo paths (parity calibration and bipartite gadget hunt, cubic census extension, census mining, bipartite EGC, Carr 4/7 strengthening, 3-connectivity) are all live portfolio entries; selection happens per-session by strategy audit, retirement only by evidence. Statement unchanged; no search executed this session on the new channels (user instruction: integrate, do not act yet). | The memo's core is correct and was independently re-derived (`A016`): `A011` R1 proves the ring spectrum exactly for every \(L\ge3\) with no pinch input — D-A3 had been built from R2's sufficient dyadic window, and the criterion was never re-derived from R1. Verified consequences: \(S=\{3,7\}\) and \(S=\{2,6\}\) are fatal shapes invisible to the 2-atom frame; the corrected necessary conditions include an even through-path. Verified mitigations the memo lacked: `C027` is stream-level and profile-agnostic, so every new channel is as empty as the pinched one through order 15 (18 for degree-\(\ge3\) terminal pairs); all-odd ladders live at odd bands, disjoint from the band-4 pencil battlefield; and the pinched results (`L026`–`L033`) stand unchanged as the closure of one genuine channel. | `A016`, `references/external-agent-strategy-memo-2026-07-24.md`, `CLAIMS.md` (`L034` + notes), `OBLIGATIONS.md` (`G013` rewritten, `G014` opened), `PROOF.md`, `STATE.md`, `PROJECT_STATE.md`, `problem.json` |
| 2026-07-24 | Identify `L034`'s parity and mod-4 channels with the **bipartite class** and re-instrument their search accordingly (`L035`): for a vertex-taut two-terminal graph, parity-constancy of the through-set is *equivalent* to bipartiteness, so (a) any connected bipartite power-free graph with at most two sub-cubic vertices disproves 0.1 outright — no path enumeration, no tautness test, no external import — and (b) bipartite generation is an exhaustive instrument for those channels modulo the standing 1-atom relativization. Retire the inherited next action (the odd-\(S\) taut scan mode over the general order-\(\le15\) stream) as superseded rather than executed, and replace it with the genbg-based hunt, which reaches orders the general stream cannot. Correct `A016` M3 ("instance, not equivalence") and M4 ("no reduction theory"); de-gate `G014` item (2) for this thread. Statement unchanged. | The inherited scan would have re-scanned a class already proved empty (`C027` is profile-agnostic through order 15) using an enumeration-bound scanner, whereas the structure theorem converts the same question into a forbidden-cycle test over a far sparser class: the bipartite \(C_4\)-free near-cubic class has 2 members at order 14 and 10,865 at order 21, so the channel was pushed six orders past the general wall in minutes (`C034` — empty, with every member carrying a \(C_8\) and never fewer than 13). The theorem's kill condition was tested exhaustively before recording (`E015`/`verify_parity.py`: 994 connected graphs of order \(\le7\) and all 926 \(C_4\)-free graphs of orders 8–9, all ordered terminal pairs, through-sets by explicit path enumeration; V2 never fired, and all 10,788 parity-constant non-bipartite pairs were non-taut, confirming tautness is the whole hypothesis). Two side effects worth recording: the run verifies the bipartite instance of 0.1 internally at these orders (so the unverified \(\ge32\) import is not needed), and it covers the bipartite instance of the 1-atom question. The odd-prime-gcd channel is explicitly **not** covered — \(\Theta(3,3,3)\) is vertex-taut, non-bipartite with \(S=\{3\}\) — and is now the only congruence channel without structure theory. | `A017`, `E015`, `CLAIMS.md` (`L035`, `C034` + notes), `OBLIGATIONS.md` (`G013` refined, `G014` item 2 de-gated), `PROOF.md`, `STATE.md`, `problem.json` |
| 2026-07-24 | Run both `G015` routes concurrently as the dossier's first **orchestrated parallel session** (S019: one orchestrating session, one Opus worker subagent per route, per the `O011` protocol), and act on the audited results: (1) reframe **R1 to the conditional form** "a tight 1-atom yields a cubic counterexample" — which is all `G015` needs — and retire the unconditional form "no tight 1-atom exists" as an R1 target (its reducts are counterexamples, not atoms; the method cannot reach it); with `L040`/`L041` the conditional form is proved **modulo excluding case (5b)** alone. (2) **Close R2's constant route**: upgrade `C004`–`C006` to verified strength (line-by-line audit of arXiv:2605.22844v1), supersede `C006` internally by `L038` (\(3\lvert V_3\rvert\ge2n+3\), via the new subdivision descent `L037`), and record the route's exact ceiling — S15 certifies \(2/3\) as sharp for the non-power hypotheses, the proved constraints cap at \(2/3+O(1/n)\), and no constant \(<1\) delivers the reduction — retiring "grind the constant" permanently; the descent survives as a reusable mechanism and the disjoint longer-link extension as the only live R2 continuation. (3) Make the **order-16 \(\mathcal G\)-profile scan** the next decisive move, with the mod-4 congruence hunt as the proof-side companion (the parity proof `L042` is its model). Statement unchanged. | The two legs were mechanistically distinct and dependency-free, exactly the pairing `process/concurrency.md` permits; both worker outputs passed the orchestrator's line-by-line audit with machine checks reproduced under two interpreters. The R1 reframing is forced by the mathematics: minimality arguments hand back counterexamples of order \(<n_0\), which the engine (`L040`) converts into cubic counterexamples — a *success* for the conditional target where `A012` Remark T4.1 saw only an unclosable branch. The R2 closure is likewise forced: a sharpness certificate at exactly \(2/3\) plus the LP cap shows further counting cannot approach 1, and a density constant below 1 was never logically sufficient for `G015`. The two routes meet on the link graph (a one-defect subgraph of it is a tight 1-atom), which concentrates all remaining `G015` work on the same object: the case-(5b) residual. | `A019`, `A020`, `E016`, `E017`, `references/carr-2026-verification-2026-07-24.md`, `CLAIMS.md` (`C004`–`C006` verified, `L037`–`L042` new), `OBLIGATIONS.md` (`G015`, `G013`(a)), `PROOF.md`, `STATE.md`, `problem.json`, `S019` |
| 2026-07-24 | Retire the unrestricted 1-atom question as a target and restate the cubic reduction with **tight** 1-atoms (`L036`, `A018`): a counterexample plus one pendant vertex is a 1-atom under D-A4, so "no 1-atom exists" is *equivalent* to statement 0.1 and `L029`'s "modulo the 1-atom question" was vacuous. `A012` T4's proof only ever produces an exceptional vertex of degree 2, so the reduction is restated there and remains non-vacuous. Withdraw `A012` Remark T4.1 as unproved. Correct `A016` M6's ceiling (it was circular). Open `G015` — the cubic reduction as the programme's named proof-side deliverable, with two independent non-circular routes — and restructure `STATE.md`'s flat thread list into a tiered roadmap (Tier 0 the conjecture; Tier 1 `G015`; Tier 2 restricted-class theorems; Tier 3 cheap legs that may be run but never selected as primary; Tier 4 leverage infrastructure). Add a process rule: every strategy audit must name the tier its route serves. Statement unchanged. | The user asked why sixteen consecutive strategy audits deferred the 1-atom question while every record called it "the only sub-question with direct proof-side yield". The answer is mathematical: it is not a sub-question. The pendant construction is one line and had never been tried on the one-terminal object, though `L026` had already used the same hiding idea on the two-terminal one. Consequences ran wide — the interface's "ceiling" contained the conjecture as one of its halves, so sessions 11–16 were working toward a target whose proof-side best case was smaller than recorded, and the genuinely reachable theorem (the cubic reduction, unconditional) had never been named. The tiering also fixes the selection bias that produced this: ranking routes by cheapest-decisive-computation reliably promotes search legs and buries theorem targets that have no cheap move, which is precisely how a conjecture-complete object stayed on the live list for six sessions. Every thread stays live per the standing instruction; demotion is not deletion. | `A018`, `CLAIMS.md` (`L036` new, `L029` restated, `C034` wording, dependency notes), `OBLIGATIONS.md` (`G013`(a) rewritten, ceiling corrected, `G015` opened), `PROOF.md`, `STATE.md` (roadmap), `PROJECT_STATE.md`, `problem.json` |
| 2026-07-24 | S021 (second orchestrated parallel session, per `O011`; workers on `fable`/`opus`): (1) **Adopt the `E019` generator (`genc48`, geng + PREPRUNE C8 plugin) as the standard search instrument** for every \\(\\{C_4,C_8\\}\\)-free class from order 17 up, retiring filter-the-stream (geng at `-d2`, genbg for bipartite) at those orders; `G014` item 6 is discharged as an instrument, with Markström's method imported as a design only. (2) **Retire arithmetic-only exclusion of the case-(5b) chain branch as impossible** (`L045`: the forced memberships interact in exactly three equal-exponent patterns, and abstract solutions exist from block orders 15–16), and record the chain branch as **theorem-closed below order 36** (`L046`): the case-(5b) frontier below 36 is the 2-connected configuration alone, and future proof attempts should consume interference or realizability structure, not memberships. (3) **Reject the MathOverflow-512914 answer's cubic-to-min-degree-3 step** as an import (its general form is the open `G015`); use its cubic-20 figures only at corroborated strength and supersede its order-20 claim by the internal stage-B follow-up run. Statement unchanged. | The two legs were mechanistically distinct and dependency-free; both passed the orchestrator's audit with reproductions (E020 search re-run 0/0/0; E019 anchors 146/146 re-run; order-18 stream slices 3/24 sampled, all empty, against the old instrument and the original E015 detector). Instrument adoption is forced by price and by verification depth: ~75× cheaper at order 16 with set-equality agreement everywhere both instruments reach, plus an external nonzero positive control (cubic 24 = Markström Table 3 = E005). The chain-branch retirement is forced by `L045`'s freeness — continuing to seek a membership-only contradiction would repeat the congruence-hunt mistake `C037` already recorded once. | `A022`, `E019`, `E020`, `references/mathoverflow-512914-audit-2026-07-24.md`, `CLAIMS.md` (`L043`–`L047`, `C038`–`C040`), `OBLIGATIONS.md` (`G015`, `G013`(a), `G014`), `PROOF.md`, `STATE.md`, `problem.json`, `S021` |
| 2026-07-24 | Adopt the interference program as the primary case-(5b) proof route: first the candidate lemma T5 ("vertex-taut \(\Rightarrow\) interference-complete") through its ordered kill rungs (the vertex-taut order-19 exemplar's full cycle set, `smallworld 13`, sparse general orders 8–9) and, if it survives, the clean-window-reroute proof attempt; then the order-windowed forcing target (F). Congruence-type and membership-only routes stay retired (`C037`/`L045`). | The S022 dissection landed the interference outcome at the exact frontier (all 553 blockers decompose, `C041`), the property is empirically exactly vertex-tautness-shaped with necessity proved (`C042`/`L048`(i)), and `L048` shows the calculus converts the tight 1-atom's power-freeness into through-path arithmetic under completeness — the only surviving genre; either T5 outcome redirects the route decisively, and the first-ever profile member (order 19, doubly blocked) supplies the sharpest test object. | `A023`, `E021`, `E022`, `CLAIMS.md` (`L048`, `C041`–`C043`), `OBLIGATIONS.md`, `PROOF.md`, `STATE.md`, `S022` |
| 2026-07-25 | S023: **Promote T5 from labelled conjecture to internally proved theorem** (`L049`, the trimming construction; companion biconditional `L050`; delegated audit `R002` PASS with all findings repaired in place), retire the recorded reroute/exchange proof plan as superseded (its two sub-obligations resolved: cycle-edge essentiality proved as Lemma A; weaving control shown unnecessary), and **make the forcing target (F) the entire proof side of case (5b) below order 36** (`L048`(iii) now unconditional: the residual object's spectrum equals its through-path \(T_1\) set, every element a trunk-split pair value). Statement unchanged. | The pre-registered kill order was honored — `smallworld 13` and the sparse probes ran first and survived (`C044`: biconditional exact at 13; order \(\le8\) exhaustive) — and the proof then landed by a mechanism found at normalization (trim any through-path forced through a prescribed cycle edge at its first/last cycle contacts; complete through both arcs), whose every step was re-executed mechanically on every instance in scope including all eight profile objects, and which passed a fresh-context logic audit with zero critical or major findings. Keeping T5 as a target would be compliance, not research; (F) is where the open content now lives, and its first move (the power-collision realization tables of the ten named objects) is cheap and falsifiable. | `A024`, `R002`, `E023`, `CLAIMS.md` (`L049`/`L050`/`C044` new; `C042`/`L048` updated), `OBLIGATIONS.md` (`G015`, `G013`(a)), `PROOF.md`, `references/textbook-classics-2026-07-25.md`, `STATE.md`, `problem.json`, `S023` |
| 2026-07-25 | S024: **Close the membership-collision form of (F) as a route and re-aim the (F) program as (F-S) ∨ (F-T)** — (F-S): in the window \([18,35]\), vertex-taut (5b)-profile pairs with \(4,8\notin\mathrm{Spec}\) force \(S\cap\{6,14\}\ne\emptyset\); (F-T): they force \(16\in\mathrm{Spec}\); either closes case (5b) below 36. The route's first move becomes the S-gap census at the window bottom. Statement unchanged. | The pre-registered opening probe took its branch (b): the complete trunk-split realization tables of all ten named objects (`E025`/`C045`) show no membership-patterned regularity — none of nine pre-registered patterns universal, 30 cycles membership-blind outright, the calibration pair's 100% power-participant structure a small-order artifact — so a membership-arithmetic forcing lemma would be chasing an illusion (the empirical third leg beside `C037` and `L045`). What the tables do show is order confinement (the trunk bound `L051`, tight everywhere) and frontier saturation (all eight profile objects: spectrum \([3,n]\setminus\{4,8\}\), \(S\supseteq[6,n-1]\ni6,14\)), which is exactly the shape of the two sub-targets; both correctly fail off-window on the calibration pair, satisfying the `A021` consumption discipline. | `A025`, `E025`, `CLAIMS.md` (`L051`/`C045` new), `OBLIGATIONS.md` (`G015`), `STATE.md`, `problem.json`, `S024` |
| 2026-07-25 | (S027) Stop trying to prove (F-S) or (F-T) as separate lemmas; **decide the disjunction (F) directly, order by order, by exhaustive enumeration after a proved reduction**. Concretely: restrict to chord-minimal systems on a Hamiltonian \(a\)–\(b\) path (`L052`(ii)), prune by the monotone-reroute savings certificate (`L052`(iii)), take the poison set to be the full \(\{2,6,14,30\}\), and add \(C_{16}\)-freeness as a *hypothesis* rather than a target. Statement 0.1 unchanged; (F), (F-S), (F-T) stand verbatim. | Only (F) closes case (5b), so separating the disjuncts was pure self-imposed difficulty: assuming *both* conclusions fail hands the search \(C_{16}\)/\(C_{32}\)-freeness for free, and that hypothesis is what cuts the enumeration's growth from \(\approx\times3.5\) to \(\approx\times1.9\) per order — the difference between stalling near order 26 and reaching the top of the window. The reduction-first framing also replaces an unproved combinatorial lemma about chord systems (which nobody had axiomatised) with a finite decision procedure whose soundness is two short proofs. Empirically the redirect is already vindicated: `E028` is empty at every order tested and exhibited the first order-21/22 profile objects, all doubly blocked. | `A027`, `E028`, `CLAIMS.md` (`L052`/`C048`), `OBLIGATIONS.md` (`G015`), `STATE.md`, `LOG.md`, `problem.json` |
| 2026-07-25 | (S027) **Defer the `E024` harvest** (the order-21 \(\mathcal G\) ladder rung, running since the S023 close) behind the `E028` ladder and the non-Hamiltonian stratum, instead of treating it as the first action. It stays running and untouched, excluded from every ledger row. | It is unfinished (part 9 of 16 at session close), and its likely outcome — an empty rung — moves an order floor without bearing on (F), which is the only live proof-side question below order 36. Its (F)-relevant content (new profile members as data points) has in any case been partly pre-empted: `E028` exhibited order-21 and order-22 profile objects directly, in minutes rather than a day, on the stratum where the argument lives. Recorded as a deliberate reordering of an inherited next action, per the strategic-autonomy rule, at the user's direction and on independent merit. | `A027`, `STATE.md`, `OBLIGATIONS.md` (`G015`), `problem.json`, `LOG.md` |
| 2026-07-25 | (S027 post-close review) **Re-order the `G015` moves: promote the `E024` harvest to first, stop the `E028` ladder after order 30, and make the non-Hamiltonian stratum (`A027` T5) the primary proof work.** This reverses the S027 opening judgement that the `E024` harvest was low-value. | The remaining risk in case (5b) is stratified by **shape, not by size**. `E028` cleared the Hamiltonian stratum at orders 16–29; the non-Hamiltonian stratum is cleared at *no* order, including the ones already done. So extending the ladder to 35 completes nothing — it widens the axis that is already the easy one — while `R003` F4 shows that above order 26 the ladder had stopped exercising (F)'s mechanism at all. What is now decisive is the **split**: does the exactly-two profile force a through-path covering every vertex? All eight independently generated profile objects do, but that is eight points at orders 19–20, and `C047`(e)'s control says only 22–29% of ordinary taut degree-2 pairs are Hamiltonian — so the profile may or may not force it. `E024` measures exactly that at order 21, over the *complete* class rather than the Hamiltonian-only slice `E028` can see. Cheap proxy available meanwhile on data already on disk: sweep the orders 18–20 classes by degree-2 count and see whether \(\max S=n-1\) becomes universal as that count falls toward two. | `A027` T5, `E028`/`C048`, `R003` F4, `C047`(e); `STATE.md`, `problem.json`, `LOG.md`, `OBLIGATIONS.md` (`G015`); the tooling half is `O012` |
| 2026-07-26 | Record that the (F) programme is a **finite-window instrument for an unbounded question**, and demote "extend the ladder / decide the stratum per order" from the proof-side route to a source of floors. | `L046` gives 2-connectivity of the case-(5b) residual object only below order 36, so (F) — even decided completely on both strata — closes case (5b) for \(n_0\le35\) and leaves \(n_0\ge36\) untouched: `G015` does not follow from any amount of laddering. `R003` F4 had already shown the ladder proves class-emptiness rather than poison forcing above order 26, and `L053` now shows the ambient class is nonempty at arbitrarily large order, so that verdict must fail at some order. The user's standing steer for this session (bias to proof-side work; "more computation is getting ridiculous, we're just moving boundaries") names the same thing the records had stopped saying out loud. | `A028` T9, `L053`, `OBLIGATIONS.md` `G015`, `STATE.md`, `problem.json`, `S030` |
| 2026-07-26 | Adopt **(INT) \(\wedge\) (L-A)** as the named order-unbounded architecture for case (5b), superseding `A026` T7's (L-A)/(L-B) split. | (INT) — \(S\supseteq[8,\max S]\) for a vertex-taut \(\{C_4,C_8\}\)-free exactly-two-profile pair — together with \(\max S\ge14\) yields \(14\in S\), which is poison by `L048`(iii); that is (F-S) at **every** order, with no window, no ladder and no \(C_{16}\) hypothesis. (L-B) is the special case of (INT) that matters, so the split is subsumed rather than abandoned. (INT) survived its first kill test (`C050`) with its constant pinned from both sides and a five-degree-2-vertex margin at the operative value 14. It is recorded as a **conjecture**: 1,920 corpus rows refute it the moment the profile hypothesis is dropped, and it may not be cited as support for any proved row. | `A028` T9, `C050`, `E029`, `OBLIGATIONS.md` `G015`, `STATE.md`, `S030` |
| 2026-07-26 | Retire "prove the Hamiltonian forcing (turn `C049`'s 27/27 into a lemma)" as a route at class strength, and record calibration object #3. | `L053`: a cubic non-Hamiltonian graph of girth \(\ge17\) minus an edge is an exactly-two-profile, 2-connected, \(\{C_4,C_8,C_{16}\}\)-free pair with no Hamiltonian through-path, so the class-level hypotheses do not force one. Any surviving form of the route must consume power-freeness, the poison condition, minimality, or an order bound — the same discipline Petersen\(-e\) imposed on the congruence route. The route was `S029`'s recorded best live alternative; killing it is why the non-Hamiltonian stratum is now unavoidable rather than optional. | `A028` T1, `L053`, `references/large-girth-non-hamiltonian-cubic-2026-07-26.md`, `STATE.md`, `S030` |
