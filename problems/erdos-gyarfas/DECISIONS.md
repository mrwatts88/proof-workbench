# Decision log

Append decisions that alter the statement, assumptions, conventions, proof
architecture, or disposition of a major approach.

| Date | Decision | Reason | Affected records |
|---|---|---|---|
| 2026-07-23 | Create statement version 0.1 for intake. | Initial dossier creation. | `STATEMENT.md`, `problem.json` |
| 2026-07-23 | Interpret the named conjecture as the finite simple undirected statement, with a simple cycle of length \(2^k\) for \(k\ge 2\). | This exposes the graph category and cycle convention without changing the standard substantive target; powers \(1\) and \(2\) cannot be simple-cycle lengths. | `STATEMENT.md`, `CLAIMS.md` |
| 2026-07-23 | Begin with an internal structural and exact-small-case attack; defer literature and current-status claims until sources are deliberately inspected. | This keeps imported facts separate from deductions and leaves provenance visibly unverified. | `STATEMENT.md`, `STATE.md`, `S001` |
| 2026-07-23 | After proving the order-\(9\) and order-\(10\) exclusions, stop using one-order-at-a-time enumeration as the main route. | The literature reports substantially stronger finite searches, while the order-\(11\) codegree budget already leaves twelve degree sequences; finite extension is evidence, not a scalable proof mechanism. | `A002`, `A003`, `E002`, `STATE.md` |
| 2026-07-23 | Import only precisely matched results from the inspected 2022–2026 primary papers, and keep the original 1997 source marked uninspected. | This distinguishes verified statement correspondence and usable external theorems from bibliographic or search-result reports. | `STATEMENT.md`, `CLAIMS.md`, `OBLIGATIONS.md`, `references/source-audit-2026-07-23.md` |
| 2026-07-23 | Stop the local induced-\(P_{13}\)/cubic-domination classification as a proposed \(C_{16}\)-forcing route. | `A004/L007` gives a connected cubic graph satisfying every structural conclusion used by the route while its girth excludes \(C_4,C_8,C_{16}\); further local enumeration would not repair the missing global input. | `A004`, `CLAIMS.md`, `OBLIGATIONS.md`, `STATE.md` |
| 2026-07-23 | Use edge-maximal power-cycle saturation as the primary global reframing, while retaining the Liu–Montgomery variable-length path method as the live alternative. | `L008` gives a certificate across every nonedge, `L009` shows that it is not shared by the prior bipartite cubic large-girth obstruction, and `L011` preserves the certificates inside the 2-connected leaf-block reduction. The route is not yet decisive because witness intersections and theta lengths remain uncontrolled. | `A005`, `CLAIMS.md`, `OBLIGATIONS.md`, `STATE.md` |
| 2026-07-23 | Do not treat one external ear and its three theta cycle lengths as the decisive saturation mechanism; retain the full Mersenne witness and its ordered intersections with the shortest odd cycle. | `L013` shows that an arbitrary ear is already forced by 2-connectivity, while `L014` gives infinite one-ear theta length triples with no power-of-two cycle. The extra information unique to saturation lies in the full \(2^k-1\)-edge witness. | `A005`, `CLAIMS.md`, `OBLIGATIONS.md`, `STATE.md` |
| 2026-07-23 | Retire single-witness saturation forcing entirely; repurpose `L008` as constraint supply, and make the order-\(11\) exhaustive search for saturated counterexamples the primary route, with the variable-length reframing kept as the live alternative. | `L015`–`L016` fire the recorded pivot trigger: full one- and two-excursion witness patterns both admit unconstrained infinite power-free families. Powers of two are exponentially sparse, so any bounded witness diagram with a free segment length admits power-free solutions; only unbounded witness coupling or an even-length interval mechanism can force. The trigger's recorded consequence named the variable-path reframing as the next primary; it is instead retained as the alternative because no small falsifiable first move for it at minimum degree \(3\) was identified, while at orders \(\le15\) saturation collapses to the finitely checkable condition "every nonedge has a path of length \(3\) or \(7\)", where an exhaustive order-\(11\) search is immediate, decisive either way, and informative for both routes. | `A005`, `CLAIMS.md`, `OBLIGATIONS.md`, `PROOF.md`, `STATE.md`, `problem.json` |
| 2026-07-23 | Drop the premise that `L008` saturation accelerates the finite search; run the frontier as a plain exhaustive \(\{C_4,C_8\}\)-free enumeration with saturation kept only as a survivor classifier. | The witness condition constrains completed graphs, not partial ones, so it cannot prune a generation tree; and at orders \(11\)–\(12\) the full \(\{C_4,C_8\}\)-free class proved empty, so the saturated subclass never arose and `L017` was proved without `L008`. Incremental \(C_8\) rejection, not saturation, is what makes the exhaustion cheap. | `A006`, `E004`, `CLAIMS.md`, `OBLIGATIONS.md`, `STATE.md` |
| 2026-07-23 | Stop the small-order exhaustion at thirteen; reach the published frontier by import, and make the novelty of the saturation line the deciding question for the next attack. | The user clarified the repository mission: lean on all existing verified knowledge to stand at the frontier instantly, and spend effort only on passing it; re-deriving reported computations is explicitly not a goal, and reported prior bounds already exceed the internal orders. The edge-maximal saturation reduction `L008` did not appear in any source audited in `S002`, so a deliberate literature check (`G011`) either makes strengthening it the frontier-passing target or imports its strongest published form. | `O008`, `OBLIGATIONS.md`, `STATE.md`, `problem.json`, `PROJECT_STATE.md`, `A006` |
| 2026-07-23 | Import the general finite bound at its primary-source strength — every counterexample has at least \(16\) vertices — and treat the circulating "at least 17" as an unsupported overread that must not be relied on. | Both primary sources agree: Royle's archived 2002 page says "I have checked this conjecture for graphs on up to 15 vertices," and Markström (2004) reports the same search as "all relevant graphs on less than 16 vertices." No inspectable source states an order-\(16\) search; Wikipedia and Hegde–Sandeep–Shashank repeat \(17\) without a matching primary statement. | `C012`, `references/source-audit-2026-07-23-S007.md`, `CLAIMS.md`, `STATE.md` |
| 2026-07-23 | Record the `G011` novelty verdict: the edge-maximal power-cycle saturation reduction appears nowhere in the swept literature, so strengthening the saturation line becomes the frontier-passing objective; the verdict carries a standing repeat-before-publishing caveat. | The recorded sweep (queries plus full-text inspection of every EGC-specific source held) found no saturation, edge-maximality, or Mersenne-witness formalism attached to this conjecture, and the cycle-saturation literature never treats the power-of-two family; one indexed sweep cannot prove absence, so the caveat stays attached to any external claim. | `G011`, `references/source-audit-2026-07-23-S007.md`, `STATE.md`, `problem.json` |
| 2026-07-23 | Verify leaned-on reported computations instead of trusting them: reproduce Markström's order-24 cubic census internally (E005) and re-derive the general orders 14-16 with an independent pipeline (E006) rather than importing blind; imported bounds whose reproduction would be expensive (cubic orders 26-28, Royle's degree-structured search) stay imported with source strength only. | The mission directs verification or refutation of merely reported results that the route will lean on; the 24-vertex graphs are the intended test material for the saturation line, and the general small orders gate the new bound, so their correctness must be internal; full re-derivation of everything else would be re-deriving known results for its own sake. | `E005`, `E006`, `CLAIMS.md`, `references/source-audit-2026-07-23-S007.md` |
| 2026-07-23 | Cap the exhaustive-search layer where it stops being cheap (about order 18 with the current pipeline) and add a named falsification-side route to the portfolio: the spectrum-density-versus-gap program — Bensmail-style confinement constructions (`C017`) against forced spectrum-density theorems (Bondy–Vince pairs; Sudakov–Verstraëte consecutive even lengths) inside the 2-connected block reduction, where confinement is impossible. | The user asked whether exhaustive checking is the likeliest path to settling the statement; it is not and was never priced as one — finite exclusions cannot touch the universal claim, and both deep routes are mechanistic. Erdős and Gyárfás themselves reportedly expected the conjecture to be false, Bensmail's constructions show exactly how bounded cycle spectra dodge sparse power sets and exactly why 1-connectivity is essential to the trick, so the decisive open tension is whether a 2-connected minimum-degree-3 spectrum can gap every power of two against the known density forcings. | `STATE.md`, `CLAIMS.md` (`C017`), `references/source-audit-2026-07-23-S007.md`, `S007` |
| 2026-07-23 | Amend the next action from positioning ("analyze test material, write a lemma target") to attempting: open the voltage-graph lift construction program on the falsification route with pre-named kill conditions, keeping the controlled-start interval lemma as the proof-side alternative and the order-18 census as capped support. | The user directed that missing tools be treated as internal research targets, now contract law via `O009`; the falsification route is generate-and-verify, the genre where this project's tooling is strongest, and voltage lifts give actual algebraic control of lifted cycle lengths, so a concrete construction attempt with kill conditions dominates pure target-sharpening on expected research value. The resolution estimate is unchanged — posture is not evidence. | `STATE.md`, `problem.json`, `S008` |
| 2026-07-23 | Close the cyclic/abelian arm of the voltage-lift program: its pre-named kill condition (a) fired, with the obstruction proved rather than merely observed. Retain the lift machinery (`L019`, `L020`, the walk-class sieve) and route the program's continuation to a fresh choice between the non-abelian sieve (odd-order groups 21, 27, where the obstruction vanishes at its root) and the proof-side interval lemma now fed by the `L021` length formula. | `L021`: commutator words are voltage-invisible over every abelian group and occur at power-of-two lengths on every tested base (8 dumbbell, 16 theta3/\(K_4\)/prism, 32 \(K_{3,3}\), 4 bouquet); `C020` makes the certificate verdict exhaustive over all \(m\ge2\) for the complete cycle-rank-2 base list plus \(K_4\), \(K_{3,3}\), prism, and `C021` verifies at truth level that the cyclic-lift universe of these bases contains no counterexample through order 30. Cyclicity was chosen for the no-wrap correspondence, but cyclic means abelian, and abelianization is precisely what blinds voltages to commutators — the leverage and the obstruction are the same fact. | `A008`, `E007`, `CLAIMS.md` (`L019`–`L021`, `C019`–`C021`), `OBLIGATIONS.md` (`G012`), `STATE.md`, `problem.json` |
| 2026-07-23 | Retire the voltage-certificate program as a counterexample generator for **all** finite groups, not only abelian ones, and continue it in-session past the original plan at the user's direction. The A009 route analysis replaced the recorded odd-order targets before running them: Feit–Thompson makes every odd-order group solvable, the solvable cascade (double commutators, power-twisted commutators) kills those predictably, so the decisive test was the perfect group \(A_5\) — which also died, exposing the true obstruction as the group-size collision wall rather than any group structure. Adopt proving the collision-wall lemma as the program's closing theorem and the refined `G012`; shift falsification-side weight toward the proof-side interval route, which inherits the wall's forcing heuristic. | `C022`: all assignments over orders 21, 27, 27, and 60 on all tested bases are certificate-dead by length 16, zero survivors — exactly the collision-wall prediction \(2\log_2\lvert\Gamma\rvert\) and independent of structure (solvable and perfect groups die identically). A lift must dodge powers up to \(n_B\lvert\Gamma\rvert\) while certificates die at \(\approx2\log_2\lvert\Gamma\rvert\): an exponential gap no group choice crosses. Structure only selects which of \(\{4,8,16\}\) bites first. | `A009`, `E008`, `CLAIMS.md` (`C022`), `OBLIGATIONS.md` (`G012` refined), `STATE.md`, `problem.json` |
