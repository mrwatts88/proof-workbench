# Proof obligations

An obligation is resolved only when the resolution column links to an exact proof,
counterexample, decision, or certificate.

| ID | Obligation | Status | Blocks | Resolution |
|---|---|---|---|---|
| G001 | Normalize the statement, definitions, quantifiers, negation, and boundary conventions. | resolved | All research | `STATEMENT.md` version 0.1; `DECISIONS.md` |
| G002 | Use or strengthen `L001` to prove the power-cycle conclusion for a 2-connected graph with at most one degree-\(2\) vertex and every other degree at least \(3\). | open | `C001` | — |
| G003 | Find a global mechanism that upgrades the absence of a \(4\)-cycle, plus the minimal-counterexample restrictions, into a forced cycle of length \(8,16,\ldots\). | open | `C001` | `L005`–`L006` close only two finite orders |
| G004 | Complete source correspondence by inspecting the original 1997 article body and, where needed, the original Markström computation rather than later reports. | in_progress | Full provenance and prior-bound verification; not the formal truth of version 0.1 | Markström (2004) now read in full and Royle's 2002 page recovered from the archive, fixing the true bounds (`C012`, `C013`) and exposing the "17" overread; only the 1997 article body remains uninspected after recorded access attempts (`references/source-audit-2026-07-23-S007.md`) |
| G005 | Extend exact \(C_4\)-free enumeration from order \(9\) through order \(10\), with auditable degree sequences and survivor structure. | resolved | Selection of the next structural lemma; not a universal proof by itself | `E002`, `A002/L005`, `A003/L006` |
| G006 | On the induced \(P_{13}\) forced by `C007`, classify how the cubic dominating set from `C005` can attach, and prove that a surviving attachment pattern yields a \(4\)-, \(8\)-, or \(16\)-cycle—or exhibit a pattern that refutes this route. | resolved | A scalable route to `C001` | `A004/L007` gives a complete cubic large-girth graph satisfying the structural bundle while avoiding all three lengths |
| G007 | Find a genuinely global restriction on a minimal counterexample that is not shared by arbitrary connected cubic graphs of girth at least \(17\), and use it noncircularly to force some power-of-two cycle. | in_progress | A replacement scalable route to `C001` | `L008`–`L016` supply, stress-test, and delimit the saturation restriction; by `L015`–`L016` its decisive use must couple unboundedly many witnesses or generate an interval of even cycle lengths, and the forcing step remains open. New quantitative input from `L024`: minimum degree \(3\) forces tailless nb closed **walks** at every length \(\equiv0\bmod4\) (every even length if non-bipartite) past a constant threshold, balanceable in any finite quotient at logarithmic cost. S011 scoped the walk-to-cycle conversion: no *unconditional* bounded transfer exists (`C024`'s ring carries all those walks with unbounded-ratio spectrum gaps), so the conversion must consume power-freeness itself; its sharp necessary form is the spread-doubling condition of `L025`/`G013` |
| G008 | In the saturated block from `L011`, convert the Mersenne-path witnesses and odd-cycle ears into adjustable even cycle lengths, or exhibit an exact overlap/length obstruction that retires this route. | resolved | Completion of the saturation route toward `C001` | `A005/L015`–`L016`: every one- and two-excursion full-witness pattern admits infinite power-free families, retiring single-witness forcing; the saturation facts `L008`–`L013` remain available as constraints |
| G009 | Determine whether any order-\(11\) saturated counterexample exists: exhaustively enumerate graphs on \(11\) vertices with minimum degree at least \(3\) and no \(C_4\) or \(C_8\) in which every nonedge is joined by a simple path of length \(3\) or \(7\); prove none exists — lifting every counterexample's order to at least \(12\) via `L006` and `L008` — or record the survivors. | resolved | The finite frontier and the next structural insight into saturated counterexamples | `E004` and `A006/L017` settle a stronger statement: at orders \(11\) and \(12\) even the full \(\{C_4,C_8\}\)-free class is empty, so no counterexample of either order exists and `L008` was not needed |
| G010 | Extend the exhaustive \(\{C_4,C_8\}\)-free search through orders \(13\), \(14\), and \(15\) while it stays exact and auditable; at the first order with survivors, record them verbatim, classify them by the `L008` saturation condition, and extract their structure. | not_needed | The finite frontier through order \(15\), where the small-order collapse ends | Retired by the mission clarification (`O008`): reported prior computations already exceed these orders, so extending adds no new information. Order \(13\) had already been cleared by `E004` and stands as `L017`. Superseded by `G004` and `G011`. |
| G011 | Determine from a deliberate literature sweep whether the edge-maximal power-cycle saturation reduction (`L008`, with `L009`–`L016`) appears in any published or preprint source; if absent, make strengthening the saturation line toward a publishable new partial result the frontier-passing target, and if present, import the strongest published form and rebase the route on it. | resolved | Selection of the frontier-passing attack; the project's candidate novel asset | Sweep recorded in `references/source-audit-2026-07-23-S007.md`: no appearance in the swept indexed literature or in any full-text-inspected EGC source; the saturation line is adopted as the frontier-passing target, with the standing caveat that the sweep must be repeated skeptically before any external novelty claim |
| G013 | Settle the atom question (`L025`, criterion generalized by `L034`): either exhibit an assembly witness — a 1-atom (power-free graph with exactly one sub-cubic vertex), or a power-free (D)-gadget whose through-set has some \(L\)-fold sumset (\(L\ge3\)) avoiding all powers of two (pinch/ratio-\(<2\) is one channel; all-odd \(S\), odd-prime gcd, and \(S\subseteq2+4\mathbb{Z}\) are others) — which disproves statement 0.1 by ring assembly; or close the assembly channels by proving the corresponding necessary conditions (spread-doubling for the pinched channel; an even through-path, power-of-2 gcd, etc. for the congruence channels). **Ceiling (A016 M6, corrected in S018 by `L036`):** the recorded form was circular — "closing every channel plus the 1-atom question" contains 0.1 as one of its halves. The non-circular statement is: closing every assembly channel **and** proving no *tight* 1-atom exists yields exactly the cubic reduction (`L029`), and nothing more. The proof-side deliverable of the whole programme is therefore `G015`, not 0.1. S012 refinement: spread-doubling in full is *equivalent* to statement 0.1 (`L026`), and the unrestricted \(s_{\min}\)-rungs are each conjecture-complete, so the ladder is meaningful only taut-relativized (`L027`/`L028`). S013 refinement: taut rungs \(s_{\min}\le3\) closed \(C_4\)-only (`L030`); every 2-atom with \(s_{\min}\le3\) routes through the 1-atom question. **S014 refinement (`L031`/`L032`): the taut half reduces exactly to the block question.** Live sub-questions: (a) the **tight 1-atom question** (rewritten in S018 after `L036`; structured in S019): does a power-free graph with exactly one sub-cubic vertex, that vertex of degree **exactly 2**, exist? The unrestricted 1-atom question of D-A4 is *conjecture-complete* (`L036`: a counterexample plus one pendant vertex is a 1-atom), so it is not a sub-question and is retired as a target; the tight version is strictly weaker as far as anything proved shows, is what `A012` T4 actually consumes, and is the lobe content of every non-taut 2-atom that matters. **S019 (`L039`–`L042`):** every tight 1-atom has order \(\ge17\); a minimum-order one is, under (R), the 2-path closure of a vertex-taut member of the class \(\mathcal G\) (both defect-neighbours cubic, `L039`'s bijection), 2-connected, non-bipartite, with forced power/Mersenne memberships in its through-set — and for `G015` only that case (5b) remains to exclude. Empty through order 15 stream-level (`C027`), through order 16 for the exact \(\mathcal G\) profile (`C036`, S020), **and through order 17 for `C027`'s entire class by the dedicated generator (`C039`, S021: the \(\le2\)-sub-cubic class is empty at 16 and 17; the exactly-two-degree-2 profile is empty at 17, class count 2,580)**; **S022 (`C043`) extends the profile ladder through order 19**: order 18 is profile-empty (class 108,447), and order 19 carries the **first-ever profile member** — `R???C@?GC_B?@_aAA_aP?W_?BO@Gc?`, vertex-taut, 2-connected, non-bipartite, but \(C_{16}\)-blocked (46 of them) **and** \(S\)-condition-violating (\(S=\{5..18\}\ni6,14\)) — doubly excluded, with the 1-bucket (tight-1-atom shape) empty at both orders directly by generation; **the S022 follow-up harvest spent order 20 too** (`C043` extension: profile 7, all \(C_{16}\)-blocked, zero power-free, 0-/1-buckets empty), so **every \(\mathcal G\)-member has order \(\ge21\) and every tight 1-atom has order \(\ge22\)** (cases (4)/(5) of `L041` propagate `L047` and the \(\mathcal G\) floor; the direct generation route certifies \(\ge21\)), and the chain branch of case (5b) is confined to \(n_0\ge36\) (`L046`, S021), leaving the residual object 2-connected below that; the blocking structure at the frontier is now itself mapped: every blocker of every closest known object is a two-through-path interference cycle (`C041`), the property is empirically exactly vertex-tautness-shaped (`C042`), and the interference calculus `L048` converts power-freeness into through-path arithmetic under the conjectured completeness (`A023` T5); for bipartite graphs empty through order 24 (`C034`; order 23 in the S019 follow-up harvest, order 24 in the S021 one, by the `E019` generator); the congruence-obstruction leg against the case-(5b) residual object is retired (`C037`/`A021` — kill fired on Petersen\(-e\); the chain-cancellation tension is now spent as the S021 theorem package `L043`–`L046` — quantified, and provably unable to exclude the chain case from memberships alone — so the surviving proof-side levers are the 2-connected branch's \(C_8\)-interference structure and whatever makes power-freeness itself fight, with `L044` at filter strength); (b′) the **block question** — does a power-free vertex-taut 2-connected core (non-terminal degrees \(\ge3\), terminal degrees \(\ge2\)) with \(s_{\max}\le2\,s_{\min}\) exist? Taut 2-atoms exist iff such a block does (`L032`), so a block witness disproves 0.1 by lift and ring. Catalogue through order 14 (`C030`/`C031`): the strict taut world is exactly six pendant lifts; the block world is five 2-connected cores, **all at exact equality \(s_{\max}=2s_{\min}\)** (bands 4,5,5,5,6, the band-4 block being Petersen minus an edge), each blocked from power-freeness only by \(C_8\), every \(C_8\) a two-through-path symmetric difference. Open block rungs: strict blocks at \(s_{\min}\ge4\) (none known; empty through **16**, `C035`), and equality-block power-forcing (\(C_4\)-only closure impossible from band 4 up — the five blocks exist — so the power spectrum itself must fight here). A power-free block needs order \(\ge16\) (`C027`). **S015 refinement (`L033`): the band-4 case is exactly the pencil endgame** — either a band-4 core has two internally disjoint 4-paths (= a \(C_8\) through both terminals, killing power-freeness) or its 4-paths form a rigid pencil (fan at a terminal neighbor, or a unique shortest path); proving no pencilled band-4 taut 2-connected core exists closes band 4 outright. Empty through order 16 (`C032`/`C035` — the S017 harvest: twelve boundary hits, three new equality blocks F16/G16/H16 raising the block catalogue to eight, all at exact equality, all band-4 cores disjoint-type, no strict pair at 16); \(C_4\)-freeness essential to both rungs (`C033`); the band-2 closed rung (\(S\subseteq\{2,3,4\}\), \(4\in S\)) is the remaining \(C_4\)-only gap below band 4. **S015 second refinement (`L034`, external memo audited in `A016`): the pinched channel is not the whole assembly interface.** New live sub-question **(c) the parity/congruence channels**: does a power-free (D)-gadget exist with all-odd \(S\) (equivalently at its bipartite instance: a bipartite graph, terminals in opposite parts, min degree 3 off-terminals, girth \(\ge6\), no \(C_8/C_{16}\)), or odd-prime gcd, or \(S\subseteq2+4\mathbb{Z}\)? Any hit disproves 0.1 by an odd ring. All channels are empty through order 15 (`C027` is stream-level and profile-agnostic; through 18 for both-terminals-\(\ge3\) via `L022`/`C023`), but the taut *shape* data for the new channels was never scanned (banded scanners cannot see e.g. \(S=\{3,7\}\)); the all-odd ladder lives at odd bands 3, 5, 7 only, disjoint from the band-4 pencil battlefield. The parity channel has no pendant/block reduction theory yet (the pendant shift flips parity). **S016 refinement (`L035`, `A017`): sub-question (c) splits.** Channels (i) and (iii) *are* the bipartite channel — for vertex-taut gadgets, parity-constant \(S\) is equivalent to bipartiteness (T2), so any connected bipartite power-free graph with \(\le2\) sub-cubic vertices disproves 0.1 with no path enumeration (T3), and bipartite generation is exhaustive for those channels modulo 1-atoms (T4). `E015`/`C034` then closes them through order 24 (25 with a pendant; the order-22 leg completed in S018, the order-23 leg in the S019 follow-up harvest) — eight orders past the general wall — with every member of the class carrying a \(C_8\) and the minimum \(C_8\) count never below 13. What remains open in (c): channel (ii), the odd-prime \(\gcd\) channel, which is **not** bipartite-forced (\(\Theta(3,3,3)\)) and has no structure theorem, no reduction, and no dedicated search; and a parity analogue of `L032`'s 2-connected block extraction (T0 gives the chain degree-free, but the excess arithmetic that isolates one block has no parity counterpart — an odd chain can be carried entirely by bridges). | open | The reframed core of `G007`; the project's live disproof channel | `C027` excludes atoms, blocks, and all `L034`-channel gadgets through order 15; `L028`/`L030` close taut rungs \(s_{\min}\le3\); `L031`/`L032` collapse the taut *pinched* side onto blocks; `C030`/`C031` give the exact finite catalogue through order 14; `L033`/`C032`/`C033` reduce band 4 to the pencil endgame and extend the catalogue through order 15; `C035` (S017) extends it through order 16 (eight equality blocks, no strict pair, endgame base and interference census intact); `L034`/`A016` widen the criterion and open the congruence channels; `L035`/`C034` identify channels (i)/(iii) with the bipartite class and close them through order 23 |
| G014 | Audit and import (or refute the relevance of) the external leads surfaced by the 2026-07-24 memo, each with precise statement, matched hypotheses, and a source in `references/`: (1) Chen–Saito — minimum degree 3 forces a cycle of length \(\equiv0\bmod3\); (2) the bipartite counterexample lower bound \(\ge32\) attributed to Nowbandegani–Esfandiari, and the reported bipartite-EGC verification to 31 vertices (this gates the Mersenne-window logic of the bipartite gadget hunt) — source named by the user in S021+: "An Experimental Result on the Erdős–Gyárfás Conjecture in Bipartite Graphs" (Nowbandegani et al.; cited internally there as [NoEs11]), still unfetched and unaudited; note the bound's scope is bipartite graphs with **all** degrees \(\ge3\), so it does not cover the `E015`/`E019` gadget class (\(\le2\) degree-2 vertices) at orders 24–31, whose hits disprove 0.1 through non-bipartite ring assemblies (`L035` T3) — the S016 de-gating stands; (3) Heckman–Krakovski — EGC for 3-connected cubic planar graphs; (4) Shauger and Daniel–Shauger — EGC for classes excluding large stars; (5) census availability and provenance: cubic vertex-transitive graphs to order 1280, cubic girth-\(\ge9\) lists (58–64 vertices), snark and cage collections; (6) Markström's 2004 generator method (canonical construction path with incremental \(C_8\) rejection) as the design reference for a dedicated \(\{C_4,C_8\}\)-free generator. Until audited, none of these may support a proof step or a coverage claim. **S016 update:** item (2) is no longer gating. `L035` T3 makes any bipartite power-free near-cubic graph a disproof outright, so the bipartite hunt never needed the Mersenne-window logic the \(\ge32\) bound supported; and `C034` verifies the bipartite instance of 0.1 internally at orders \(\le23\) (S019 follow-up harvest), so the reported external verification is corroborated in that range rather than imported. Items (1), (3)–(6) still gate their threads (the cubic census extension, census mining, and the Chen–Saito/Heckman–Krakovski/Shauger lines), and item (2) still needs auditing before any claim about orders 25–31 (order 24 is now internally verified, `C034`/S021 harvest). **S021 update:** item (6) is **discharged as an instrument** — the generator is built, verified 23 ways against the independent pipeline plus an external positive control, and in production (`E019`/`C039`); Markström's method entered as a *design* only, with no external result imported through it. New item (7): the MathOverflow-512914 thread (question body unread; direct fetch blocked in-harness) — its cubic-order-20 figures are corroborated or being recounted internally, but its "cubic therefore minimum-degree-3" step is **invalid as quoted** (its general form is exactly the open `G015`) and may support nothing; see `references/mathoverflow-512914-audit-2026-07-24.md`. The internal `E019` stage-B follow-up run supersedes the thread's order-20 claim either way. | open | The cubic census extension, the census-mining leg, and the partial-case imports (no longer the bipartite gadget hunt) | `L035`/`C034` de-gate item (2) for the bipartite hunt |
| G012 | Prove the collision-wall lemma, closing the voltage-certificate program for all finite groups with an effective bound: for every finite group \(\Gamma\), every connected base of minimum degree \(\ge3\), and every voltage assignment, there is a tailless non-backtracking closed walk of length \(2^k\) with identity net voltage for every \(2^k\ge C\log_2\lvert\Gamma\rvert+C_B\) — via composition of same-voltage walk pairs (\(W\cdot\overline{W'}\)), where only the first/last-arc diversity bookkeeping remains open (`A009` also records why coset confinement cannot dodge: undirectedness forces confinement period \(\le2\)). A proof retires the certificate route permanently and hands `G007` its quantitative core; a counterexample family would be a new value-confinement lever and would reopen construction. Part (b) of the original formulation is resolved: the non-abelian data exists (`C022`) — orders 21, 27, and \(A_5\) all certificate-dead by 16, killing the group-structure escape entirely. | resolved | Closing theorem of the lift program; quantitative input to `G007` | Proved as `L024` (`A010` Theorem W7, \(C=4\), \(C_B=4\lceil\log_2m\rceil+4R_B+24\)), with the arc-digraph package `L023`; construction verified by `E009`; delegated logic audit `R001` passed (minor findings repaired in place). Caveats inherited by `G007` (per `R001` F3/F4): the certificate-closure corollary is per-base, for \(\lvert\Gamma\rvert\ge\Gamma_0(B)\), alongside the finite verdicts `C020`/`C022`; and the forced even lengths are \(\equiv0\bmod4\) for bipartite bases (all even lengths only when non-bipartite) — powers of two are unaffected |
| G015 | **The cubic reduction as a named deliverable (opened S018; both routes advanced in S019).** Prove, unconditionally, that statement 0.1 holds iff it holds for cubic graphs — equivalently that every minimum-order-then-size counterexample is cubic. **S019 state (orchestrated parallel run, both legs audited):** **R1** is reduced to a *single local configuration*: by `L040`/`L041`, "a tight 1-atom yields a cubic counterexample" — which is all the reduction needs, and strictly easier than "no tight 1-atom exists" (that unconditional form is retired as an R1 target; its reducts are counterexamples, not atoms) — holds **unless** a minimum-order tight 1-atom realizes case (5b): defect vertex non-cut, both neighbours cubic, \(B-u\) vertex-taut, i.e. a member of `L039`'s class \(\mathcal G\) closed by a 2-path. Excluding case (5b) closes `G015`. The residual object is heavily constrained (`L042`: 2-connected, degree-\(\ge4\) independent, non-bipartite, forced Mersenne/power memberships in \(S\), chain cancellation, band-4 pencil) and every tight 1-atom has order \(\ge17\) (`L041`). **R2's verification prerequisite is discharged** — Carr's four results verified line-by-line, `C004`–`C006` now carried at verified strength (`references/carr-2026-verification-2026-07-24.md`) — and the density bound is improved to \(3\lvert V_3\rvert\ge2n+3\) (`L038`, via the subdivision descent `L037`); but the **constant route is closed as a path to `G015`**: S15 certifies \(2/3\) as the exact ceiling of the non-power hypotheses, the proved constraint set caps at \(2/3+O(1/n)\), and no constant \(<1\) delivers the reduction (`A020` W2-T7/T8). Surviving R2 extension: the internally-disjoint longer-link descent (`A020` W2-T8(c)). The routes **meet**: a one-defect subgraph of the link graph of any minimal counterexample is exactly a tight 1-atom (`A020` W2-T8(d)). **S020 update:** move (i), the order-16 \(\mathcal G\)-profile scan, is **done and empty** (`C036`, `E018`: 29,713,305 profile members at 16, all \(C_8\)-blocked, zero power-free) — every tight 1-atom now has order \(\ge18\); and move (ii), the congruence-type obstruction, is **dead** — its pre-registered kill condition fired (`C037`/`A021`: the forced-membership profile is realized from order 10, by Petersen\(-e\) among others, with no residue structure at any modulus; the chain calculus caps congruence information at parity). Live moves, in recorded order: (i) the **chain-cancellation tension** of `L042`/W1-T14 (Minkowski-additive across the block chain — the one proof-side mechanism the kill does not touch; first falsifiable step: quantify the cancellation and try to bound the chain length or force 2-connectivity of \(H\), with Petersen\(-e\) as the standing sanity check — any exclusion argument must fail on it unless it invokes power-freeness or minimality); (ii) the disjoint long-link descent (`A020` W2-T8(c)); (iii) the search ladder at order 17 — same decisive shape as 16 but \(\sim\!17\times\) the stream (\(\sim\!6\) e9 graphs), for which the `G014` item-6 \(\{C_4,C_8\}\)-free generator is the right instrument (the \(C_8\) filter was 100% decisive at every order ever scanned in this profile; minimum \(C_8\) count at 16 is **1**, so the class is one cycle away from a witness in the crude counting sense). **S021 update (both legs spent productively):** the chain-cancellation move is now a theorem package (`L043`–`L046`): the tension is *quantified* (`L045` — exactly three exponent-disjointness conditions per cut and nothing more, so the forced memberships alone can **never** exclude the chain case), every prefix/suffix is forced to meet all three families and to be non-bipartite (`L043`), and the order dichotomy `L046` confines the chain case to \(n_0\ge32\) (\(\ge36\) with `C039`) — **the 2-connected branch is the entire case-(5b) frontier below order 36**; the chain-case kill test was refuted in its searched range with a single identified mechanism (`C038`; named kill rung: (2,2)-profile blocks at orders 15–16, testing whether \(\{7,8,12,13,14\}\)-shaped through-sets are realized). The `G014` item-6 generator is **built, verified, and in production** (`E019`/`C039`) and move (iii) is **spent empty at 17**: every tight 1-atom now has order \(\ge19\), every \(\mathcal G\)-member \(\ge18\). Live moves, in order: (i) the 2-connected branch — \(C_8\)-interference structure (the `C037` diagnosis), with `L044` (terminal power saturation) joining the generation-time filter stack at filter strength; (ii) the search ladder at orders 18–20 via `E019` (\(\approx\)4 min / 25 min / 2.8 h on 8 workers); (iii) the `C038` kill rung at block orders 15–16; (iv) the disjoint long-link descent (`A020` W2-T8(c), unchanged). **S022 update (both legs run to verdicts in one orchestrated session):** move (i) landed the **interference outcome** — `C041`: all 553 blockers of the closest known objects (the min-\(C_8\) exemplars at 14–16, exhaustively extracted, and the three-degree-2 \(C_{16}\) boundary at 16–17) are two-through-path symmetric differences, so the non-interference pivot trigger did **not** fire; the property is empirically **exactly vertex-tautness-shaped** (`C042`: exact biconditional at orders 10–12; zero failures over all 12,313 taut pairs of all connected graphs of orders 4–7), and the calculus is fixed (`L048`: interference = the \(t=1\), leak-**pinned** case of `A021` T1's identity, and under interference-completeness \(\mathrm{Spec}(B)=T_1(H,a,b)\cup(S+2)\) — the atom's power-freeness becomes through-path arithmetic, the genre that survives both kill theorems `C037`/`L045`). Move (ii) spent rungs 18 and 19 (`C043`): order 18 profile-empty; order 19 the **first nonempty profile rung**, its unique member vertex-taut but \(C_{16}\)-blocked **and** \(S\)-violating. **S022 follow-up harvest:** the order-20 rung is spent too (`C043` extension: profile 7, all \(C_{16}\)-blocked, none power-free; 0-/1-buckets empty) — **every \(\mathcal G\)-member has order \(\ge21\), every tight 1-atom \(\ge22\)** (`L041` cases (4)/(5) propagating `L047` and the \(\mathcal G\) floor; the direct generation route certifies \(\ge21\)) — and **T5 survived its first two kill rungs**: the order-19 exemplar (411/411 cycles decompose, stage A, determined-partner algorithm) and the three on-disk order-20 profile members (1,890/1,890, `t5_n20_profile.json`; all three \(S\ni6,14\) — the double blocking repeats at every profile object seen). **Both residual follow-ups landed (S022 second harvest):** the four part-14 members are recovered, identified, and T5-tested — **all eight profile objects in existence survive T5, 4,661/4,661 cycles, every one with the \(S\ni\{6,14\}\) double blocking** (`C042`(e)) — and the min-degree-3 order-21 sweep is **empty**, so **every counterexample has \(\ge22\) vertices** (`L047`, window \([22,24]\); atom floors unchanged at 22/21 — `L041` case (5) still binds at 22). Live moves now, in order: (i) the **T5 program** (`A023`): remaining kill rungs — `smallworld 13` (minutes), a sparse general probe at orders 8–9; if T5 survives, the **proof attempt** (clean-window reroute + minimal-choice exchange; named sub-obligations: cycle-edge essentiality in taut pairs, weaving control); (ii) the forcing target **(F)** (`A023` T6, order-windowed \([18,35]\), calibrated on Petersen\(-e\) **and** the order-14 exemplar `M?AA@?WcKWHOWOL??`; all eight profile members realize the 16-collision and the \(S\)-violation **simultaneously** — (F)-shaped data); (iii) the \(\mathcal G\) rung at order 21 (\(\approx\)18 h on 8 workers) and the min-degree-3 rung at order 22 (\(\approx\)38 h) — both deliberate decisions once the cheap rungs are spent; (iv) the `C038` kill rung at block orders 15–16 and the disjoint long-link descent (unchanged). | open | The programme's proof-side deliverable; the value of Thread C's cubic searches; the corrected reading of `G013`'s ceiling | `L029`, `L036`–`L042`, `C004`–`C006` (verified), `C027`, `C036`, `C037`, `L022`; attempts `A019`/`A020`/`A021`, experiments `E016`/`E017`/`E018` |
