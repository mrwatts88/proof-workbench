# Proof obligations

An obligation is resolved only when the resolution column links to an exact proof,
counterexample, decision, or certificate.

| ID | Obligation | Status | Blocks | Resolution |
|---|---|---|---|---|
| G001 | Normalize the statement, definitions, quantifiers, negation, and boundary conventions. | resolved | All research | `STATEMENT.md` version 0.1; `DECISIONS.md` |
| G002 | Use or strengthen `L001` to prove the power-cycle conclusion for a 2-connected graph with at most one degree-\(2\) vertex and every other degree at least \(3\). | open | `C001` | — |
| G003 | Find a global mechanism that upgrades the absence of a \(4\)-cycle, plus the minimal-counterexample restrictions, into a forced cycle of length \(8,16,\ldots\). | open | `C001` | `L005`–`L006` close only two finite orders |
| G004 | Complete source correspondence by inspecting the original 1997 article body and, where needed, the original Markström computation rather than later reports. | in_progress | Full provenance and prior-bound verification; not the formal truth of version 0.1 | Markström (2004) now read in full and Royle's 2002 page recovered from the archive, fixing the true bounds (`C012`, `C013`) and exposing the "17" overread; only the 1997 article body remains uninspected after recorded access attempts (`references/source-audit-2026-07-23-S007.md`) |
| G005 | Extend exact \(C_4\)-free enumeration from order \(9\) through order \(10\), with auditable degree sequences and survivor structure. | resolved | Selection of the next structural lemma; not a universal proof by itself | `E002`, `A002/L005`, `A003/L006` |
| G006 | On the induced \(P_{13}\) forced by `C007`, classify how the cubic dominating set from `C005` can attach, and prove that a surviving attachment pattern yields a \(4\)-, \(8\)-, or \(16\)-cycle—or exhibit a pattern that refutes this route. | resolved | A scalable route to `C001` | `A004/L007` gives a complete cubic large-girth graph satisfying the structural bundle while avoiding all three lengths |
| G007 | Find a genuinely global restriction on a minimal counterexample that is not shared by arbitrary connected cubic graphs of girth at least \(17\), and use it noncircularly to force some power-of-two cycle. | in_progress | A replacement scalable route to `C001` | `L008`–`L016` supply, stress-test, and delimit the saturation restriction; by `L015`–`L016` its decisive use must couple unboundedly many witnesses or generate an interval of even cycle lengths, and the forcing step remains open. New quantitative input from `L024`: minimum degree \(3\) forces tailless nb closed **walks** at every length \(\equiv0\bmod4\) (every even length if non-bipartite) past a constant threshold, balanceable in any finite quotient at logarithmic cost. S011 scoped the walk-to-cycle conversion: no *unconditional* bounded transfer exists (`C024`'s ring carries all those walks with unbounded-ratio spectrum gaps), so the conversion must consume power-freeness itself; its sharp necessary form is the spread-doubling condition of `L025`/`G013` |
| G008 | In the saturated block from `L011`, convert the Mersenne-path witnesses and odd-cycle ears into adjustable even cycle lengths, or exhibit an exact overlap/length obstruction that retires this route. | resolved | Completion of the saturation route toward `C001` | `A005/L015`–`L016`: every one- and two-excursion full-witness pattern admits infinite power-free families, retiring single-witness forcing; the saturation facts `L008`–`L013` remain available as constraints |
| G009 | Determine whether any order-\(11\) saturated counterexample exists: exhaustively enumerate graphs on \(11\) vertices with minimum degree at least \(3\) and no \(C_4\) or \(C_8\) in which every nonedge is joined by a simple path of length \(3\) or \(7\); prove none exists — lifting every counterexample's order to at least \(12\) via `L006` and `L008` — or record the survivors. | resolved | The finite frontier and the next structural insight into saturated counterexamples | `E004` and `A006/L017` settle a stronger statement: at orders \(11\) and \(12\) even the full \(\{C_4,C_8\}\)-free class is empty, so no counterexample of either order exists and `L008` was not needed |
| G010 | Extend the exhaustive \(\{C_4,C_8\}\)-free search through orders \(13\), \(14\), and \(15\) while it stays exact and auditable; at the first order with survivors, record them verbatim, classify them by the `L008` saturation condition, and extract their structure. | not_needed | The finite frontier through order \(15\), where the small-order collapse ends | Retired by the mission clarification (`O008`): reported prior computations already exceed these orders, so extending adds no new information. Order \(13\) had already been cleared by `E004` and stands as `L017`. Superseded by `G004` and `G011`. |
| G011 | Determine from a deliberate literature sweep whether the edge-maximal power-cycle saturation reduction (`L008`, with `L009`–`L016`) appears in any published or preprint source; if absent, make strengthening the saturation line toward a publishable new partial result the frontier-passing target, and if present, import the strongest published form and rebase the route on it. | resolved | Selection of the frontier-passing attack; the project's candidate novel asset | Sweep recorded in `references/source-audit-2026-07-23-S007.md`: no appearance in the swept indexed literature or in any full-text-inspected EGC source; the saturation line is adopted as the frontier-passing target, with the standing caveat that the sweep must be repeated skeptically before any external novelty claim |
| G013 | Settle the atom question (`L025`, criterion generalized by `L034`): either exhibit an assembly witness — a 1-atom (power-free graph with exactly one sub-cubic vertex), or a power-free (D)-gadget whose through-set has some \(L\)-fold sumset (\(L\ge3\)) avoiding all powers of two (pinch/ratio-\(<2\) is one channel; all-odd \(S\), odd-prime gcd, and \(S\subseteq2+4\mathbb{Z}\) are others) — which disproves statement 0.1 by ring assembly; or close the assembly channels by proving the corresponding necessary conditions (spread-doubling for the pinched channel; an even through-path, power-of-2 gcd, etc. for the congruence channels). **Ceiling (A016 M6):** closing every assembly channel plus the 1-atom question does not prove 0.1 — it closes the assembly interface and (via `L029`) reduces 0.1 to cubic graphs; only the 1-atom half has direct proof-side yield. S012 refinement: spread-doubling in full is *equivalent* to statement 0.1 (`L026`), and the unrestricted \(s_{\min}\)-rungs are each conjecture-complete, so the ladder is meaningful only taut-relativized (`L027`/`L028`). S013 refinement: taut rungs \(s_{\min}\le3\) closed \(C_4\)-only (`L030`); every 2-atom with \(s_{\min}\le3\) routes through the 1-atom question. **S014 refinement (`L031`/`L032`): the taut half reduces exactly to the block question.** Live sub-questions: (a) the **1-atom question** — the lobe content of every non-taut 2-atom, and by `L029` the sole obstruction to reducing 0.1 to cubic graphs; (b′) the **block question** — does a power-free vertex-taut 2-connected core (non-terminal degrees \(\ge3\), terminal degrees \(\ge2\)) with \(s_{\max}\le2\,s_{\min}\) exist? Taut 2-atoms exist iff such a block does (`L032`), so a block witness disproves 0.1 by lift and ring. Catalogue through order 14 (`C030`/`C031`): the strict taut world is exactly six pendant lifts; the block world is five 2-connected cores, **all at exact equality \(s_{\max}=2s_{\min}\)** (bands 4,5,5,5,6, the band-4 block being Petersen minus an edge), each blocked from power-freeness only by \(C_8\), every \(C_8\) a two-through-path symmetric difference. Open block rungs: strict blocks at \(s_{\min}\ge4\) (none known; empty through **16**, `C035`), and equality-block power-forcing (\(C_4\)-only closure impossible from band 4 up — the five blocks exist — so the power spectrum itself must fight here). A power-free block needs order \(\ge16\) (`C027`). **S015 refinement (`L033`): the band-4 case is exactly the pencil endgame** — either a band-4 core has two internally disjoint 4-paths (= a \(C_8\) through both terminals, killing power-freeness) or its 4-paths form a rigid pencil (fan at a terminal neighbor, or a unique shortest path); proving no pencilled band-4 taut 2-connected core exists closes band 4 outright. Empty through order 16 (`C032`/`C035` — the S017 harvest: twelve boundary hits, three new equality blocks F16/G16/H16 raising the block catalogue to eight, all at exact equality, all band-4 cores disjoint-type, no strict pair at 16); \(C_4\)-freeness essential to both rungs (`C033`); the band-2 closed rung (\(S\subseteq\{2,3,4\}\), \(4\in S\)) is the remaining \(C_4\)-only gap below band 4. **S015 second refinement (`L034`, external memo audited in `A016`): the pinched channel is not the whole assembly interface.** New live sub-question **(c) the parity/congruence channels**: does a power-free (D)-gadget exist with all-odd \(S\) (equivalently at its bipartite instance: a bipartite graph, terminals in opposite parts, min degree 3 off-terminals, girth \(\ge6\), no \(C_8/C_{16}\)), or odd-prime gcd, or \(S\subseteq2+4\mathbb{Z}\)? Any hit disproves 0.1 by an odd ring. All channels are empty through order 15 (`C027` is stream-level and profile-agnostic; through 18 for both-terminals-\(\ge3\) via `L022`/`C023`), but the taut *shape* data for the new channels was never scanned (banded scanners cannot see e.g. \(S=\{3,7\}\)); the all-odd ladder lives at odd bands 3, 5, 7 only, disjoint from the band-4 pencil battlefield. The parity channel has no pendant/block reduction theory yet (the pendant shift flips parity). **S016 refinement (`L035`, `A017`): sub-question (c) splits.** Channels (i) and (iii) *are* the bipartite channel — for vertex-taut gadgets, parity-constant \(S\) is equivalent to bipartiteness (T2), so any connected bipartite power-free graph with \(\le2\) sub-cubic vertices disproves 0.1 with no path enumeration (T3), and bipartite generation is exhaustive for those channels modulo 1-atoms (T4). `E015`/`C034` then closes them through order 21 (22 with a pendant; an order-22 run was launched and left unfinished) — past the general order-15 wall — with every member of the class carrying a \(C_8\) and the minimum \(C_8\) count never below 13. What remains open in (c): channel (ii), the odd-prime \(\gcd\) channel, which is **not** bipartite-forced (\(\Theta(3,3,3)\)) and has no structure theorem, no reduction, and no dedicated search; and a parity analogue of `L032`'s 2-connected block extraction (T0 gives the chain degree-free, but the excess arithmetic that isolates one block has no parity counterpart — an odd chain can be carried entirely by bridges). | open | The reframed core of `G007`; the project's live disproof channel | `C027` excludes atoms, blocks, and all `L034`-channel gadgets through order 15; `L028`/`L030` close taut rungs \(s_{\min}\le3\); `L031`/`L032` collapse the taut *pinched* side onto blocks; `C030`/`C031` give the exact finite catalogue through order 14; `L033`/`C032`/`C033` reduce band 4 to the pencil endgame and extend the catalogue through order 15; `C035` (S017) extends it through order 16 (eight equality blocks, no strict pair, endgame base and interference census intact); `L034`/`A016` widen the criterion and open the congruence channels; `L035`/`C034` identify channels (i)/(iii) with the bipartite class and close them through order 21 |
| G014 | Audit and import (or refute the relevance of) the external leads surfaced by the 2026-07-24 memo, each with precise statement, matched hypotheses, and a source in `references/`: (1) Chen–Saito — minimum degree 3 forces a cycle of length \(\equiv0\bmod3\); (2) the bipartite counterexample lower bound \(\ge32\) attributed to Nowbandegani–Esfandiari, and the reported bipartite-EGC verification to 31 vertices (this gates the Mersenne-window logic of the bipartite gadget hunt); (3) Heckman–Krakovski — EGC for 3-connected cubic planar graphs; (4) Shauger and Daniel–Shauger — EGC for classes excluding large stars; (5) census availability and provenance: cubic vertex-transitive graphs to order 1280, cubic girth-\(\ge9\) lists (58–64 vertices), snark and cage collections; (6) Markström's 2004 generator method (canonical construction path with incremental \(C_8\) rejection) as the design reference for a dedicated \(\{C_4,C_8\}\)-free generator. Until audited, none of these may support a proof step or a coverage claim. **S016 update:** item (2) is no longer gating. `L035` T3 makes any bipartite power-free near-cubic graph a disproof outright, so the bipartite hunt never needed the Mersenne-window logic the \(\ge32\) bound supported; and `C034` verifies the bipartite instance of 0.1 internally at orders \(\le21\), so the reported external verification is corroborated in that range rather than imported. Items (1), (3)–(6) still gate their threads (the cubic census extension, census mining, and the Chen–Saito/Heckman–Krakovski/Shauger lines), and item (2) still needs auditing before any claim about orders 22–31. | open | The cubic census extension, the census-mining leg, and the partial-case imports (no longer the bipartite gadget hunt) | `L035`/`C034` de-gate item (2) for the bipartite hunt |
| G012 | Prove the collision-wall lemma, closing the voltage-certificate program for all finite groups with an effective bound: for every finite group \(\Gamma\), every connected base of minimum degree \(\ge3\), and every voltage assignment, there is a tailless non-backtracking closed walk of length \(2^k\) with identity net voltage for every \(2^k\ge C\log_2\lvert\Gamma\rvert+C_B\) — via composition of same-voltage walk pairs (\(W\cdot\overline{W'}\)), where only the first/last-arc diversity bookkeeping remains open (`A009` also records why coset confinement cannot dodge: undirectedness forces confinement period \(\le2\)). A proof retires the certificate route permanently and hands `G007` its quantitative core; a counterexample family would be a new value-confinement lever and would reopen construction. Part (b) of the original formulation is resolved: the non-abelian data exists (`C022`) — orders 21, 27, and \(A_5\) all certificate-dead by 16, killing the group-structure escape entirely. | resolved | Closing theorem of the lift program; quantitative input to `G007` | Proved as `L024` (`A010` Theorem W7, \(C=4\), \(C_B=4\lceil\log_2m\rceil+4R_B+24\)), with the arc-digraph package `L023`; construction verified by `E009`; delegated logic audit `R001` passed (minor findings repaired in place). Caveats inherited by `G007` (per `R001` F3/F4): the certificate-closure corollary is per-base, for \(\lvert\Gamma\rvert\ge\Gamma_0(B)\), alongside the finite verdicts `C020`/`C022`; and the forced even lengths are \(\equiv0\bmod4\) for bipartite bases (all even lengths only when non-bipartite) — powers of two are unaffected |
