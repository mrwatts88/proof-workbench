# Claim ledger

Use stable IDs and the states in `process/records.md`. Split claims until each row
has one independently checkable assertion.

| ID | Type | Assertion | State | Support | Dependencies |
|---|---|---|---|---|---|
| C001 | main claim | Every finite simple undirected graph of minimum degree at least \(3\) contains a cycle of length \(2^k\) for some \(k\ge2\); see `STATEMENT.md` version 0.1. | proposed | — | `D001`–`D004` |
| L001 | reduction | Every counterexample contains a 2-connected power-cycle-free subgraph in which all but possibly one vertex have internal degree at least \(3\), and the possible exception has internal degree at least \(2\). | proved | `A001/L001` | `D001`–`D004` |
| L002 | structural lemma | An edge-minimal counterexample is connected, every edge has a degree-\(3\) endpoint, and hence its degree-\(\ge4\) vertices are independent. | proved | `A001/L002` | `D001`–`D004` |
| L003 | finite exclusion | Every counterexample has at least nine vertices. | proved | `A001/L003` | `D001`–`D004` |
| L004 | path lemma | At an endpoint \(v_0\) of a longest path \(v_0\ldots v_\ell\) in a counterexample, every neighbor is on the path and no chord \(v_0v_i\) has \(i=2^k-1\) for \(k\ge2\). | proved | `A001/L004` | `D001`–`D004` |
| C002 | computational observation | The exact labelled census found no counterexample among all graphs of minimum degree at least \(3\) through order \(7\) or among all cubic graphs through order \(8\). | tested | `E001` | `D001`–`D004` |
| L005 | finite exclusion | Every counterexample has at least ten vertices. | proved | `A002/L005` | `L002`, `L003` |
| L006 | finite exclusion | Every counterexample has at least eleven vertices. | proved | `A003/L006` | `L002`, `L005` |
| C003 | computational observation | Exhaustive degree-sequence backtracking found no edge-minimal counterexample of order \(9\) or \(10\). | tested | `E002` | `L002`, `L003` |
| C004 | minimality theorem | In a counterexample minimizing order and then size, every proper subgraph with defined minimum degree has a vertex of degree at most \(2\). Equivalent finite form (`A020` W2-T2): \(G-v\) is 2-degenerate for every vertex \(v\) and \(G-e\) for every edge \(e\). | imported | Carr (2026), Lemma 0.1; `references/source-audit-2026-07-23.md`; **verified line-by-line against arXiv:2605.22844v1 and internally reconstructed** in `references/carr-2026-verification-2026-07-24.md` (S019) — statement-correspondence exact | `C001` negation and stated minimality |
| C005 | domination theorem | Every vertex of an order-then-size minimal counterexample has a neighbor of degree exactly \(3\). | imported | Carr (2026), Corollary 0.1(1); `references/source-audit-2026-07-23.md`; **verified line-by-line and internally reconstructed** in `references/carr-2026-verification-2026-07-24.md` (S019) — statement-correspondence exact | `C004` |
| C006 | density theorem | At least \(4/7\) of the vertices of an order-then-size minimal counterexample have degree exactly \(3\). Not tight: its equality case requires every cubic vertex to send all three edges to \(V_{\ge4}\), which `C005` forbids (the proof uses Corollary 0.1(2) only). **Superseded internally by the strictly stronger `L038`** (\(3\lvert V_3\rvert\ge2n+3\)). | imported | Carr (2026), Theorem 0.1; `references/source-audit-2026-07-23.md`; **verified line-by-line and internally reconstructed** in `references/carr-2026-verification-2026-07-24.md` (S019) — statement-correspondence exact | `L002` |
| C007 | induced-path theorem | Every \(P_{13}\)-free graph of minimum degree at least \(3\) has a \(4\)- or \(8\)-cycle; consequently every counterexample contains an induced \(P_{13}\). | imported | Hegde–Sandeep–Shashank (2025), Theorem 0.2; `references/source-audit-2026-07-23.md` | `D001`–`D004` |
| C008 | high-average-degree theorem | There exists an absolute constant \(d\) such that every graph of average degree at least \(d\) contains a power-of-two cycle. | imported | Liu–Montgomery (2023); `references/source-audit-2026-07-23.md` | Their finite simple graph hypotheses |
| C009 | large-girth existence theorem | For every integer \(g\ge3\), there is a finite Hamiltonian cubic graph of girth at least \(g\). | imported | Biggs (1998), Theorem 3.2, taking the prescribed 2-factor to be one cycle; `references/source-audit-2026-07-23.md` | Finite simple graph conventions |
| L007 | separation lemma | The minimal-counterexample conclusions `L002` and `C004`–`C006`, together with the induced-\(P_{13}\) conclusion drawn from `C007` and the absence of \(C_4,C_8\), do not force a \(C_{16}\). | proved | `A004/L007` | `C009` |
| C010 | computational observation | No cubic graph on \(18\) vertices contains an induced \(P_{13}\) while avoiding both \(C_4\) and \(C_8\). | tested | `E003` | Exact reduction and family definition in `E003/README.md` |
| L008 | saturation reduction | If a counterexample exists, one exists which is edge-maximal on its vertex set while remaining power-cycle-free; every nonedge is then joined by a simple path of length \(2^k-1\), and the graph is connected, non-bipartite, and below the average-degree threshold in `C008`. | proved | `A005/L008` | `D001`–`D004`, `C008` |
| L009 | separation lemma | A finite connected bipartite cubic graph of girth at least \(17\) satisfies the prior connected-cubic and induced-\(P_{13}\) bundle while avoiding \(C_4,C_8,C_{16}\), but it fails the Mersenne-path saturation conclusion of `L008`. | proved | `A005/L009` | `C009`, `A004/L007` |
| L010 | structural lemma | In a saturated counterexample supplied by `L008`, the edges which belong to no odd cycle form a matching. | proved | `A005/L010` | `L008` |
| L011 | reduction | If a counterexample exists, there is a non-bipartite 2-connected power-cycle-free block with at most one degree-\(2\) vertex such that every nonedge whose endpoints are not exceptional has an internal simple path of length \(2^k-1\). | proved | `A005/L011` | `L001`, `L008`, `L010` |
| L012 | ear lemma | In the block from `L011`, a shortest odd cycle of length at least \(7\) has an external ear contained in the union with a saturation witness between two nonexceptional cycle vertices. | proved | `A005/L012` | `L011` |
| L013 | ear lemma | Every shortest odd cycle in the block from `L011`, including a triangle or \(5\)-cycle, has an external ear. | proved | `A005/L013` | `L011` |
| L014 | separation lemma | A shortest odd cycle plus one arbitrary external ear does not force a power-of-two cycle: theta path lengths \(2,2r+1,4\) for \(r\ge2\) give only cycle lengths \(2r+3,2r+5,6\). | proved | `A005/L014` | Finite simple graph conventions |
| L015 | separation lemma | A full one-excursion `L012` witness — total length \(2^k-1\), both cycle arcs, both gaps, induced shortest odd cycle, non-Mersenne arc choice — admits infinitely many power-free realizations in every attachment configuration (aligned or crossed, each arc trivial or not, \(d\in\{2,4\}\)), so one-excursion witness data forces no power-of-two cycle. | proved | `A005/L015` | `A005/L012` for the setting; finite simple graph conventions |
| L016 | separation lemma | Full two-excursion witness data also forces no power-of-two cycle: for every \(k\ge4\), the double-theta graph on \(C_7\) with disjoint ears of length \(2^{k-1}-1\) attached at \(\{c_0,c_4\}\) and \(\{c_5,c_2\}\) realizes a witness of length \(2^k-1\) for the distance-\(2\) pair \(c_0,c_2\), and its seven cycle lengths \(7,2^{k-1}+3,2^{k-1}+3,2^{k-1}+2,2^{k-1}+2,2^k+1,2^k+2\) contain no power of two. | proved | `A005/L016` | Finite simple graph conventions |
| C011 | computational observation | The exhaustive labelled search found no graph of order \(11\), \(12\), or \(13\) with minimum degree at least \(3\) avoiding both \(C_4\) and \(C_8\); five validation anchors reproduced the independent `E001`/`E002` counts, an exact symmetry quotient, and a nonzero \(C_8\)-free positive control. | tested | `E004` | Coverage layers proved in `A006/L017` |
| L017 | finite exclusion | Every counterexample has at least fourteen vertices. | proved | `A006/L017`, computer-assisted via the exhaustive search `E004` | `L006`, `E004` |
| C012 | reported computation | Royle's 2002 search — all graphs on at most \(15\) vertices with minimum degree \(3\), the degree-\(\ge4\) vertices independent, and no \(C_4\) — found every such graph to contain a \(C_8\); via the minimal-counterexample degree structure this gives: every counterexample has at least \(16\) vertices. The circulating "at least 17" has no primary-source support. | imported | Royle (2002 archived page) and Markström (2004), quoted in `references/source-audit-2026-07-23-S007.md`; external computation, not reproduced | The degree-structure reduction (internally `L002`'s independence conclusion plus order-minimality); `D001`–`D004` |
| C013 | reported computation | Markström's search of all cubic graphs on at most \(28\) vertices found each to contain a \(C_4\), \(C_8\), or \(C_{16}\); since cubic graphs have even order, every cubic counterexample has at least \(30\) vertices. | imported | Markström (2004), quoted in `references/source-audit-2026-07-23-S007.md`; external computation, not reproduced beyond order \(24\) | `D001`–`D004`; parity of cubic order |
| C017 | spectrum-confinement theorem | For every \(q\ge3\) there exist arbitrarily large planar cubic graphs with no \(q\)-power cycles; for \(q=2\) there exist arbitrarily large planar cubic graphs whose 2-power cycles all have length \(4\) only, or all length \(8\) only. All these constructions are 1-connected: cycles are confined to bounded gadgets around an internally cubic tree, so the cycle spectrum is a fixed finite set and the powers of \(q\ge3\) can be dodged; the paper states the tools do not apply at \(q=2\). | imported | Bensmail (2017), read in full; `references/source-audit-2026-07-23-S007.md` | Finite simple graph conventions |
| C014 | reported census | The smallest cubic graphs avoiding both \(C_4\) and \(C_8\) have \(24\) vertices; the counts of connected cubic \(\{C_4,C_8\}\)-free graphs are \(4\), \(23\), \(251\) at orders \(24\), \(26\), \(28\), each such graph found containing a \(C_{16}\); exactly one of the four at order \(24\) is planar. | imported | Markström (2004), Table 3 and Section 4; `references/source-audit-2026-07-23-S007.md`; the order-\(\le24\) part is reproduced internally as `C018` | `D001`–`D004` |
| C015 | computational observation | The Markström graph (House of Graphs 51419) is cubic, planar, connected, non-bipartite, of order \(24\), with cycle spectrum exactly \(\{3,5,6,7\}\cup\{9,\dots,24\}\); every one of its \(240\) nonedges carries a simple path of length \(15\) (and \(225\) of length \(7\), \(105\) of length \(3\)), so adding any single edge creates a \(C_4\), \(C_8\), or \(C_{16}\). | tested | `E005` Part 1 (`verify_markstrom.py`), all checks passed | Graph data from House of Graphs entry 51419; detectors from `E004` |
| C016 | computational observation | The exhaustive anchored census found no graph of order \(14\)–\(17\) with minimum degree at least \(3\) avoiding both \(C_4\) and \(C_8\): the connected \(C_4\)-free minimum-degree-\(3\) classes have sizes \(6059\), \(91433\), \(1655659\), \(34758006\) at orders \(14\)–\(17\), and every member contains a \(C_8\). | tested | `E006` (geng-generated, anchors A1–A5 passed, independent per-graph re-verification) | geng (nauty 2.9.3) correctness for the delegated class, anchored as recorded in `E006` |
| L018 | finite exclusion | Every counterexample has at least eighteen vertices; consequently the smallest \(\{C_4,C_8\}\)-free graph of minimum degree at least \(3\) has between \(18\) and \(24\) vertices. | proved | `A007/L018`, computer-assisted via `E006`/`C016` (generation layer delegated to anchored geng, unlike the fully internal `E004`) | `L017`, `C016`, connectivity and collapse lemmas in `A007` |
| C018 | computational reproduction | The connected cubic \(\{C_4,C_8\}\)-free census through order \(24\): empty at orders \(14\)–\(22\) (the \(C_4\)-free cubic classes of sizes \(36\), \(269\), \(2761\), \(36101\), \(553227\) all contain \(C_8\)), and at order \(24\) exactly four graphs among \(9{,}467{,}449\) \(C_4\)-free cubic graphs — matching Markström's Table 3 — of which exactly one is planar and labelg-isomorphic to House of Graphs 51419; all four contain \(C_{16}\), and each of the four is fully Mersenne-witness-covered: every one of its \(240\) nonedges carries a simple path of length \(3\), \(7\), or \(15\) (all \(240\) carry length \(15\)). | tested | `E005` Part 2 (`generate24.py`), planarity by nauty `planarg`, isomorphism by nauty `labelg` | geng anchored as in `E005`/`E006`; detectors from `E004`; upgrades the order-\(\le24\) part of `C014` from reported to reproduced |
| L019 | projection lemma | For every \(\mathbb{Z}_m\)-voltage assignment on a base multigraph satisfying the simplicity conditions, every cycle of length \(L\ge3\) in the derived graph projects to a tailless non-backtracking closed walk of length \(L\) in the base with net voltage \(\equiv0\pmod m\); hence if no such walk exists at any power length \(\le\) the lift's order (with lengths \(1,2\) encoding simplicity), the lift is a simple minimum-degree-\(\ge3\) graph with no power-of-two cycle. | proved | `A008/L019` | `D001`–`D004`; finite group conventions |
| L020 | gauge lemma | Voltage assignments differing by a coboundary yield isomorphic derived graphs; on a connected base every assignment is equivalent to one vanishing on a fixed spanning tree, so the assignment space is \((\mathbb{Z}_m)^\mu\) (\(\mu\) = cycle rank) and the net voltage of a closed walk is the pairing \(\nu\cdot x\) of its cycle-space vector with the free voltages. Unit scaling of assignments also preserves the isomorphism type. | proved | `A008/L020` | Finite group conventions |
| L021 | commutator obstruction | If \(W_1,W_2\) are non-backtracking closed walks at a common vertex of a multigraph satisfying the four arc conditions of `A008`, then \(W_1W_2\overline{W_1}\overline{W_2}\) is a tailless non-backtracking closed walk of length \(2(\lvert W_1\rvert+\lvert W_2\rvert)\) whose net voltage is zero under every abelian voltage assignment. Realizations: length \(8\) on the dumbbell, \(16\) on theta3 and \(K_4\), hence abelian lifts of these bases of sufficient order can never satisfy the L019 certificate at those power lengths. | proved | `A008/L021`; DP-independent witnesses in `E007/data/zero-vector-witnesses.txt` (also length 16 on the prism, 32 on \(K_{3,3}\), 4 on the bouquet, computed) | `L019` for the certificate reading |
| C019 | computational observation | The four verified order-24 extremal graphs contain exactly \(315\), \(330\), \(207\), \(228\) sixteen-cycles respectively; every edge lies on at least \(70\) of them and no edge lies on all of them (a fortiori no vertex), so the \(C_{16}\) obstruction at the extremal boundary is massively redundant and cannot be removed by local surgery. | tested | `E007` `calibrate`, `data/calibration-order24-c16.txt` | `C018` graph data; `E004` detectors |
| C020 | computational observation | Certificate verdict: for each of the six bases — the complete list of cycle-rank-2 minimum-degree-\(3\) multigraph bases (bouquet, theta3, dumbbell) plus \(K_4\), \(K_{3,3}\), prism — and every modulus \(m\ge2\), no \(\mathbb{Z}_m\) voltage assignment admits the L019 walk certificate of power-of-two-freeness: the exhaustive hyperplane sieve (both parities of \(m\)) leaves zero survivors below each base's first integer-zero-vector power length, and the integer zero vector kills every larger \(m\) outright. | tested | `E007` sieve outputs in `data/` | `L019`–`L021`; walk-class DP anchored A1–A8 |
| C021 | computational observation | Truth census: for every base and every \(m\) with lift order in \([12,30]\), over all voltage assignments of both parities, every simple derived graph contains a \(C_4\), \(C_8\), or \(C_{16}\); since these are the only power lengths \(\le30\), no counterexample to `C001` exists anywhere in the \(\mathbb{Z}_m\)-lift families of the six bases through order \(30\). In particular every simple circulant \(C_m(a,b)\) with \(m\le30\) contains a \(C_4\), and the \(48\) \(K_4\)-lifts of order \(28\) avoiding \(C_4\) and \(C_8\) all contain \(C_{16}\). | tested | `E007` `truth`, `data/truth-census-orders-12-30.txt` | `E004` detectors (independent of `L019`); consistent with `L017`, `L018`, `C013` |
| C022 | computational observation | Non-abelian certificate verdict: over \(\mathbb{Z}_7\rtimes\mathbb{Z}_3\) (order 21, metabelian), the Heisenberg group mod 3 and \(\mathbb{Z}_9\rtimes\mathbb{Z}_3\) (both order 27), and the perfect group \(A_5\) (order 60), every voltage assignment on the cycle-rank-2 bases (theta3, dumbbell, bouquet2) — and on \(K_4\) for the three solvable groups — is L019-certificate-dead by length \(16\): some tailless non-backtracking closed walk of length \(4\), \(8\), or \(16\) has identity net voltage. Zero survivors among all assignments (e.g. all \(3600\) per base over \(A_5\), certificate lengths through \(64\)). The pre-registered solvable-cascade predictions of `A009` are confirmed at or before their bounds; the \(A_5\) deaths match the collision-wall prediction \(2^k\approx2\log_2\lvert\Gamma\rvert\), and the wall mechanism (not yet a proved lemma) is recorded in `A009`/`E008`. | tested | `E008` `probe`, `data/probes-mu2-bases.txt`, `data/probes-k4-solvable.txt`; anchors incl. bidirectional agreement with the `E007` engine on cyclic groups | Group tables axiom-checked at runtime; `L019`/`L020` in their group-agnostic form (`A009`); certificate-level only — no statement about actual cycle spectra of the lifts |
| C023 | computational observation | The exhaustive anchored census at order \(18\): the connected \(C_4\)-free minimum-degree-\(3\) class has \(834{,}711{,}846\) members and every one contains a \(C_8\); no \(\{C_4,C_8\}\)-free graph of minimum degree at least \(3\) exists on \(18\) vertices. | tested | `E006` order-18 extension (S009: 48 geng parts, 8 PyPy workers; anchors A1–A5 re-passed under PyPy 7.3.23; empty survivor files `data/survivors_n18.*`) | geng (nauty 2.9.3) correctness for the delegated class, anchored as recorded in `E006`; same caveats as `C016` |
| L022 | finite exclusion | Every counterexample has at least nineteen vertices; consequently the smallest \(\{C_4,C_8\}\)-free graph of minimum degree at least \(3\) has between \(19\) and \(24\) vertices. | proved | `A007` addendum (S009), computer-assisted via the order-18 census `C023` | `L018`, `C023`, connectivity and collapse lemmas in `A007` |
| L023 | structure lemma | Arc-digraph structure of a finite connected multigraph \(B\) with \(\delta\ge3\): (i) no nonempty continuation-closed arc set is reverse-free; (ii) the non-backtracking arc digraph is strongly connected; (iii) its period is \(1\) or \(2\), equal to \(2\) exactly when \(B\) is bipartite, with class = tail side; (iv) there is a finite \(R_B\) such that every ordered arc pair is joined by an nb walk of every sufficiently large admissible exact length. (`A010` W2–W5.) | proved | `A010` (hand proofs, incl. inline numerical-semigroup sub-lemma); verified on six bases in `E009`; audited in `R001` (pass) | Finite multigraph conventions; no voltage content |
| L024 | collision-wall theorem | For every finite connected multigraph base \(B\) with \(\delta(B)\ge3\) (\(m\) edges), every finite group \(\Gamma\), and every voltage assignment: \(B\) has a tailless non-backtracking closed walk with identity net voltage of every length \(L\equiv0\pmod4\) with \(L\ge4\ell^*+4R_B+8\) (every even such \(L\) if \(B\) is non-bipartite), where \(\ell^*=\lfloor\log_2(2m\lvert\Gamma\rvert)\rfloor+2\); in particular of length \(2^k\) for every \(2^k\ge4\log_2\lvert\Gamma\rvert+C_B\) with \(C_B=4\lceil\log_2m\rceil+4R_B+24\). Corollary (scoped per `R001` F3): for each base there is \(\Gamma_0(B)\) such that for all \(\lvert\Gamma\rvert\ge\Gamma_0(B)\) the walk certificate fails at some power \(2^k\le n\lvert\Gamma\rvert\) — per-base effective closure of the certificate program; the small-group/large-base regime is not covered by this corollary. | proved | `A010` W7–W8 (hand proof); construction verified exhaustively in scope by `E009` (9,606,333 assertions) and by the independent R001 probe (108 cases, 3,136 lengths); audited in `R001` (pass, minor findings repaired) | `L023`; the group-agnostic form of `L019` (`A009`) for the certificate reading only |
| C024 | interval theorem | Bondy–Vince: with the exception of \(K_1\) and \(K_2\), every simple graph having at most two vertices of degree less than three contains two cycles whose lengths differ by one or two; every non-bipartite 3-connected graph has two cycles whose lengths differ by one. Their Figure 1 remark: 2-connectedness does not restore this — an odd ring of \(K_{d,d}-e\) copies gives 2-connected minimum-degree-\(d\) graphs with gapped spectra (for \(d=3\): cycle lengths exactly \(\{4,6,9,11,13,15\}\) at three copies). | imported | Bondy–Vince (1998), Theorems 1–2 and p.12 remark, read in the published PDF; `references/source-audit-2026-07-24-S011.md` | `D001`–`D004` |
| C025 | mod-k / consecutive-lengths theorems | Gao–Huo–Liu–Ma: every graph of minimum degree \(\ge k+1\) has cycles of all even lengths mod \(k\) (all lengths mod \(k\) if also 2-connected non-bipartite); every \(k\)-connected graph (\(k\ge3\)) has a cycle \(\equiv0\bmod k\); every 3-connected non-bipartite graph of minimum degree \(\ge k+1\) has \(k\) cycles of consecutive lengths. At minimum degree 3 these give only Bondy–Vince-strength conclusions. | imported | Gao–Huo–Liu–Ma, IMRN 2022, arXiv:1904.08126, **abstract strength only**; `references/source-audit-2026-07-24-S011.md` | Their hypotheses as stated |
| C026 | diameter theorem | Every graph of diameter 2 and minimum degree at least 3 contains a cycle of length 4 or 8; hence every counterexample has diameter at least 3. | imported | Carr, arXiv:2508.19302 (2025), abstract strength, preprint not verified; `references/source-audit-2026-07-24-S011.md` | `D001`–`D004` |
| L025 | assembly reduction | The atom reduction (A011 R1–R5): (a) a **1-atom** (connected, power-free, exactly one vertex of degree \(<3\), of degree 1 or 2, others \(\ge3\)) yields a counterexample of order \(2n\) or \(3n\) by edge-doubling or triangle assembly; (b) a **2-atom** (connected two-terminal, non-terminal degrees \(\ge3\), terminal degrees \(\ge1\) summing \(\ge3\), power-free spectrum, all simple terminal-to-terminal path lengths in a window of ratio \(<2\)) yields counterexamples of unbounded order via rings of \(L\) copies placed inside a dyadic gap; the ring spectrum is exactly \(\mathrm{Spec}(H)\cup\{\sum_i s_i\}\). Contrapositively, if statement 0.1 is true, no power-free graph has exactly one sub-cubic vertex, and every power-free two-terminal graph with the degree condition has \(s_{\max}\ge2\,s_{\min}\) (**spread-doubling**). | proved | `A011` (hand proofs R1–R5); ring builder and spectrum verified against the published Bondy–Vince figure in `E010` anchor A4 | `D001`–`D004`; `C024` for context only (the proofs are self-contained) |
| C027 | computational observation | The atom search is empty through order 15: among all connected \(C_4\)-free graphs of minimum degree \(\ge1\) with at most two sub-cubic vertices and the implied edge bound (streams of 4; 5; 36; 84; 918; 4,058; 52,331; 389,734; 5,605,161; 61,813,970 at orders 6–15; class sizes up to 3,470,555), not one is \(C_8\)-free — so no 1-atom, no 2-atom, and no power-free member of the class at all exists on \(\le15\) vertices. Calibration: with power-freeness dropped, through-ratio \(<2\) (including perfectly rigid \(S=\{1\}\)) occurs 22 times at order 12 and 116 at order 13, and every occurrence contains a \(C_8\). | tested | `E010` (geng nauty 2.9.3 anchored A1–A5; orders 6–14 CPython 3.14.2, order 15 PyPy 7.3.23 with anchors re-passed) | Coverage argument in `A011`/`E010`: atoms with both terminals of degree \(\ge3\) are excluded through order 18 by `L022`/`C023` |
| L026 | equivalence lemma | Rung completeness (A012 T1): 2-atoms with \(S=\{1\}\) exist **iff** counterexamples to statement 0.1 exist, and 2-atoms with \(s_{\min}=2\) exist **iff** counterexamples exist — two disjoint copies of a counterexample can be hidden behind a bridge (terminals its endpoints) or behind the cut vertices \(a\) and \(w\) of a path \(a\,w\,b\) scaffold, in parts no terminal-to-terminal path visits. Hence each unrestricted bottom rung of spread-doubling at \(s_{\min}\in\{1,2\}\), and clause (b) of `L025` R5 by itself, is equivalent to statement 0.1. | proved | `A012` T1 (hand proofs); scaffold combinatorics verified in `E011` anchors A2–A3 | `D001`–`D004`; `L025` R3/R4 for the forward directions |
| L027 | structure lemma | Lobe decomposition (A012 T2): every (D)-gadget is vertex-taut (every vertex on some simple \(a\)–\(b\) path) or contains a **lobe** — a connected subgraph meeting the essential set in exactly one vertex \(c\), with every non-\(c\) vertex keeping all its edges inside the lobe and hence degree \(\ge3\) there, and \(\deg_{\text{lobe}}(c)\ge1\). Consequently every non-taut *power-free* (D)-gadget contains a 1-atom (\(\deg_{\text{lobe}}(c)\le2\)) or a power-free graph of minimum degree \(\ge3\) (\(\deg_{\text{lobe}}(c)\ge3\)). | proved | `A012` T2 (block–cut-tree proof); instance-verified on 265 lobe components in `E011` | `D001`–`D004`; D-A2/D-A4 of `A011`; the decomposition itself is unconditional — only the final clause consumes power-freeness |
| L028 | taut bottom rungs | (A012 T3) No taut (D)-gadget has \(S=\{1\}\); and no taut (D)-gadget with \(s_{\min}=2\), \(S\subseteq\{2,3\}\), and no 4-cycle exists — \(C_4\)-freeness alone suffices, via the unique common neighbor \(w\), the counting bounds (\(\le1\) \(w\)-edge into each terminal side, \(\le1\) cross-neighbor per vertex), and a forced 4-cycle \(a\,x\,z\,w\). The case \(S=\{3\}\) has \(s_{\min}=3\) and is not covered. Corollaries: every 2-atom with \(s_{\min}\le2\) contains a 1-atom or a power-free minimum-degree-3 graph, and has order \(\ge17\) (\(\ge18\) when \(s_{\min}=2\)). | proved | `A012` T3 (hand proofs); instance check: all 133 rung-class gadgets at orders 12–13 are non-taut (`E011`) | `L027`; `C027` and `L022` for the corollaries |
| L029 | reduction | **Cubic reduction modulo _tight_ 1-atoms (A012 T4, restated in A018 T2 — see the correction note below).** Call a 1-atom **tight** if its exceptional vertex has degree exactly 2. If no tight 1-atom exists, every counterexample of minimum order then minimum size is cubic; hence statement 0.1 is true **iff** no cubic counterexample and no tight 1-atom exist. `A012` T4's proof gives exactly this: its only 1-atom is produced by deleting an edge \(xy\) from a minimum counterexample with \(\deg(x)\ge4\), \(\deg(y)=3\), so the exceptional vertex has degree 2, and its second branch uses no 1-atom at all. **As originally stated (with D-A4's degree-1-or-2 1-atoms) the implication is vacuous**, because by `L036` the hypothesis "no 1-atom exists" is equivalent to statement 0.1 itself. The tight version is not vacuous (`L036` T3). The former remark "in a minimum-order 1-atom the sub-cubic vertex has degree 2" (`A012` T4.1) is **withdrawn as unproved**: its argument does not close when the pendant-reduct is a cubic counterexample. | proved | `A012` T4 (hand proof); restatement and vacuity analysis `A018` T2/T3 | `D001`–`D004`; `L036` for the vacuity of the unrestricted form |
| C028 | computational observation | E011 instance verification and survey at orders 12–13 of the connected \(C_4\)-free exactly-two-sub-cubic stream (1,690 and 16,106 gadgets; stream and profile counts reproduce `E010` exactly from an independent implementation): all \(19+114\) gadgets with \(S=\{1\}\) or \(2\in S\subseteq\{2,3\}\) are non-taut (`L028` instance check), and all \(38+227\) inessential components have exactly one essential attachment with internal degrees \(\ge3\) (`L027` instance check). Every taut pinched gadget at these orders — five in total — has \(S=\{6,\dots,11\}\) (ratio \(11/6\)) and contains a \(C_8\); no taut pinched gadget with \(s_{\min}\in\{3,4,5\}\) exists through order 13. | tested | `E011` (17 anchors incl. \(K_{3,3}-e\) tautness and both T1 scaffolds; CPython 3.14.2, geng nauty 2.9.3) | geng anchored as in `E010`; `L027`/`L028` supply the predictions tested |
| L030 | taut rung theorem | The taut \(s_{\min}=3\) rung (A013 T8): no vertex-taut \(C_4\)-free (D)-gadget has \(S\subseteq\{3,4,5\}\) — equivalently, every vertex-taut \(C_4\)-free two-terminal graph with \(d(a,b)\ge3\), non-terminal degrees \(\ge3\), and terminal degrees \(\ge1\) has a simple \(a\)–\(b\) path of length \(\ge6\). Power-freeness beyond \(C_4\) and the (D) clause \(\deg(a)+\deg(b)\ge3\) are not used. Proof shape: tautness confines every middle vertex to distance one from \(N(a)\) or \(N(b)\); forbidden length-6/7 paths eliminate the middle classes, then force the middle into matched degree-3 triples, then empty it; the surviving graph is three perfect matchings on \(N(a)\cup N(b)\), where each vertex triggers a dichotomy — a \(C_4\) block or a length-7 path. Corollaries: no taut 2-atom has \(s_{\max}\le5\); with `L028`, every taut 2-atom has \(s_{\min}\ge4\) and \(s_{\max}\ge6\); every 2-atom with \(s_{\min}=3\) is non-taut, contains a 1-atom or a min-degree-3 power-free graph (`L027`), and has order \(\ge19\). | proved | `A013` T1–T8, C1–C3 (hand proofs); falsification search and endgame instance check `E012`/`C029` | `D001`–`D004`; D-A1–D-A3 (`A011`), D-B1 (`A012`); corollaries also `L027`, `L028`, `C027`, `L022` |
| C029 | computational observation | E012 all-terminal-pair falsification search at orders 6–14: over every admissible terminal pair of every connected \(C_4\)-free min-degree-\(\ge1\) stream graph with \(\ge\lceil(3n-4)/2\rceil\) edges (stream totals reproduce `C027`; at order 13, 20,432 eligible graphs vs `E011`'s 16,106 — the degree-\(\ge3\)-terminal slice is covered for the first time), **no taut (D)-pair with \(S\subseteq\{3,4,5\}\) exists; none even non-taut** — every pair passing the distance and eccentricity prefilters is rejected by a length-\(\ge6\) path. The T8 endgame dichotomy holds on all 218 three-matchings structures at \(k\in\{2,4\}\): \(C_4\) present iff some \(x\) has \(\sigma\varphi(x)=\psi\sigma(x)\), and every \(C_4\)-free structure has an \(a\)–\(b\) path of length \(\ge7\). \(K_{3,3}-e\) is the positive control (caught as taut, \(S=\{3,5\}\)). | tested | `E012` (736 anchor checks; CPython 3.14.2, geng nauty 2.9.3) | geng anchored as in `E010`/`E011`; primitives from `E011`; `L030` supplies the prediction tested |
| L031 | reduction lemma | Pendant reduction and lift (A014 T1/T2): in a vertex-taut (D)-gadget \((H,a,b)\) with \(\deg(a)=1\), the neighbor \(x\) of \(a\) is internal (\(x\ne b\)), stripping the pendant edge is a bijection of through-paths, so \(S(H)=1+S(H-a,x,b)\) and \(\mathrm{Spec}(H)=\mathrm{Spec}(H-a)\); the reduct \((H-a,x,b)\) is a vertex-taut **core** (non-terminal degrees \(\ge3\), both terminal degrees \(\ge2\)); and \(H\) is strictly pinched iff the core has excess \(e=s_{\max}-2s_{\min}\le0\). Conversely the pendant lift of any vertex-taut core at either terminal is a vertex-taut (D)-gadget with a degree-1 terminal, shifted \(S\), unchanged spectrum, strictly pinched iff \(e\le0\); the two lifts are isomorphic as gadgets iff the core has a terminal-swapping automorphism. | proved | `A014` T1–T2 (hand proofs); lift/witness bijection instance-verified on the complete order-\(\le14\) catalogue (`E013` cores) | `D001`–`D004`; D-A1/D-A2 (`A011`) |
| L032 | interface theorem | Chain decomposition and block extraction (A014 T3/T4): in a vertex-taut two-terminal graph with non-terminal degrees \(\ge3\), every cut vertex separates the terminals, the block–cut tree is a path, \(S\) is the Minkowski sum of the block through-sets, the spectrum is the union, and each block is vertex-taut; in a core, bridges are neither adjacent nor terminal blocks. Consequently **taut 2-atoms exist iff a power-free vertex-taut 2-connected core with \(s_{\max}\le2\,s_{\min}\) exists** (forward: pendant-reduce, then bridge-counting forces some 2-connected block to excess \(\le0\); converse: the core itself if strict, else its pendant lift). Sharpness (T5): two Petersen\(-e\) blocks joined by a bridge form a **strict** vertex-taut core of order 20 (\(S=\{9,\dots,17\}\), spectrum \(\{5,6,8,9\}\)) — so strict cores exist, core-level spread-doubling is false, and 2-connectedness in the extraction cannot be dropped. | proved | `A014` T3–T5 (hand proofs; T5 machine-verified as `E013` anchor A9) | `L031`; D-A3 (`A011`) for the 2-atom reading |
| C030 | computational observation | The strict taut-pinched catalogue (E013 `run`): over every admissible terminal pair of every stream graph at orders 6–14 (1,470,530 pairs), exactly **six** vertex-taut strictly-pinched (D)-pairs exist — one new at order 11 (terminal degrees (1,2), \(S=\{5,6,8,9\}\), spectrum \(\{5,6,8,9\}\)) and the five `C028` witnesses at orders 12–13; **none at order 14**. In particular the strict \(s_{\min}=4\) rung target \(S\subseteq\{4,\dots,7\}\) is empty through order 14 — the first search covering it (`C029`'s scan aborted on any length-6 path and could not see \(s_{\max}\in\{6,\dots,9\}\)). Every hit contains a \(C_8\); zero non-taut pairs completed the scan at any order. | tested | `E013` `run` (stream totals asserted against `C027`; `C028` hits re-found and asserted; 88 anchors; CPython 3.14.2, geng nauty 2.9.3) | geng anchored as in `E010`–`E012`; `L028`/`L030` instance-checked |
| C031 | computational observation | The closed-ratio core catalogue and block dissection (E013 `runclosed`/`cores`/`dissect`): relaxing to \(s_{\max}\le2\,d(a,b)\) yields 54 taut hits at orders 6–14, every one at exact excess \(0\) except the six strict lifts of `C030`; all are pendant-type except exactly **five (2,2)-terminal 2-connected cores (blocks)**: Petersen\(-e\) (order 10, \(S=\{4,5,7,8\}\), 7 \(C_8\)s), A11 and B11 (order 11, \(S=\{5..10\}\)), C12 (order 12, \(S=\{5..10\}\)), D14 (order 14, \(S=\{6..12\}\)); **no strict block through order 14**. The six strict witnesses are exactly the distinct pendant lifts of the first four blocks (bijection verified; core dedup cross-checked against nauty labelg; Petersen\(-e\) identified canonically, its \(S\) = Petersen's cycle-through-edge lengths \(-1\), its 7 \(C_8\)s = Petersen's 15 minus the 8 through the deleted edge). In every block and witness, every \(C_8\) is the symmetric difference of two through-paths (23/23); every block has an internally disjoint pair of shortest through-paths (at band 4 forcing \(C_8\) directly); no inessential edges exist in the catalogue; D14 is swap-symmetric, predicting exactly one strict band-7 witness at order 15. | tested | `E013` `runclosed`/`cores`/`dissect` (strict/closed subset consistency asserted per order; labelg cross-anchor) | same tool caveats as `C030`; `L031`/`L032` supply the predictions tested |
| L033 | structure lemma | The band-4 pencil dichotomy (A015 T0–T3): in any finite simple graph with \(d(x,y)=4\): (T0) the vertices at distance 2 from both \(x\) and \(y\) are exactly the middles of 4-paths, and the 4-paths through a middle \(m\) are the product \(A(m)\times\{m\}\times B(m)\) of its \(N(x)\)- and \(N(y)\)-neighborhoods; (T1) either two internally disjoint 4-paths exist or all 4-paths share a common internal vertex — no \(C_4\)-freeness needed; (T1′) an 8-cycle through both \(x\) and \(y\) exists iff two internally disjoint 4-paths do. Under \(C_4\)-freeness: (T2) two distinct 4-paths share at most one internal vertex; with \(\ge2\) paths and no disjoint pair the pencil vertex is unique and adjacent to a terminal (a middle pencil forces a unique 4-path), and the fan is rigid — middles biject with strands, exits are unique per middle, no cross chords, every strand pair spans a hexagon; (T3) in a pencilled system every \(N(x)\)-vertex except the pencil-side one is at distance \(\ge4\) from \(y\), and the \((3,1)\)-vertices are exactly the exits. Consequence: the band-4 case of the block question closes iff the **pencil endgame** holds — no vertex-taut 2-connected \(C_4\)-free core with \(d(x,y)=4\), \(s_{\max}\le8\) has a pencilled 4-path system — and then in the strengthened form "\(C_8\) through both terminals". | proved | `A015` T0–T3 (hand proofs); exhaustive instance verification `E014`/`C033` | `D001`–`D004` only; the endgame reduction also uses D-P1 (`A014`) for the core reading |
| C032 | computational observation | E013 order-15 extension (PyPy 7.3.23; stream total 61,813,970 asserted against `C027`; 3,384,284 eligible graphs, 22,022,137 admissible pairs per mode): the strict scan finds exactly **one** taut pinched pair at order 15 — machine-identified as D14's pendant lift (band 7, \(S=\{7,\dots,13\}\), terminal-respecting isomorphism verified) — confirming `C031`'s swap-symmetry prediction; the closed scan finds 20 hits, **all pendant-type** (a degree-1 terminal), so the 2-connected block catalogue is unchanged through order 15: no strict block, and \{P10, A11, B11, C12, D14\} remain the only blocks, all at equality. Closed hits at orders \(\le15\) occupy bands 4–7 only — **no closed hit at any band \(\le3\) exists**, so the equality-block ladder starts at band 4 empirically. The closed band-4 world through 15 is exactly three objects: pendant-type pairs at orders 9 and 15 (pencil-type at their attachment, reductions have excess \(+1\)) and Petersen\(-e\) — the only band-4 core, disjoint-type with 7 \(C_8\)s. The seven strict witnesses reduce to five distinct cores (dedup cross-checked; Petersen\(-e\) re-identified); the interference census extends: every \(C_8\) in every witness and core is a two-through-path symmetric difference. | tested | `E013` `run 15`/`runclosed 15`/`cores`/`dissect` (strict/closed subset consistency asserted at 15; `C028` cross-checks re-passed; anchors re-passed under PyPy) | geng anchored as in `E010`–`E013`; `L031`/`L032` supply the lift/reduction predictions tested |
| C033 | computational observation | E014: (a) exhaustive instance verification of `L033` — all connected \(C_4\)-free graphs of orders 5–11 (22,901 graphs, 116,187 distance-4 pairs: zero failures of T0/T1/T1′/T2/T3-consequences; zero middle pencils; fan sizes 2/3/4 realized 22,332/885/10 times; disjoint pair ⟺ \(C_8\)-through-terminals every time) and all connected graphs of orders 5–9 (273,183 graphs, 43,419 pairs: T0/T1/T1′ hold; 946 middle pencils occur, all with \(C_4\)s present); two fixed anchors show T2's refinements fail without \(C_4\)-freeness. (b) Hypothesis necessity for the band-4 rungs, \(C_4\) allowed (all 2-connected \(\le2\)-sub-cubic graphs, orders 6–9, every admissible terminal pair, closed-band scan): strict blocks exist from order 6 (\(K_{3,3}-e\), band 3, \(S=\{3,5\}\) — the `L030` sharpness example recast); strict band-4 blocks with the exact rung shape \(S=\{4,5,6,7\}\) exist at order 8; band-4 equality cores with \(S=\{4,\dots,8\}\) and **no \(C_8\)** exist at order 9 (e.g. spectrum \(\{4,5,7,9\}\)); every closed taut core found at orders \(\le9\) contains a \(C_4\), consistent with `C031`. So \(C_4\)-freeness is essential to both band-4 rungs and to T2. | tested | `E014` (`probe.py a/ag/b`; primitives imported from the anchored `E013` module; PyPy 7.3.23) | geng anchored as in `E010`–`E013`; `L033` supplies the predictions tested |
| L034 | assembly criterion | Generalized ring criterion (A016 M1–M3, direct corollary of `L025`/A011 R1): if \((H,a,b)\) is a power-free (D)-gadget and for some \(L\ge3\) the \(L\)-fold sumset \(\{s_1+\dots+s_L : s_i\in S\}\) contains no power of two, then the ring \(R_L(H,a,b)\) is a counterexample to statement 0.1. Sufficient sumset-avoidance instances: (i) \(S\) all odd with \(L\) odd (all sums odd; realized e.g. by any bipartite (D)-gadget with terminals in opposite parts); (ii) \(\gcd(S)\) divisible by an odd prime (every \(L\)); (iii) \(S\subseteq2+4\mathbb{Z}\) with \(L\) odd (sums \(\equiv2\bmod4\)). The pinch condition of D-A3 (\(s_{\max}<2s_{\min}\), via R2's dyadic window) is exactly one further instance, so 2-atoms generate one of several ring channels. Contrapositive necessary conditions: if 0.1 is true, then every power-free (D)-gadget has, for every \(L\ge3\), a power of two in its \(L\)-fold sumset — in particular some through-path of even length, \(\gcd(S)\) a power of 2, and \(S\not\subseteq2+4\mathbb{Z}\). Example target shapes the pinched frame cannot see: \(S=\{3,7\}\) (\(\Sigma_3=\{9,13,17,21\}\); all-odd band-3 taut gadgets need \(7\in S\) by `L030`'s even lower bound 6) and \(S=\{2,6\}\) (\(\Sigma_3=\{6,10,14,18\}\)). All-odd \(S\) forces \(d(a,b)\) odd, so the taut parity ladder lives at odd bands only. | proved | `A016` (audit; sumset arithmetic verified by hand), citing `A011` R1 at its proved strength; provenance: external memo `references/external-agent-strategy-memo-2026-07-24.md`, independently re-derived | `L025` (R1 only; R2 not used) |
| L035 | structure theorem | The parity structure theorem and the bipartite assembly criterion (A017 T0–T5). (a) *Block chain, degree-free* (T0): in any vertex-taut two-terminal graph every cut vertex separates the terminals, the block–cut tree is a path, \(S\) is the Minkowski sum of the block through-sets, and each block is vertex-taut — `A014` T3.1/T3.2/T3.4 with the degree hypotheses deleted. (b) *Parity fan* (T1): in a 2-connected graph containing an odd cycle, any two distinct vertices are joined by simple paths of **both** parities (proof: attach a degree-2 apex to the two vertices, take a 2-fan from it to the odd cycle, and use the two arcs). (c) *Parity structure theorem* (T2): for a vertex-taut two-terminal graph, \(S\) is parity-constant **iff** \(H\) is bipartite; all-odd corresponds to terminals in opposite colour classes, all-even to the same class. Tautness is necessary (pendant triangle: \(S=\{2\}\), non-bipartite). (d) *Bipartite assembly criterion* (T3): **any** connected bipartite power-free graph with at most two sub-cubic vertices — degrees \(\ge1\) summing to \(\ge3\) if two, degree 1 or 2 if one — disproves statement 0.1, via `L025` R4 (one sub-cubic vertex), directly (none), or a 3-ring of it or of its pendant lift/reduction (two). No path enumeration, no tautness test, no external import. (e) *Channel completeness* (T4): every power-free (D)-gadget with parity-constant \(S\) — i.e. every witness of `L034` channels (i) and (iii) — is either vertex-taut and hence bipartite, or non-taut and then already disproves 0.1 through `L027`. So bipartite generation is an exhaustive instrument for those channels, modulo the standing 1-atom relativization. Channel (ii) (odd-prime \(\gcd\)) is **not** covered: \(\Theta(3,3,3)\) is vertex-taut, non-bipartite, \(S=\{3\}\). (f) *Part-size bound* (T5): a \(C_4\)-free bipartite graph with parts of sizes \(p,q\) and at most two vertices of degree \(<3\) (each \(\ge2\)) satisfies \(3q-4\le\binom p2\) and \(3p-4\le\binom q2\). | proved | `A017` T0–T5 (hand proofs); exhaustive instance verification `E015`/`verify_parity.py` — every connected graph of order \(\le7\) (994 graphs, 39,690 ordered pairs) and every connected \(C_4\)-free graph of orders 8–9 (926 graphs, 63,696 pairs), through-sets by explicit path enumeration: T2's kill condition (taut + parity-constant + non-bipartite) never fired in 952 hypothesis instances, T1 held in 27,196, T0's chain/Minkowski in 33,962, and all 10,788 parity-constant non-bipartite pairs were non-taut (sharpness); plus `E015` anchors (20,082 assertions under both interpreters) and the generator cross-check | `D001`–`D004`; D-A1/D-A2/D-A4 and `L025` R1/R4 (`A011`); D-B1 and `L027` (`A012`) for T4; `L034` for the channel reading. Corrects `A016` M3 (the bipartite reformulation *is* an equivalence on the taut side) and M4 (the parity channel does have a reduction theory: pendant shift toggles the two bipartite sub-cases) |
| C034 | computational observation | E015 bipartite hunt (CPython 3.14.2 and PyPy 7.3.23; nauty 2.9.3 `genbg -q -c -Z1 -d2 p q mine:0`, \(\text{mine}=\lceil(3n-2)/2\rceil\), splits pruned by `L035` T5): **no connected bipartite \(C_4\)-free graph with at most two sub-cubic vertices (each of degree \(\ge2\)) is power-free at any order \(n\le23\)** — hence, with the pendant reduction, no witness of `L035` T3 of order \(\le24\) with a degree-1 vertex either. Class sizes / minimum \(C_8\) count by order: 12–13: 0; 14: 2 (min 13 \(C_8\)s); 15: 1 (21); 16: 6 (20); 17: 8 (27); 18: 75 (23); 19: 197 (26); 20: 2,715 (18); 21: 10,865 (24); 22: 178,549 (14, from 55,451,237 generated in 2,798s; splits (9,13):1, (10,12):15,849, (11,11):162,699); 23: 928,562 (19, from 260,796,118 generated in 5,239.8s under PyPy 7.3.23; splits (10,13): 6,012, (11,12): 922,550, eight smaller splits proved empty by the T5 bound). The order-22 leg was launched in S016 and completed during S018; sub-cubic profiles at 22 are 155,205 two-terminal gadgets, 21,579 tight-1-atom candidates and 1,765 minimum-degree-\(\ge3\) graphs, all with a \(C_8\). The order-23 leg was launched as an S019 background job (anchors re-passed under PyPy first) and harvested as the S019 follow-up; its profiles are 814,453 / 107,787 / 6,322, all with a \(C_8\), and every order-23 member carries **both** a \(C_8\) and a \(C_{16}\). **Every member of the class at every scanned order contains a \(C_8\)** — the \(C_{16}\) test is never decisive, and the minimum \(C_8\) count never drops below 13, so the class is far from a witness (contrast the pinched channel's equality blocks, blocked by 3–7 \(C_8\)s). Sub-results at the same orders: no bipartite **tight** 1-atom (the one-sub-cubic-vertex column, degree exactly 2 since the stream has minimum degree 2: 332 candidates at order 20, 1,208 at 21, 21,579 at 22, 107,787 at 23, all with a \(C_8\)) — the correct object after `L036`; no bipartite counterexample to 0.1 (the min-degree-\(\ge3\) column: 50 at order 20, 69 at 21, 1,765 at 22, 6,322 at 23), which verifies internally the sub-range of the unverified `G014` item-2 import. The order-14 class is exactly the Heawood graph (cubic, 21 \(C_8\)s, identified by `labelg`) and one 20-edge graph with terminals in opposite classes (all-odd \(S\), 13 \(C_8\)s). **S021 follow-up harvest — order 24, by the new instrument:** `genc48 -c -f -b -d2 24 35:276` (`E019`/`C039`'s generator, `-b` path anchored at seven (variant, order) pairs; 16 parts, all zero, 925 s wall on 8 workers) finds **no bipartite \(\{C_4,C_8\}\)-free graph of minimum degree \(\ge2\) at order 24 at all** — the \(\le2\)-sub-cubic filter never engages — so the class is empty through order **24** (25 with a pendant, `A014` T1), and a fortiori no bipartite counterexample and no bipartite tight 1-atom at 24. First order covered by generation rather than genbg-filtering; inherits `C039`'s anchor stack and caveat shape (no genbg cross-check at 24 itself; genbg and the generator agree at every even order 14–22). | tested | `E015` (`anchors` 20,082 checks under both interpreters; `crosscheck 14` proves the genbg-generated class equals the `E010`-stream-derived class by `labelg` set equality; T5 skips validated against genbg at orders 8–14; cycle detector cross-validated against an independent brute-force enumerator on all 994 connected graphs of order \(\le7\) and 1,000+ bipartite graphs of orders 8–11) | genbg/geng/labelg from nauty 2.9.3; `L035` T3/T4/T5 supply the criterion, the completeness reading, and the split pruning; the run says nothing about `L034` channel (ii) |
| C035 | computational observation | E013 order-16 closed extension (S017; PyPy 7.3.23, 5,986s): stream 1,006,553,591 (new datum — no `C027` reference exists at 16); 55,213,924 eligible graphs; 420,006,891 admissible pairs; zero non-taut completions; **twelve taut closed hits, all at boundary \(s_{\max}=2d\) and all containing a \(C_8\)** — so no strict taut pinched pair exists at order 16 (the strict catalogue stands complete at seven witnesses through 16, via the boundary-flag subsumption asserted at orders \(\le15\)) and no power-free closed taut pair exists at 16 (pair-level only; order 16 has no analogue of `C027`'s stream-level statement, and the `L034` congruence channels remain unscanned there). Bands 4:3/6:3/7:6; eight hits pendant-type (boundary, so their reductions are excess-\(+1\) cores). **Three new equality blocks on three new graphs — the first new blocks since D14 and the first band-4 blocks since Petersen\(-e\):** F16 (band 4, terminal degrees (3,3), on a graph with no sub-cubic vertex; its two admissible band-4 pairs are isomorphic as two-terminal gadgets), G16 (band 4, (2,4)), H16 (band 6, (2,2), the first block with \(C_{16}\) in its spectrum). The block catalogue grows to **eight, every one at exact equality \(s_{\max}=2\,s_{\min}\)**; no strict block through order 16. All three band-4 core gadgets are **disjoint-type** (4 shortest paths, 4 internally disjoint pairs, 4 of 6 \(C_8\)s through both terminals), so the `L033` pencil endgame's exhaustive empty base extends to order 16; the interference census stays perfect — every \(C_8\) of every new core pair is a two-through-path symmetric difference (6/6, 6/6, 6/6, 10/10). | tested | `E013` `runclosed 16` (S017 addendum in the E013 README; data `catalogue_closed_n16.json`; classifications via the anchored E013 primitives incl. the labelg-cross-anchored two-terminal isomorphism engine) | geng anchored as in `E010`–`E013`; `L031`–`L033` supply the predictions tested |
| L036 | completeness lemma | **1-atom completeness (A018 T1–T3).** A 1-atom in the sense of D-A4 (connected, power-free, exactly one vertex of degree \(<3\), that vertex of degree 1 **or** 2) exists **iff** a counterexample to statement 0.1 exists: (\(\Leftarrow\)) is `L025` R4; (\(\Rightarrow\)) take a connected component of the counterexample and attach one pendant vertex — a pendant lies on no cycle, so the spectrum and power-freeness are unchanged, and the pendant is the unique sub-cubic vertex. Hence **the unrestricted 1-atom question is conjecture-complete** — the one-terminal analogue of `L026`'s rung completeness, and cheaper (no scaffold: a pendant is spectrum-invisible). Consequences: (a) `L029`'s reduction is vacuous as originally stated and must be read with **tight** 1-atoms (exceptional degree exactly 2), where it is not vacuous; (b) "no tight 1-atom" is implied by 0.1 but is not known to imply it — the pendant construction yields degree 1 only, and a *cubic* counterexample admits no known conversion into a tight 1-atom (edge deletion leaves two sub-cubic vertices; subdivision moves cycle lengths), so the tight question is a genuine strictly-weaker sub-question; (c) `A016` M6's ceiling reading is circular as recorded — the non-circular form is "closing every assembly channel plus **no tight 1-atom** yields exactly the cubic reduction"; (d) `A012` Remark T4.1 ("minimum-order 1-atoms are tight") is withdrawn as unproved, its argument failing precisely when the reduct is a cubic counterexample. | proved | `A018` T1–T3 (hand proofs; the construction is one line and definitional against D-A4) | `D001`–`D004`; D-A4 and `L025` R4 (`A011`); audits `L029`/`A012` T4 and `A016` M6 |
| L037 | structure theorem | **The subdivision descent and the degree-\(\ge4\) structure of a minimal counterexample (A020 W2-T3/T4/T5).** In an order-then-size minimal counterexample \(G\), write \(A=V_3\), \(B=V_{\ge4}\), \(\beta=\lvert B\rvert\), \(A_j=\{a\in A:\lvert N(a)\cap A\rvert=j\}\), \(p=\lvert A_1\rvert\), \(q=\lvert A_2\rvert\). (Apex) Every component \(S\) of \(G[A]\) has a \(b\in B\) with \(\lvert N(b)\cap S\rvert\ge d(b)-2\ge2\); for a \(K_2\)-component that \(b\) is unique, has degree exactly 4, and spans a triangle with it. (Descent) The **link graph** \(H_1\) on \(B\) — one edge \(N(a)\cap B\) per \(a\in A_1\) — is simple and loopless; every \(\ell\)-cycle of \(H_1\) lifts to a \(2\ell\)-cycle of \(G\), so \(H_1\) is **power-free**; by order-minimality of \(G\) it has no subgraph of minimum degree \(\ge3\), i.e. is **2-degenerate**, giving \(p\le2\beta-3\) for \(\beta\ge2\). (Equality classification) \(\lvert V_3\rvert=\frac23n\) forces \(G[A]\) a perfect matching, every \(d(b)=4\), and \(G\cong S(H)+\mathcal P\) for a simple 4-regular \(H\) on \(n/3\) vertices with a \(P_3\)-decomposition \(\mathcal P\) — and the descent then kills the case (\(H_1=H\) would be 4-regular). The descent converts a minimal counterexample into a *smaller* power-free object — the dossier's first mechanism of that kind — and is independent of the density question. | proved | `A020` W2-T3/T4/T5 (hand proofs; audited line-by-line by the S019 orchestrator); machine support `E017` | `C004`/`C005` (verified), `L002` (independence, for the order-then-size object via the verified Cor 0.1(2)), `D001`–`D004` |
| L038 | density theorem | **The cubic-density theorem (A020 W2-T1/T6), superseding `C006` internally.** In an order-then-size minimal counterexample of order \(n\): \(\lvert V_{\ge4}\rvert\le\frac12\lvert V_3\rvert\), i.e. \(\lvert V_3\rvert\ge\frac23n\) — from \(e(A,B)=2p+q\ge4\beta\) (independence) and \(2p+q\le2\lvert A\rvert\) (`C005`: \(G[A]\) has no isolated vertex) — and, with the descent (`L037`), \(3\lvert V_3\rvert\ge2n+3\): \(\lvert V_3\rvert/n\ge\frac23+\frac1n\), \(\lvert V_{\ge4}\rvert\le\frac{n-3}3\), and \(\lvert A_2\rvert\ge6\) whenever \(\beta\ge2\) (cases \(\beta\le1\) closed by `L003`). **Delimitation (W2-T7/T8, `E017`):** the explicit 15-vertex graph S15 satisfies \(\delta\ge3\), the full conclusion of Carr's Lemma 0.1, \(C_4\)-freeness, independent \(V_{\ge4}\), and `C005`'s conclusion, at density exactly \(2/3\) — so no argument from those hypotheses can beat \(2/3\); the proved constraint set has LP optimum \((2\beta+3)/(3\beta+3)\to2/3\), so the additive \(+3\) is the entire gain this counting affords; and no density constant \(<1\) can deliver `G015` (the final step is necessarily structural). S15's link graph is \(K_5\), whose 4-cycles lift to its fifty 8-cycles — exactly the failure the descent forbids. | proved | `A020` W2-T0/T1/T6 (hand proofs, audited); sharpness `A020` W2-T7 + `E017` (35 checks, CPython 3.14.2 and PyPy 7.3.23, reproduced independently by the orchestrator) | `C004`/`C005` (verified), `L002`, `L003`, `L037` |
| L039 | correspondence lemma | **The closure calculus and the (3,3) correspondence (A019 W1-T1/T2/T3).** For a graph \(H\) and distinct \(a,b\in V(H)\), with \(S=S(H,a,b)\) the set of simple \(a\)–\(b\) path lengths: \(\mathrm{Spec}(H+u)=\mathrm{Spec}(H)\cup(S+2)\) (\(u\) a new vertex joined to \(a,b\)); if \(ab\notin E\), \(\mathrm{Spec}(H+ab)=\mathrm{Spec}(H)\cup(S+1)\), and likewise \(\mathrm{Spec}(H+az)=\mathrm{Spec}(H)\cup(P_H(a,z)+1)\); if also \(N(a)\cap N(b)=\emptyset\), \(\mathrm{Spec}(H/(a{=}b))=\Sigma'\cup S\subseteq\mathrm{Spec}(H)\cup S\) with \(\Sigma'\) the lengths of cycles meeting \(\{a,b\}\) at most once — **equality fails** in that direction (recorded asymmetry, caught by `E016` A1). Consequently \((B,u)\mapsto(B-u,a,b)\) is a **bijection** from tight 1-atoms with \(B-u\) connected and both \(u\)-neighbours cubic onto \(\mathcal G=\{(H,a,b):\) connected, \(\deg a=\deg b=2\), all others \(\ge3\), \(\mathrm{Spec}(H)\) power-free, \(S\cap(\mathbb P-2)=\emptyset\}\), \(\mathbb P=\{4,8,16,\dots\}\). Every member of \(\mathcal G\) is a \(C_4\)-free (D)-core with \(2\notin S\) (no common neighbour), \(1\in S\iff ab\in E\), and order \(\ge16\) by `C027` — so tight 1-atoms in this case have order \(\ge17\). This is `L034`'s ring shape at \(L=1\), offset 2, and the criterion imposes **no upper bound on \(s_{\max}\)**: a congruence channel, not a pinch. | proved | `A019` W1-T1–T3 (hand proofs, audited); `E016` A1/A5 (verified on 12,109 connected graphs / 331,115 vertex pairs; the failed first draft of the identification equality is why the inclusion is stated) | `D001`–`D004`, D-A2 (`A011`); `C027` (tested) for the order bound |
| L040 | reduction engine | **The engine and the peel (A019 W1-T4/T5).** Let \(n_0\) be the minimum order of a tight 1-atom (assuming one exists). (Engine) If any counterexample of order \(<n_0\) exists, a **cubic** counterexample exists: run `A012` T4 against an order-then-size minimal counterexample \(G^*\) of order \(<n_0\) — if some degree-\(\ge4\) vertex has a cubic neighbour, deleting that edge yields a tight 1-atom of order \(<n_0\) (bridge case included), impossible; if all its neighbours have degree \(\ge4\), deleting one such edge contradicts size-minimality; so \(G^*\) is cubic. Strictness \(<n_0\) is load-bearing. (Peel) Any 1-atom of D-A4 (exceptional degree 1 **or** 2) of order \(<n_0\) yields a counterexample of order \(<n_0\), hence a cubic one: the degree-2 case is impossible below \(n_0\), and deleting the leaf leaves a counterexample or an impossible smaller tight 1-atom. This is the structural repair of the withdrawn `A012` Remark T4.1 — the branch it could not close ("the reduct is a counterexample") is the conditional target's *goal*, not an obstacle. No Carr import is used (W1-T16). | proved | `A019` W1-T4/T5 (hand proofs, audited) | `A012` T4 machinery at `L029`'s recorded strength; `D001`–`D004` |
| L041 | conditional reduction | **The case analysis at the defect vertex; the cubic reduction modulo one configuration; the order bound (A019 W1-T6/T7/T8).** Let \(B\) be a minimum-order tight 1-atom, \(u\) its degree-2 vertex, \(N(u)=\{a,b\}\), \(H=B-u\) (at most two components). Exactly one of five cases holds: (1) \(u\) cut with a cubic side — **impossible** (the side is a smaller tight 1-atom); (2) \(u\) cut, both sides \(\ge4\) — both sides are counterexamples of order \(\le n_0-2\), and \(n_0\ge39\) (`L022`); (3) \(u\) non-cut, degrees \(\{3,\ge4\}\) — **impossible** (\(H\) is a smaller tight 1-atom); (4) \(u\) non-cut, both \(\ge4\) — \(H\) is a counterexample of order \(n_0-1\), \(n_0\ge20\); (5) both cubic — \((H,a,b)\in\mathcal G\) (`L039`), splitting by tautness: (5a) non-taut — `L027`'s lobe is a min-degree-3 power-free graph or a 1-atom, and the peel (`L040`) yields a counterexample of order \(<n_0\); (5b) vertex-taut — **survives**. Hence under (R) ("no counterexample of order \(<n_0\)") every minimum-order tight 1-atom is in case (5b), and: **if case (5b) is excluded, then a tight 1-atom implies a cubic counterexample (`L040`), so with `L029` statement 0.1 holds iff no cubic counterexample exists — the `G015` deliverable reduced to one local configuration.** Unconditionally: **every tight 1-atom has order \(\ge17\)** (cases (1)/(3) die against `C027`'s \(\le15\) emptiness, (2)/(4) against `L022`, (5) against `C027` through `L039`). `E016` A2 verifies the case dichotomy exhaustively on 67,432 shaped graphs at orders 5–9. | proved | `A019` W1-T6/T7/T8 (hand proofs, audited); `E016` A2/A4 | `L027`, `L029`, `L039`, `L040`; `C027` (tested) and `L022` for the order bounds; (R)-conditional statements flagged inline |
| L042 | structure theorem | **The residual object: forced structure in case (5b) under (R) (A019 W1-T9–T15.1).** Conditional on (R), for a minimum-order tight 1-atom in case (5b): \(B\) is **2-connected**; refining to minimum order *then size*, the degree-\(\ge4\) vertices of \(B\) are **independent** (`L002`'s conclusion transported; 2-connectivity supplies bridgelessness); the through-set satisfies \(S\cap(\mathbb P-2)=\emptyset\), and if \(ab\notin E\) also \(S\cap\mathbb P\ne\emptyset\) **and** \(S\cap(\mathbb P-1)\ne\emptyset\) (else identifying or joining the terminals yields a counterexample of order \(<n_0\)); \(\min S=d_H(a,b)\notin\{2,6,14,\dots\}\) and \(\max S\ge7\) when \(d\ge3\) (`L030`'s boundary fact plus \(6\in\mathbb P-2\)); in the triangle case \(ab\in E\), the suppressed \((H-a-b,p,r)\) is a vertex-taut \(C_4\)-free (D)-core whose \(p\)–\(r\) length set \(\Lambda\) avoids \(\mathbb P-4\) and \(\mathbb P-3\), meets \(\mathbb P-1\), and has \(\max\Lambda\ge6\) (`L028`/`L030`); **Mersenne saturation**: for every non-neighbour \(z\) of a terminal, \(H\) has a simple terminal-to-\(z\) path of length \(2^k-1\) (else \(H+az\) is a smaller tight 1-atom) — demoted as a standalone lever by the pre-registered `E016` A6 kill test (generically satisfied in the ambient class without power-freeness), but its parity corollary stands: **\(H\) and \(B\) are non-bipartite**, a hand proof at every order of what `C034` verified through order 22; at any cut vertex of \(H\) (which separates the terminals, `L032`), each side's local through-set must itself meet a forbidden set (\(S_1\cap(\mathbb P-2)\ne\emptyset\), or \(S_1\cap(\mathbb P-1)\ne\emptyset\) in the bridge case) while the Minkowski sum avoids \(\mathbb P-2\) — a cancellation tension, and neither terminal is a cut vertex; and at band \(d_H(a,b)=4\) the `L033` pencil dichotomy applies **verbatim** (no two internally disjoint 4-paths; unique terminal-adjacent pencil vertex; fan of \(\le2\) strands). Transfer verdict (W1-T15, recorded analysis, not a claim): `L030`'s middle-layer collapse does **not** transfer — it anchors on interval confinement of the through-set, and the closure channel bounds nothing above — so the machinery that reaches the residual object is the band machinery, not the collapse machinery. | proved | `A019` W1-T9, T9.1, T10, T11, T11.1, T12, T13, T14, T15.1 (hand proofs, audited); `E016` A3/A6 | (R) as standing hypothesis, flagged inline; `L028`, `L030`, `L032`, `L033`, `L039`–`L041`; `C034` as computational context only |

| C036 | computational observation | **E018 order-16 \(\mathcal G\)-profile scan (S020): the decisive order is empty.** Over the full geng stream `geng -q -c -f -d2 16 23:120` (346,573,602 graphs; 24-part res/mod split whose sum is reproduced exactly by an independent unsplit `geng -u` run; maximum edge count seen 31 against geng's own cap 33), the profile class — connected, \(C_4\)-free, exactly two degree-2 vertices, all others \(\ge3\) — has 29,713,305 members at order 16, and **every one contains a \(C_8\)** (the \(C_{16}\) test is never decisive); zero are power-free, so \(\mathcal G\) (`L039`) has **no member of order 16** — the \(S\)-condition never even engages. With `L041`'s case analysis this lifts the bound unconditionally: **every tight 1-atom has order \(\ge18\)**, and every \(\mathcal G\)-member has order \(\ge17\) (case (5) needs a \(\mathcal G\)-reduct of order \(n_0-1\); cases (2)/(4) give \(\ge39\)/\(\ge20\) via `L022`). Stream-level emptiness (`C027`'s form) thus extends to order 16 for this one profile; the rest of the \(\le2\)-sub-cubic class at 16 remains pair-level only (`C035`). Calibration: the identical chain reproduces `E016` A6's class counts exactly at orders 8–13 (1; 2; 22; 125; 1,139; 10,966) and extends them to 130,461 (order 14) and 1,826,839 (order 15), all \(C_8\)-blocked — an independent re-derivation of `C027`'s verdict on this profile through 15. Proximity statistic: the minimum \(C_8\) count over the order-16 class is **1** (orders 14/15: 1/2) — single-\(C_8\) blocking occurs, against a minimum of 13–19 in the bipartite class (`C034`); the always-decisive \(C_8\) filter makes a dedicated \(\{C_4,C_8\}\)-free generator (`G014` item 6) the right instrument from order 17 on. | tested | `E018` `scan.py` (26-anchor suite passed under CPython 3.14.2 and PyPy 7.3.23 before production; production PyPy 7.3.23, 8 workers, 2,893.5 CPU-s; harvest identity profile = \(C_8\)-blocked + \(C_{16}\)-blocked + survivors asserted; survivor path re-checks power-freeness independently of the filter chain) | geng nauty 2.9.3 anchored as in `E010`–`E016`; `L039`/`L041` supply the correspondence and the case analysis; inherits `C027`/`L022` computational-lineage caveats |
| C037 | computational observation | **E018/mod4.py (A021, S020): the congruence-route kill condition of `A019` exit item 2 fired.** In the vertex-taut part of the case-(5b) terminal profile (power-freeness dropped, as in `E016` A6), the residual object's forced membership triple — \(S\cap\mathbb P\ne\emptyset\), \(S\cap(\mathbb P-1)\ne\emptyset\), \(S\cap(\mathbb P-2)=\emptyset\) — is **realized**: 2 witnesses at order 10 (residue sets \(\{0,1,3\}\)), 0 at 11, 3 at 12 and 55 at 13 (all with full residue set \(\{0,1,2,3\}\)); both admissible mod-4 patterns occur, so residue-2 presence is uncontrolled and no residue-level invariant separates the witnesses from the class. One order-10 witness is **Petersen minus an edge** (`labelg` canonical-form equality; \(S=\{4,5,7,8\}\), spectrum \(\{5,6,8,9\}\)), which satisfies every `L042` through-set and structure constraint testable without power-freeness (memberships, \(\min S=d=4\), \(\max S\ge7\), non-bipartite, 2-connected, vertex-taut, Mersenne-saturated) and fails only the band-4 pencil condition — exactly where power-freeness enters. Hence the hypothesis set {tautness, \(C_4\)-freeness, (5b) profile, the three memberships, non-bipartiteness, 2-connectivity, saturation} is simultaneously realizable and **no congruence-type theorem at any modulus can exclude case (5b) from those hypotheses alone**; the mechanism-side half (`A021` T1: the chain calculus controls path arithmetic mod 2 only — the \(2\lvert E(P)\cap E(Q)\rvert\) leak) says no cycle-structure hypothesis can confine \(S\) mod 4 in the first place. Side data: vertex-tautness is generic in the profile class (\(\ge98.9\%\) at orders 10–13); no taut member with \(S\subseteq\mathbb P-2\) (invisible shape) exists through order 13. | tested | `E018` `mod4.py` (PyPy 7.3.23; exact path enumeration with essential-vertex mask; `labelg` identification), analysis `A021` T1–T4 | primitives anchored as in `C036`; `L035` T2 explains the forced non-bipartiteness of every witness; kill condition as pre-registered in `A019` exit item 2 / `problem.json`; consumes nothing from `C004`–`C006` |
| L043 | interface theorem | **The chain-case constraint system (A022 W1-T1/T2/T3/T5/T7; S021).** For the case-(5b) residual object under (R) with \(H=B-u\) **not** 2-connected: the block–cut tree is a path \(L_1,\dots,L_m\) (\(m\ge2\)) with \(S=T_1+\dots+T_m\); \(ab\notin E(H)\) always (so the full `L042` memberships apply); end blocks are 2-connected with \(\deg_{L_1}(a)=\deg_{L_m}(b)=2\); bridges are interior, pairwise non-adjacent, and \(k\ge\lceil(m+1)/2\rceil\) blocks are non-bridge; every non-bridge block is, standalone, 2-connected, \(C_4\)-free, power-free, with \(\le2\) degree-2 vertices (both among its terminals). **Closure battery** (all [min] — every prefix hides \(b\), so each closure lands tight-1-atom-shaped): every prefix \(A_i\) meets \(\mathbb P-2\) (bridge variant \(\mathbb P-1\), provably redundant), meets \((\mathbb P-1)\cup\{1\}\), and meets \(\mathbb P\cup\{1,2\}\), with the \(\{1\},\{2\}\)-escapes localized to \(i\le2\); at attachment degree \(\ge3\) the whole prefix is Mersenne- and power-saturated at every legal pair; mirrors on suffixes. Consequently every prefix and suffix contains both parities, so — by `L035` T2 on the vertex-taut prefixes — **every prefix, every suffix, and both end blocks are non-bipartite** (middle blocks unconstrained). | proved | `A022` W1-T1/T2/T3/T5/T7 (hand proofs; audited line-by-line by the orchestrating session); instance checks `E020` anchors A5–A8 | (R) via `L041`; `L032`/`A014` T3.3, `L035` T0/T2, `L039`, `L042` (`A019` W1-T14/T12), `L027`; [min]/[R] labels per row as recorded in `A022` |
| L044 | saturation lemma | **Terminal power saturation (A022 W1-T4; S021).** For **every** case-(5b) residual object under (R) (2-connected or not): for each terminal \(t\in\{a,b\}\) and every \(x\notin N_H(t)\cup\{t\}\) with \(N_H(t)\cap N_H(x)=\emptyset\) and \(x\) not the other terminal, \(P_H(t,x)\cap\mathbb P\ne\emptyset\) — the merge \(H/(t{=}x)\) is otherwise a tight 1-atom of order \(n_0-2\). The \(\mathbb P\)-channel sibling of `L042`'s Mersenne saturation, which `A019` missed; [min]. **Status: forced structure and a generation-time filter, not an exclusion lever** — vacuous on Petersen\(-e\) (diameter 2), and by the `E016` A6 precedent it must pass an A6-style genericity kill test before any promotion (named follow-up, not run). | proved | `A022` W1-T4 (hand proof; audited) | (R) via `L041`; `L039` merge inclusion (recorded asymmetry honored); demotion discipline per `A019`/`E016` A6 |
| L045 | arithmetic lemma | **The collision table: the chain cancellation quantified (A022 W1-T6; S021).** For \(x=2^p+\varepsilon_1\), \(y=2^q+\varepsilon_2\), \(p,q\ge2\), \(\varepsilon_i\in\{0,-1,-2\}\): \(x+y\in\mathbb P-2\) **iff** \(p=q\) and \(\{\varepsilon_1,\varepsilon_2\}\in\{\{-1,-1\},\{0,-2\}\}\). So at every cut of the chain, (C7) forces exactly three exponent-disjointness conditions — \(M(A_i)\cap M(Z_i)=\emptyset\), \(E_0(A_i)\cap E_2(Z_i)=\emptyset\), \(E_2(A_i)\cap E_0(Z_i)=\emptyset\) — and **nothing more**: every other forced-pair sum avoids \(\mathbb P-2\) automatically (in particular \((\mathbb P-2)+(\mathbb P-2)\) never meets \(\mathbb P-2\)). Corollary: the forced memberships alone can never exclude the chain case (neighbouring near-intervals dodge naturally; abstract solutions exist from block orders 15–16, `C038`); any exclusion must consume through-set **realizability** or \(C_8\)-interference structure. This is also the formal ceiling of `A019`'s "cancellation tension" reading. | proved | `A022` W1-T6 (hand proof; 2-adic case check re-derived by the orchestrating session); machine anchor `E020` A9/A9b | none beyond `D001`–`D004` (pure arithmetic); the corollary reads `L043`'s system |
| L046 | order dichotomy | **The chain case is confined to high order (A022 W1-T8; S021).** Under (R), in case (5b) with \(H\) not 2-connected: every non-bridge block lies in `C027`'s class and is power-free, so has order \(\ge16\) (`C027`), \(\ge17\) if both its terminals have block-degree 2 (`C036`), and \(\ge18\) once the class emptiness extends through order 17 (`C039`); hence \(\lvert V(H)\rvert\ge14k+m+1\) (16-per-block reading) and **either \(H\) is 2-connected or \(n_0\ge32\)** — \(n_0\ge33\) at \(m=3\), \(\ge48\) at \(m\ge4\), \(m\le(n_0-9)/8\); with `C039`'s per-block \(\ge18\) the dichotomy sharpens to **\(n_0\ge36\)** (\(m=3\): \(\ge37\); \(m\ge4\): \(\ge54\); \(m\le(n_0-10)/9\)). Contrapositive: **for \(n_0\le35\) the residual object's \(H\) is 2-connected** — the chain case is empty below order 36, and the 2-connected branch is the whole case-(5b) frontier there. The recorded falsifiable target of the S020 checkpoint ("bound the block-chain length or prove \(H\) 2-connected") is delivered in this dichotomy form; an absolute bound on \(m\) is impossible from memberships alone (`L045`). | proved | `A022` W1-T8 (hand proof; accounting re-derived by the orchestrating session) | (R) via `L041`; `L043`; `C027`/`C036`/`C039` at tested strength with their geng lineages; `E010` stream spec verified (`-d1`, mine \(=\lceil(3n-4)/2\rceil\) implied) |
| C038 | computational observation | **E020 (S021): the chain-case kill test is refuted in range; the chain floor; the C037 datum explained.** Block catalogue: all 2-connected \(C_4\)-free \(\le2\)-degree-2 blocks at orders \(\le13\) over all terminal pairs, plus order-14 exactly-two-degree-2 (129,654 of the 130,461-member class — the exactly-two counts match `E016` A6/`E018`/`C036` at every order 8–14); 226,619 taut pair instances (every 2-connected pair taut — the 2-fan fact, corroborated, kept non-load-bearing); no block exists below order 8 (streams empty at 4–7; the unique order-8 block is `GCpdag`, \(T=\{3,4,5,6,7\}\)). **Kill search: zero candidates pass even the pre-registered level** over all \(m\le3\) chain shapes ([end,end], [end,bridge,end], [end,mid,end]; 9,045 \(\mathbb P-2\)-meeting end-set pairs × 176 mid sets); single binding mechanism: every admissible pair's Minkowski sum contains \(14\) (max reachable sum 26 < 30; realized end sets are fat near-intervals reaching \(n-1\)). **Chain floor:** every vertex-taut member of the (5b) profile class with a cut vertex is a chain of blocks of order \(\ge8\), hence has order \(\ge15\); at orders \(\le14\), cut vertex \(\iff\) non-taut — predicting `E018`'s class-minus-taut counts 0/1/19/113 at orders 10–13 exactly, so **`C037`'s "all sixty witnesses 2-connected" was forced by order alone and carried no chain-case evidence**. Abstract frontier: \(\{7,8,12,13,14\}+\{8,11,12,13,14,15\}\) (block orders 15–16) passes the kill and extended levels with exactly `L045`'s dodging pattern — whether such through-sets are **realized** by taut \(C_4\)-free blocks at orders 15–16 is the named kill rung. Scope: blocks \(\le13\) any profile + order-14 two-degree-2 only; \(m\le3\); nothing here bears on the real (power-free, order-\(\ge16\)) blocks. | tested | `E020` `blocks.py` (CPython 3.14.2, geng nauty 2.9.3; 12 anchors; catalogue + search re-run and reproduced by the orchestrating session: 0/0/0 at all levels) | geng anchored as in `E010`–`E018`; primitives copied verbatim from `E018` and re-anchored; the A014 T5 composite rebuilt and failing exactly the 7 recorded rows (`A022` W1-T9) |
| C039 | computational observation | **E019 (S021): the dedicated \(\{C_4,C_8\}\)-free generator, and the order-17 \(\mathcal G\)-profile scan — EMPTY.** Instrument: nauty 2.9.3 geng recompiled with the `prune_c8` PREPRUNE plugin (`genc48`; source tarball sha256-verified; completeness argument recorded against the vendored source: the construction sequence is induced-subgraph-monotone, \(C_8\)-containment is subgraph-monotone, the plugin only deletes subtrees — output = geng's isomorph-free stream ∩ \(C_8\)-free). Verification: 146 anchors under CPython 3.14.2 **and** PyPy 7.3.23 — 23 `labelg` **set-equality** comparisons against the independent geng-then-\(C_8\)-filter pipeline across five switch variants (all nonzero), named-object in/out tests, res/mod partition checks, and the **cubic order-24 positive control** (4 graphs, set-equal to `E005` and Markström's Table 3). **Production (order 17, `-c -f -d2 17 25:136`, 16 parts):** class 2,580 (= independent unsplit count); **exactly-two-degree-2 profile: 0**; survivors 0; \(\mathcal G\)-members 0. With `L041`: **every tight 1-atom has order \(\ge19\), every \(\mathcal G\)-member \(\ge18\)**. Reproductions: profile-empty at 14/15/16 (re-deriving `C027`/`C036` cross-instrument, order 16 at ~75× less CPU than `E018`); **`C027`'s own class (min degree \(\ge1\), \(\le2\) sub-cubic) is empty at orders 16 and 17** (272,596 and 229,259 \(\{C_4,C_8\}\)-free stream graphs, none with \(\le2\) sub-cubic vertices) — a two-order extension of `C027`; likewise no \(\{C_4,C_8\}\)-free min-degree-\(\ge2\) graph at 16/17 has \(\le2\) degree-2 vertices at all. Proximity: min #degree-2 over the class is **3** at 16 and 17 (4 over the power-free part), and every 3-degree-2 boundary graph is killed by \(C_{16}\) — the first decisive \(C_{16}\) anywhere in this dossier. Second-algorithm spot check: all 205 near misses re-verified by the independent brute-force spectrum enumerator. Feasibility: tree ×\(\approx6.5\)/order — order 18 ≈ 4 min, 19 ≈ 25 min, 20 ≈ 2.8 h on 8 workers. | tested | `E019` (`prune_c8.c`, `scan.py`, `run17.sh`, `data/`; production PyPy 7.3.23, 305.9 CPU-s, 8 workers; anchors re-run by the orchestrating session under CPython: 146/146; plugin and geng call sites audited line-by-line) | nauty canonicity imported and anchored as in `E010`–`E018`; `L039`/`L041` for the correspondence and consequences; inherits `C027`/`L022` lineage caveats; `G014` item 6 discharged as an instrument |
| C040 | computational observation | **E019 min-degree-3 sweep (S021): no \(\{C_4,C_8\}\)-free graph of minimum degree \(\ge3\) exists on 14–19 vertices.** `genc48 -c -f -d3 n ⌈3n/2⌉:C(n,2)`: outputs 0 at every order 14–19 (trees \(3.6\times10^5\) … \(5.23\times10^9\); order 19 = 2,657 s). Coverage: \(\delta\ge3\) forces the edge floor, `maxe` is coverage-safe, and a disconnected example has a connected component of smaller order in the same class (excluded at the smaller orders here and below 14 by `C027`). Verification beyond the `C039` anchor stack: **full** filter-the-stream cross-checks on this exact switch set at orders 12–17 (stock geng streams 57 / 503 / 6,059 / 91,433 / 1,655,659 / 34,758,006, `labelg` set-equal results, both empty at 14–17); the cubic order-24 **nonzero positive control** on the same `-d3` code path; and the S021 orchestrator **slice audit at order 18** (`audit_mindeg3_n18_parts.py`: stock geng parts 3, 11 and 19 of 24 — 7,607,271 + 24,452,192 + 6,685,165 = 38,744,628 stream graphs, ~24% of the order-18 stream — filtered by the original `E015` detector imported from its own file: zero \(C_8\)-free survivors in every slice). **S021 follow-up harvest (stage B): order 20 is also empty** — `genc48 -c -f -d3 20 30:190`, 16 parts (partition property anchored in `C039`), all zero, 3,272 s wall on 8 workers. **S022 follow-up harvest (stage D): order 21 is also empty** — `genc48 -c -f -d3 21 32:210`, 16 parts, all zero (16 empty output files, all return codes 0; the 146-anchor gate re-passed under PyPy immediately before the run; 20,288 s wall on 8 workers) — so the emptiness now runs 14–21. Residual caveat, explicit: orders 18–21 have no *full* stream-side cross-check (streams \(\approx1.6\times10^8\), \(\approx2\times10^{10}\), larger at 20–21); those verdicts rest on the generation layer, the sampled order-18 slices, the order-20 cubic sub-slice with three agreeing instruments (`E019`'s probe, stage A's stock-geng recount, and the MathOverflow-512914 filter-the-stream computation: 510,489 cubic / 36,101 \(C_4\)-free / 0 \(\{C_4,C_8\}\)-free — the first two figures reproduced exactly by stage A), and the S022 different-tree 0-bucket corroborations at 18–20 (`C043`; same binary, different switch set and trees \(\times6.2\)–\(6.4\) larger with nonzero output). The MO thread's min-degree-3-at-20 *inference* remains unimported (`references/mathoverflow-512914-audit-2026-07-24.md`); stage B settles order 20 without it. | tested | `E019` `probe mindeg3` (PyPy 7.3.23) + `crosscheck mindeg3` 12–17 + `audit_mindeg3_n18_parts.py` (CPython 3.14.2, stock geng, `E015` detector) | `C039`'s instrument and anchor stack; `C027` for the sub-14 base; geng lineage as in `E010`–`E018` |
| L047 | finite exclusion | **Every counterexample has at least twenty-two vertices**; consequently the smallest \(\{C_4,C_8\}\)-free graph of minimum degree \(\ge3\) has between \(22\) and \(24\) vertices (window upper end unchanged: Markström's order-24 graphs, `C014`/`E005` — the window is now three orders wide). Proof: a counterexample of order \(\le21\) is power-free, hence \(\{C_4,C_8\}\)-free with \(\delta\ge3\); each of its connected components is in `C040`'s class at some order \(\le21\), all empty (orders \(\le13\) by `C027`/`L022` lineage, 14–21 by `C040` including the S021 stage-B and S022 stage-D follow-up harvests). Supersedes `L022`'s constant (\(\ge19\)); inherits `C040`'s computational lineage including the explicit 18–21 caveat. (Constant history: \(\ge20\) at the S021 close, \(\ge21\) at its same-day harvest, \(\ge22\) at the S022 stage-D harvest.) | proved | `C040`; the component argument is `L022`'s, one order up | `C040`, `C027`, `L022`; `D001`–`D004` |
| L048 | interference calculus | **The interference calculus (A023 T2/T3/T4; S022).** For a two-terminal pair \((H,a,b)\), a cycle \(C\) is an **interference cycle** iff \(E(C)=E(P)\,\triangle\,E(Q)\) for two distinct simple \(a\)–\(b\) paths (`E013`'s census predicate, matched exactly). (i) Every edge and vertex of an interference cycle lies on a simple \(a\)–\(b\) path — interference cycles are confined to the essential subgraph, so cycle-edge essentiality is **necessary** for interference-completeness (T2). (ii) Every cycle through both terminals is an interference cycle via its two arcs (T3). (iii) Interference is exactly the \(t=1\) case of `A021` T1's identity, \(\ell(C)=\ell(P)+\ell(Q)-2\lvert E(P)\cap E(Q)\rvert\), with the leak term **pinned** by the witnessing pair (a realization statement, not a residue statement — the genre `L045`'s corollary and `C037`'s diagnosis name as surviving); \(T_1(H,a,b):=\{\ell(C):C\ \text{interference}\}\subseteq\mathrm{Spec}(H)\) always, and if every cycle of \(H\) is an interference cycle (**interference-completeness** — `A023` T5's conjectured conclusion, a hypothesis here), then \(\mathrm{Spec}(H)=T_1(H,a,b)\) and for the case-(5b) closure \(B=H+u\): \(\mathrm{Spec}(B)=T_1(H,a,b)\cup(S+2)\) — the tight 1-atom's entire power-freeness becomes through-path-system arithmetic (\(T_1\cap\{4,8,16,\dots\}=\emptyset\) and \(S\cap(\mathbb P-2)=\emptyset\)). Consistency anchor: the recorded 2-closure of Petersen\(-e\), \(\{5,\dots,10\}=\{5,6,8,9\}\cup(\{4,5,7,8\}+2)\). **S023 upgrade: the completeness hypothesis in (iii) is discharged by `L049` for every vertex-taut pair** — in particular for the case-(5b) residual object, vertex-taut by `L042` — so \(\mathrm{Spec}(H)=T_1(H,a,b)\) and \(\mathrm{Spec}(B)=T_1(H,a,b)\cup(S+2)\) hold **unconditionally**, and every element of \(T_1\) is realized by a **trunk-split pair** in `L049`'s full arc form (two paths sharing a common \(a\)–\(u\) prefix and \(v\)–\(b\) suffix that meet \(C\) only at \(u\) resp. \(v\), and taking the two \(u\)–\(v\) arcs of \(C\) with \(V(A_1)\cap V(A_2)=\{u,v\}\) — the Moreover-form, spelled out per `R002` F7). | proved | `A023` T2/T3/T4 (hand proofs; re-derived line-by-line by the orchestrating session; `A021` T1 citation verified verbatim); (iii)'s hypothesis discharge: `A024`/`R002` (S023) | `A021` T1; `A019` W1-T1(1) for the closure identity; `L049` (S023) discharges (iii)'s former completeness hypothesis |
| C041 | computational observation | **E021 (S022): the frontier interference dissection — all 553 blockers decompose; the non-interference pivot trigger did NOT fire.** Exhaustive extraction over `E018`'s exact stream at orders 14–16 (stream totals 1,706,820 / 20,629,645 / 346,573,602 and class sizes 130,461 / 1,826,839 / 29,713,305 asserted equal to the records; order 16 in the 24-part split at \(\le4\) workers): the complete lists of profile members with \(\le3\) \(C_8\)s are **11 / 20 / 103** graphs (histogram low ends 1:1; 2:3, 3:17; 1:3, 2:6, 3:94; minima 1/2/1 = `C036`; no \(C_8\)-free member — `C027`/`C036` re-verified). **Dissection: all 385 blocking \(C_8\)s of all 134 exemplars are interference cycles** (strata both/one/neither: 0/6/25 at 14, 0/5/52 at 15, 5/71/221 at 16 — 380/385 in the informative strata), and **all 168 \(C_{16}\)s of the sixteen three-degree-2 boundary graphs** at orders 16–17 (`E019` spotcheck data, spectra re-verified) decompose **for every one of the three terminal pairs** (504 readings; all 84 informative one-terminal readings, all at 17; "neither" is order-impossible); the five `E013` blocks reproduce their recorded census exactly (23/23, including the length/overlap combos multiplicities). Decomposition arithmetic: all 18,299 family-(1) pair-incidences satisfy \(x+y-2s=8\); only **5** are disjoint-type (\(s=0\)) — `L033`'s band-4 shape is a measure-zero corner; the generic mechanism is heavy-overlap window rerouting, with same-length pairs (invisible to \(S\) as a set) carrying a large share. Structure: **134/134 exemplars vertex-taut**, 132/134 2-connected (the two cut-vertex exemplars at orders 15/16 — `C038`'s chain floor corroborated), 0/134 bipartite; every blocker edge essential (extending `C031`'s "no inessential edges" to the frontier). **New second calibration object** `M?AA@?WcKWHOWOL??` (order 14: full `L042` membership triple with \(S=\{3,\dots,13\}\setminus\{6\}\), vertex-taut, 2-connected, non-bipartite, 3 \(C_8\)s, all interference, none through a terminal) — any case-(5b) exclusion argument must fail on it, alongside Petersen\(-e\), unless it consumes power-freeness or minimality. | tested | `E021` `dissect.py`/`run16x4.py` (45 anchors under CPython 3.14.2 and PyPy 7.3.23, incl. exact `E013` block-census reproduction and a genuine negative control; production PyPy; full CPython dissect re-run byte-identical; orchestrator audit: anchors re-run both interpreters, three sample claims re-derived with independently written code, probes re-run exact) | predicate = `E013` semantics; `E018` stream and primitives lineage; descriptive scope — the dissection proves nothing beyond the named graphs; extraction exhaustive at orders 14–16 |
| C042 | computational observation | **E021 (S022): the tautness dichotomy — interference-completeness is empirically exactly vertex-tautness-shaped.** (a) `smallworld`, exhaustive over the profile class at orders 10–12 with power-freeness dropped: **every cycle of every length of every vertex-taut member is an interference cycle** (1,266 taut members, 108,882 cycles, zero failures), while **every one of the 20 non-taut members fails** (1,148 of their 1,168 cycles non-decomposable) — an exact biconditional on the sample, with the necessity direction proved (`L048`(i)). (b) `tautgeneral`, exhaustive over **all** connected graphs of orders 4–7 and **all** 12,313 vertex-taut pairs (no degree, girth, \(C_4\), or power hypotheses): 723,926 (pair, cycle) instances, **zero failures**. This is the evidence base for the candidate lemma "vertex-taut \(\Rightarrow\) interference-complete" (`A023` T5 — a labelled conjecture, not a claim). **S022 follow-up harvest: the first two kill rungs are spent, T5 SURVIVES both.** (c) Stage A — the order-19 exemplar (vertex-taut, the most relevant object in the dossier): all **411 cycles** across every length \(\{3,5,6,7,9..19\}\) decompose, zero failures (398 through-paths; `exemplar_t5_n19.json`; determined-partner algorithm — \(E(Q)=E(P)\triangle E(C)\) tested against the complete path index — deliberately different from `E021`'s pairwise search). (d) The three on-disk order-20 profile members (`t5_n20_profile.json`): all vertex-taut, all **1,890 cycles decompose**, zero failures, every \(C_{16}\) verdict re-verified by the independent pairwise algorithm (254/254). (e) The four part-14 order-20 members (`collect_n20_part14.json` + `profile_n20_part14.g6`; recollected from the full part-14 re-run whose stream total reproduces the stage-B tally **exactly**, 439,745; two identical-aggregate runs — determinism check): `S????A?O@_@_?oIABODC?S@?@a?H_CA_?`, `S???C@?G?_P?B?@_@OQH?WO?PO?cQD?g?`, `S???C@?G?_P?B?@_@OQGAGc?p?ABOC_W?`, `S???C@?G?_P?B?oO?S`B?@__SO@?o?s??` — all vertex-taut, 2-connected, 65–80 \(C_{16}\)s each, and all **2,360 cycles decompose**, zero failures (first member independently re-audited by the orchestrator: spectrum, 69 \(C_{16}\)s, \(S\ni6,14\), 572 cycles). **Cumulative: all eight profile objects in existence (orders 19–20) survive T5 — 4,661/4,661 cycles — and every one carries the \(S\ni\{6,14\}\) double blocking.** **S023 (`E023`/`C044`): the remaining ordered kill tests are spent and survived** — `smallworld 13` (biconditional **exact at order 13**: all 10,853 taut members pass on 1,614,300 cycles; all 113 non-taut members fail) and the general probes (order \(\le8\) **exhaustive** — every graph, every vertex-taut pair, every cycle; orders 9–11 cyclomatic-bounded slices), zero failures everywhere. **The dichotomy is now a theorem on connected graphs with \(\delta\ge2\) (`L050`), with sufficiency in full generality (`L049`)**; this row's empirical scope notes are superseded by those proofs, and further scanning of this class can no longer falsify T5. | tested | `E021` `smallworld`/`tautgeneral` (PyPy 7.3.23; both re-run exactly by the orchestrating session) | primitives as in `C041`; `L048`(i) supplies the failure direction |
| C043 | computational observation | **E022 (S022): the \(\mathcal G\)-profile ladder at orders 18–19 — order 18 empty; order 19 is the FIRST nonempty profile rung, its unique member doubly blocked; every tight 1-atom and every \(\mathcal G\)-member has order \(\ge20\).** Instrument: `E019`'s `genc48` reused by path import (nothing written into `E019`; binary sha256-pinned in `E022/data/provenance.json`); the 146-anchor suite re-passed under CPython 3.14.2 **and** PyPy 7.3.23 before any production (anchor output **byte-identical** to `E019`'s own file). Production `-c -f -d2`, 16 parts, `--verify-all` on throughout: **order 18** — class 108,447, **profile 0**, min #degree-2 = 3; **order 19** — class 74,589, **profile 1**: `R???C@?GC_B?@_aAA_aP?W_?BO@Gc?` (19 vertices, 28 edges, degrees \(4\cdot3^{16}\cdot2^2\), terminals \((7,8)\) non-adjacent at distance 5, girth 3, spectrum \(\{3,5,6,7,9,\dots,19\}\), **46 \(C_{16}\)s**, 2-connected, non-bipartite, **vertex-taut** (orchestrator-verified), \(S=\{5,\dots,18\}\) a full interval with \(S\cap(\mathbb P-2)=\{6,14\}\)) — **\(C_{16}\)-blocked and \(S\)-condition-violating independently**; survivors 0; \(\mathcal G\)-members 0. Verification: order-18 independent unsplit count = 108,447 **exactly**; order-19 dual-split partition check (16 vs 24 parts) — totals equal, both runs isomorph-free, class sets `labelg` **set-equal**, every aggregate equal (the order-19 *unsplit* count is the named S022 follow-up, stage C); spotchecks: all 812 + 1,421 class members with \(\le4\) degree-2 vertices re-verified by the brute-force spectrum enumerator; class files complete (saved rows = tallies; no save cap hit). **Buckets: the 0- and 1-buckets are empty at both orders** (coverage complete — \(\le2\) degree-2 vertices force \(m\ge\) mine), so (i) `C040`'s orders 18–19 — exactly its two flagged orders — are corroborated from a different generation tree (\(\times6.2\)–\(6.4\) larger, nonzero output; same generator binary, so the generation-layer caveat stands), and (ii) **no tight 1-atom of order 18 or 19 exists, directly by generation**: a tight 1-atom of order \(\le19\) is connected (a min-degree-3 component would contradict `C040`) and sits in the 1-bucket. Consequences with `C036`/`C039`: **every tight 1-atom has order \(\ge20\)** (the direct route; `L041`'s case route agrees — case (5) now needs a \(\mathcal G\)-reduct of order \(\ge20\), case (4) gives \(\ge20\), case (2) \(\ge39\)) **and every \(\mathcal G\)-member has order \(\ge20\)**. Proximity inverted at the boundary: min #degree-2 reached the target **2** at order 19 (the exemplar) while the minimum over the power-free part **rose** to 5; \(C_{16}\) is decisive on the entire \(\le4\)-degree-2 stratum at 19 (1,421/1,421) and on 811/812 at 18. Growth \(\times7.90\) (17→18) and \(\times7.06\) (18→19). **S022 follow-up harvest (stages B/C):** stage C — the order-19 independent unsplit count = **74,589 exactly**, closing this row's named partition follow-up. Stage B — **order 20 is spent**: class 2,569,481 (16 parts, `--verify-all`; \(2.67\times10^{11}\) prune calls; 74,188 CPU-s, 12,510 s wall on 8 workers — growth \(\times8.25\), above the \(\times6.5\)–\(7.9\) trend but far inside the stop condition), **profile 7** (parts 1/8/14), **every one \(C_{16}\)-blocked, zero power-free** — so \(\mathcal G\) has no order-20 member and **every \(\mathcal G\)-member has order \(\ge21\)**; the degree-2 histogram starts at bucket 2 (0-/1-buckets empty, coverage complete as at 18/19), so **no tight 1-atom of order 20 exists, directly by generation**, and with `L041` the atom floor becomes **\(\ge22\)**: case (2) \(\ge39\); case (4) — \(H\) is a counterexample of order \(n_0-1\), so `L047` (\(\ge21\)) gives \(n_0\ge22\) (the case's original constant 20 predates `L047`); case (5) — the \(\mathcal G\)-reduct has order \(n_0-1\ge21\), so \(n_0\ge22\); the direct route independently certifies \(\ge21\). Every member of the \(\le4\)-degree-2 stratum (7 + 377 + 7,205 = 7,589 graphs) is \(C_{16}\)-blocked (the power-free part starts at 5 degree-2 vertices — minima 4/5/5 at 18/19/20); spotcheck: 4,436 near-boundary graphs re-verified by the brute-force enumerator. **SAVE_LIMIT caveat, new at 20:** the class files are per-part 200,000-row samples (572,530 of 2,569,481 saved) — orders 18/19 were complete; parts 1/8 are complete and their **3 profile members were fully verified and T5-tested by the orchestrating session** (`t5_n20_profile.json`: all vertex-taut, 2-connected, non-bipartite-spectrum, \(S\ni6,14\) — the same independent double blocking as the order-19 exemplar — 1,890 cycles, **zero non-decomposable**, all 254 \(C_{16}\)s re-verified by the independent pairwise algorithm); the part-14 recollection recovering the other 4 (`collect_n20_part14.py`, single part re-run with a full profile collector and a stream-total assertion) **landed and is harvested**: stream total 439,745 reproduced **exactly**, exactly 4 members found, identities and per-graph data on disk (`profile_n20_part14.g6`, `collect_n20_part14.json`; two runs with identical aggregates — the first lacked per-graph persistence, a disclosed script bug, and was re-run fixed); their T5 verdicts are recorded on `C042`. | tested | `E022` (`ladder.py` + part drivers + `splitcheck`/`summary`; production PyPy 7.3.23, contended timings recorded; orchestrator audit: anchor byte-compare, harvest/count/splitcheck data re-read directly, the exemplar fully re-derived with independently written code incl. tautness, class-file membership checked in both splits) | `E019`/`C039` instrument and anchor stack; `L041` for the case route; `C040` for the connectivity step of the direct route; inherits the geng/nauty generation-layer lineage; exhaustive for the stated class at orders 18–19; nothing proved at order \(\ge20\) |
| L049 | interference-completeness theorem | **T5 is a theorem (S023): every vertex-taut pair is interference-complete.** Let \(H\) be a finite simple graph and \(a\ne b\) vertices (distinctness explicit per `R002` F6 — it is definitional in the census predicate) such that every vertex of \(H\) lies on a simple \(a\)–\(b\) path (**vertex-taut**, `E018/mod4` semantics). Then for **every** cycle \(C\) of \(H\) and every prescribed edge \(pq\in E(C)\) there exist two **distinct** simple \(a\)–\(b\) paths \(P,Q\) with \(E(P)\,\triangle\,E(Q)=E(C)\) and \(pq\in E(P)\cup E(Q)\) — and they can be taken in **trunk-identical arc form**: there are distinct \(u,v\in V(C)\), an \(a\)–\(u\) path \(T_a\) and a \(v\)–\(b\) path \(T_b\) with \(V(T_a)\cap V(C)=\{u\}\), \(V(T_b)\cap V(C)=\{v\}\), \(V(T_a)\cap V(T_b)=\emptyset\), such that \(P=T_a\,A_1\,T_b\), \(Q=T_a\,A_2\,T_b\) for the two \(u\)–\(v\) arcs \(A_1,A_2\) of \(C\) (\(V(A_1)\cap V(A_2)=\{u,v\}\)); hence \(\ell(P)+\ell(Q)-2s=\ell(C)\) with \(s=\lvert E(P)\cap E(Q)\rvert=\lvert E(T_a)\rvert+\lvert E(T_b)\rvert\). En route, **Lemma A** (`A024` T4): in a vertex-taut pair, every edge lying on a cycle lies on a simple \(a\)–\(b\) path. Proof mechanism (`A024` T1–T5): tautness forces the block chain (block–cut tree a path, terminals non-cut in the end blocks; standalone chain-splice lemma), the Menger fan corollary + subdivision put a through-path through any prescribed cycle edge, and the **trimming construction** (keep only the prefix to the first \(V(C)\)-hit and the suffix from the last, complete through both arcs) yields the pair — the `A023` sub-obligations are resolved (essentiality proved; weaving control unnecessary, the woven middle is discarded). No connectivity, degree, girth, \(C_4\)/\(C_8\), or power hypothesis anywhere; connectivity is derived from tautness. Discharges `A023` T5 from labelled conjecture to theorem. | proved | `A024` T1–T5; **audited `R002`: PASS at lemma level, 0 critical / 0 major, 2 minor + 6 notes, all repaired in place** (delegated-subagent, fresh context; reviewer re-ran all `E023` commands independently and re-verified the claim set with a fully independent implementation through order 6 + the ten named objects); per-instance mechanical verification `E023`/`C044` | Menger \(k=2\)/Whitney + block facts B1–B3, precise statements in `references/textbook-classics-2026-07-25.md`; `E013`/`E021` census semantics; `D001` |
| L050 | tautness biconditional | **For connected \(H\) with \(\delta(H)\ge2\) and any \(a\ne b\): \((H,a,b)\) is interference-complete iff it is vertex-taut** (`A024` T6(b)). Sufficiency is `L049` (no side conditions). Necessity: in this class a non-taut pair always has an off-path vertex **on a cycle** (leaf blocks of the block–cut tree are 2-connected when \(\delta\ge2\), so they carry cycles; the T6(b) case analysis — \(\ge3\) leaves, two leaves with terminals split or together — consumes the standalone chain-splice lemma per the `R002` F1 rescoping), and such a cycle is non-decomposable by `L048`(i). Companion (`A024` T6(a)): **every 2-connected graph is interference-complete for every vertex pair** (2-connected pairs are taut — fan corollary; the `A022` classical remark now consumed). Upgrades `C042`'s empirical biconditional to a theorem on its class (the profile class is connected with \(\delta\ge2\)); predicts `C044`'s 113/113 non-taut failures at order 13; subsumes the pinched-world 100% census (`C031`/`C032`/`C035`) and the frontier dissection verdict (`C041`, 553/553) as instances. | proved | `A024` T6(a)/(b), audited under `R002` (the one substantive finding, F1, was the rescoping this row's proof now cites) | `L049`; `L048`(i) for the failure direction; block facts as in `L049` |
| C044 | computational observation | **E023 (S023): the last two pre-registered T5 kill rungs spent and survived; the biconditional exact at order 13; the `A024` proof mechanically verified per instance.** Anchors first: `E021`'s 45-check suite re-passed under PyPy 7.3.23 **and** CPython 3.14.2; the new slice driver calibrated against `E021/tautgeneral.json` (exact match: 19,476 / 12,313 / 723,926 / 0). (a) **`smallworld 13`** (exhaustive in-class; class 10,966 = `A021`'s count): all 10,853 vertex-taut members pass on all **1,614,300** cycles, zero failures; **all 113 non-taut members fail** — the dichotomy exact at 13. (b) **Order \(\le8\) exhaustive**: slices `8:12` + `13:28` cover all 11,094 connected order-8 graphs with \(\ge8\) edges (the 23 trees are cycle-free, vacuous) — 218,095 taut pairs, **36,797,657 cycles, zero failures** — so with the 4–7 base, T5's conclusion is verified on every graph of order \(\le8\), every vertex-taut pair, every cycle. (c) Cyclomatic-bounded slices: order 9 \(\le6\) (355,162 taut pairs; 9,247,364 cycles), order 10 \(\le4\) (120,252; 1,193,874), order 11 \(\le3\) (39,360; 217,272) — zero failures. (d) **`constructive`** — every step of `A024`'s construction asserted per instance (Lemma-A witness through the prescribed edge; first/last-hit trimming invariants; simplicity of both hybrids; \(\triangle=C\) exactly; membership of both hybrids in the census path list): orders 4–7 general (**3,727,132** (cycle, edge) instances), order-8/9 sparse slices (2,008,186 + 11,577,122), and the ten named objects — Petersen\(-e\) (29 cycles), the order-14 exemplar (64), and **all eight profile objects** (411 + 4,250 = 4,661 cycles, the recorded S022 totals reproduced object by object; 66,038 edge instances) — **all pass**. Cross-interpreter: `tautcal` and `constructive named` re-run in full under CPython 3.14.2, outputs identical. `R002` additionally re-ran every recorded command with writes redirected outside the repository (all aggregates reproduced) and re-verified T5/Lemma A/the biconditional with an independent implementation (own parser/enumerators, no shared code) on all labelled graphs through order 6 and the ten named objects — zero failures. Scope: exhaustive for the delimited classes; **verification of `L049`'s proof, not the proof** (that is `A024`/`R002`). | tested | `E023` `rungs.py` (production PyPy 7.3.23; anchors + cross-checks CPython 3.14.2; primitives imported from `E021`/`E018`/`E013`; data in `E023/data/`) | `E021`/`E018`/`E013` primitives and geng lineage; `A021` class count at 13; `L049`/`L050` for what the verification targets |
| L051 | trunk bound | **The trunk bound (A025 T2; S024).** Let \((H,a,b)\) be any two-terminal pair, \(n=\lvert V(H)\rvert\), and let a cycle \(C\) of length \(L\) be realized by a trunk-split pair in `L049`'s arc form with shape \((x,y,s\,\vert\,a_1,a_2\,\vert\,t_a,t_b)\). Then \(s=t_a+t_b\le n-L\), hence \(x+y=L+2s\le 2n-L\) and \(\max(x,y)\le s+L-1\le n-1\). *Proof:* the \(t_a\) non-\(u\) vertices of \(T_a\) and the \(t_b\) non-\(v\) vertices of \(T_b\) are pairwise distinct and all lie off \(V(C)\) (the arc-form disjointness conditions), so \(t_a+t_b\le n-\lvert V(C)\rvert=n-L\); the rest is `L049`'s length identity and \(a_i\ge1\). Combined with `L048`(iii) (every spectrum element of the case-(5b) residual object is a trunk-split pair value), this confines the object's entire collision system by order: a \(C_{16}\) realization in the window has \(s\le n-16\), a \(C_{32}\) realization at order \(\le33\) has \(s\le1\). **Tightness (`E025`):** all ten named objects realize \(s=n-L\) exactly (max \(s\) = 2/6/3/4 at orders 10/14/19/20). | proved | `A025` T2 (one-paragraph hand proof from `L049`'s arc form; tightness computed in `E025`) | `L049` (arc form and length identity); `L048`(iii) for the reading |
| C045 | computational observation | **E025 (S024): the (F) opening probe — complete trunk-split power-collision realization tables of the ten named objects; verdict: no membership-patterned regularity (pre-registered branch (b)).** Anchors first: `E021`'s 45-check suite re-passed through the import (PyPy 7.3.23 **and** CPython 3.14.2) plus 14 new-code checks both interpreters, including a **weave control** the trunk-split classifier must reject (`5+7-2*3` excluded, `5,5,2` kept) and exact reproduction of the recorded `E013` Petersen\(-e\) census and the recorded `E021` family-1 per-blocker combos for the order-14 exemplar. The tables: for each of the ten objects (the eight profile members, g6 identities re-verified field-by-field against `E022` records; Petersen\(-e\) from `E013` core 3; the order-14 exemplar), **every** witnessing pair of **every** power cycle (\(C_8/C_{16}\)) with its \((x,y,s)\), and the complete trunk-split sub-table with full shapes \((x,y,s\,\vert\,a_1,a_2\,\vert\,t_a,t_b)\), every `L049` arc-form invariant asserted per pair: **604 power cycles, 61,901 witnessing pairs, 1,971 trunk-split realizations**; every power cycle has \(\ge1\) trunk-split realization (the pre-registered soundness alarms against `L049` did **not** fire); CPython full-payload cross-check identical. **Verdict:** none of the nine pre-registered membership patterns is universal (`has_P` fails on 270/594 profile \(C_{16}\)s, `has_PP` on 556, `has_forced_pair` on 400), and **30 cycles across six profile objects are membership-blind outright** (entire trunk-split sets \(\{(5,13,1)\}\), \(\{(9,11,2),(10,10,2)\}\), or \(\{(5,11,0)\}\)); the calibration pair's 100% `has_PP` structure (every calibration \(C_8\) realized by two power-length paths) collapses to 1–8/37–112 at the frontier — a small-order artifact. **Structural findings:** the trunk bound `L051` is tight on all ten objects; pooled \(C_{16}\) \(s\)-distribution \(\{0{:}111,1{:}403,2{:}763,3{:}482,4{:}182\}\); all eight arc splits occur (mode (4,12): 510; equal split (8,8): 152/1,941); trunk-split skeleton thin (2.6–4.0 per cycle vs 76–138 witnessing pairs on profile objects); every "both"-stratum \(C_{16}\) has \((u,v)=(a,b)\) forced (111/111). **Frontier saturation:** all eight profile objects have spectrum exactly \([3,n]\setminus\{4,8\}\) and \(S\supseteq[6,n-1]\) (seven gap-free; N20p14-A \(=\{3\}\cup[6,19]\)) — in particular \(6,14\in S\) for all eight (the recorded double blocking); both calibration objects gap \(S\) exactly at 6 (the only known \(\mathbb P-2\) dodges, orders 10 and 14). Scope: exhaustive for the ten named graphs; proves nothing at other orders and nothing about power-free graphs or 0.1. | tested | `E025` `tables.py` (production PyPy 7.3.23; anchors + full-payload cross-check CPython 3.14.2; primitives imported from `E021`/`E018`/`E013`; data in `E025/data/`) | `E013`/`E021`/`E022` records and primitives; `L049` (the arc form the classifier tests); `A021` calibration discipline; `L051` for the bound the data saturates |
| C046 | computational observation | **E026 (S025): the S-gap census at the window bottom — (F-S) survives its first kill test, and the tautness-only saturation mechanism is refuted.** Over every unordered pair of degree-2 vertices of every \(\{C_4,C_8\}\)-free class member on disk — orders 10–19 complete (14/0/94/10/778/168/7,615/2,580/108,447/74,589 members), order 20 the per-part-complete 11-of-16-part sample (**572,519** graphs; the `S022` "572,530 saved" figure counted the 11 class-file headers — corrected here against the `scan_n20` tallies, which also sum to 2,569,481 over all 16 parts) plus the four recorded part-14 profile members as a separate stratum: **18,754,354 pairs over 767,004 members**. Definitions: a pair is *gapped* iff \(S\cap\{6,14\}=\emptyset\) (at \(n\le14\) the 14-half is trivial); a *full \(\mathbb P{-}2\) dodge* additionally has \(2\notin S\); tautness is the `E018` essential-mask semantics. Anchors: `E021`'s 45-check suite + 57 new checks under CPython 3.14.2 **and** PyPy 7.3.23 — the calibration pair (Petersen\(-e\), the order-14 exemplar) must be **found** gapped+taut, the eight profile objects saturated field-for-field against the `E022` records, three independently written enumerators agreeing on 20,554 pairs. **(a) The (F-S) verdict (pre-registered outcome (i)): zero kill candidates.** No exactly-two-degree-2 member of any censused class carries a gapped pair; the hypothesis class of (F-S) has exactly eight known realizations (1 at order 19, 3+4 at 20) and every one has \(6,14\in S\). **(b) The mechanism verdict: tautness does NOT force the poisons.** 9,061 vertex-taut gapped pairs exist in the same classes (8/8/371/24 at orders 12/14/16/17; **2,727/167/5,756** at 18/19/20 — the dodge rate stays at 0.01–0.2% of pairs with no monotone decay), 6,934 of them full \(\mathbb P{-}2\) dodges; so tautness + \(\{C_4,C_8\}\)-freeness + window order leave the dodge abundant, and any interpolation lemma proving (F-S) must consume the exactly-two profile (min degree \(\ge3\) off the terminals). **(c) The gradient:** the minimum degree-2 count on a member carrying a taut gapped pair is 5/6/**4** at orders 18/19/20 (order-20 histogram \(4{:}6,5{:}84,\dots,11{:}8\)); no taut gapped pair sits on a \(\le3\)-degree-2 member at any order (thin strata, hundreds of members). **(d) The (F-T) wider-class reading:** 5,419 of the taut gapped rows sit on **power-free** (\(C_{16}\)-free) members, most 2-connected — at orders 18–20, every hypothesis of the case-(5b) residual object *except* the exactly-two profile is realized simultaneously with the full dodge; the double blocking of the eight profile objects is a phenomenon of the profile stratum, not of the class. Verification: every gapped pair and every profile member recomputed in full by the `E018` enumerator with the 6/14 verdicts asserted (540,135 agreements per run) + a deterministic stride sample (3,867 more, 2,642 of them taut); per-line degree/edge integrity on every member; the `power_free` flag re-verified on every 500th member (1,510) and on every gapped row; two full production runs with **identical tallies** (the second re-run after trimming storage to taut-only rows); zero bipartite members in all censused classes. Scope: exhaustive for the censused sets only; proves nothing at orders 21–35, nothing about the five uncensused order-20 parts, and nothing about (F-S) beyond its kill test — the eight-object hypothesis class remains the only in-window evidence. | tested | `E026` `census.py` (production PyPy 7.3.23, 3 workers nice-15 beside the running `E024`; anchors both interpreters; primitives imported from `E019`/`E021`/`E018`/`E013`; data in `E026/data/`, taut rows + analysis stored, `run1_tallies.json` preserving the untrimmed run's tallies) | `E019`/`E022` class files and generation-layer caveats; `E018` `paths_with_essential` semantics; `A025` T4/T5 (the target and the pre-registration); `A021` calibration discipline (the census must and does find the calibration dodges off-window) |

| C047 | computational observation | **E027 (S026): the near-miss corpus dissection — the dodge economy has three rigid shapes, the mechanism is subdivision, and the chord-exchange calculus reproduces the profile saturation.** Over the 9,061 stored taut gapped pairs of `C046` (per-row fields re-validated; \(\min S=d(a,b)\) asserted on every row; every 250th row fully re-enumerated; the `L035` parity law asserted on all rows and 7,781 control taut pairs, zero violations) against the eight profile objects and a 556-member stride control with 14,098 full enumerations. **(a) Taxonomy:** every dodge is one of — **A** (short + 6-hole: \(\max S\le13\), 7,380 rows; the minimal full dodges have \(\max S=13\) *exactly* at every order 16–20), **A′** (distance: \(\min S=7\) exactly, never \(\ge8\); 1,226 rows), **B** (long-range mod-4: \(\max S\ge15\), holes exactly at \(\{6,10,14\}\), 455 rows, 395 with even part exactly \(\{4,8,12,16\}\subseteq4\mathbb Z\) — the `L034` channel-(iii) pattern realized in-window on non-bipartite members; needs \(\ge7\) degree-2 vertices everywhere). The 14-dodge is never by distance; the 6-dodge never by shortness. **(b) Pair-locality:** the order-20 frontier members (ndeg2 = 4: one adjacent-terminals full dodge \(S=\{1\}\cup[8,13]\); six gapped rows) each carry **five fully saturated sibling pairs** (\(S\) full intervals to 19) with full spectra and triangles — no member-level hypothesis separates dodgers from the profile; the profile hypothesis is load-bearing by **two subdivision vertices** at order 20. **(c) Subdivision frame:** corridor weights pool to {2: 47,662, 3: 2,006, 4: 10}; smoothing every dodge row, **99.1% (8,978/9,061) reduce to a graph whose simple part carries a \(C_4\) or \(C_8\)** (all 83 exceptions parallel-corridor theta-hubs, all shape A; shape B 100% class-violating) — near-misses are subdivisions of class-violating graphs; the profile is the subdivision-free stratum. **(d) Killed candidates:** member-level lemmas (b) and odd-cycle-supply discrimination (100% of dodge members have triangles). **(e) Control base rates:** ordinary taut pairs violate the upper-interval property at 12–17% with gap positions concentrated at 7–9 and nearly vanishing by 13–14 (11 of 7,781 at \(\ge13\)); Hamiltonian pairs 22–29%; the profile-8's joint gap-free+Hamiltonian property is far outside these rates. **(f) The exchange validation:** on one Hamiltonian path per profile object, the first-order disjoint-chord calculus (savings \(\sum(\sigma_k-1)\); every generated length a real path, asserted \(\in S\)) generates the entire top of \(S\) down to 10 — all misses in \([4,9]\), **14 generated on all eight**; the **span law** (no chord of span 3 or 7 in a \(\{C_4,C_8\}\)-free graph — chord + arc = \(C_4\)/\(C_8\); proved, `A026` T5) holds by assertion on all eight objects and all 36 Hamiltonian dodge rows; the dodgers carry exactly two span-2 chords with remaining spans \(\equiv1,2\pmod4\), so their disjoint packings never realize savings \(\equiv3\pmod4\) — exactly the class hitting \(\{14,10,6\}\) from \(M=17\) — and the calculus fills no interval on any dodger (0/36). Scope: the corpus is a selected set; order 20 samples 11/16 parts; the exchange test is one deterministic path per object (a lower-bound demonstration); nothing at orders 21+; no bearing on the truth of (F-S) — the frontier trend is compatible with failure at higher orders, which the `E024` harvest + census extension will test. | tested | `E027` `dissect.py` (anchors 35 checks under CPython 3.14.2 and PyPy 7.3.23; production PyPy nice-15 single process beside the running `E024`; imports the `E026`→`E021`/`E019`/`E018` chain read-only; data in `E027/data/`) | `C046` (the corpus and its verification layer); `E019`/`E022` class files and caveats; `L035` (the parity soundness alarm); `A026` T1–T7 (the reading); `A021` calibration discipline |
| L052 | reduction | **The chord-minimal descent and the monotone reroute (S027, `A027` T1/T2/T6).** Let \(H\) be a simple graph of order \(n\), let \(a,b\in V(H)\) have \(d(a)=d(b)=2\) and every other degree \(\ge3\), and suppose \(H\) has a **Hamiltonian \(a\)–\(b\) path** \(P=v_0v_1\cdots v_M\) (\(a=v_0\), \(b=v_M\), \(M=n-1\)). Identify each chord \(v_iv_j\in E(H)\setminus E(P)\) with the interval \([i,j]\), \(j-i\ge2\), and write \(\mathcal C\) for the chord set. **(i) Coverage.** \(\mathcal C\) covers \(\{0,\dots,M\}\), with \(\deg_{\mathcal C}(0)=\deg_{\mathcal C}(M)=1\). **(ii) Descent.** For every inclusion-minimal subcover \(\mathcal C'\subseteq\mathcal C\), the spanning subgraph \(H'=P+\mathcal C'\) again has exactly the two degree-2 vertices \(a,b\) with all other degrees \(\ge3\), still has \(P\) as a Hamiltonian \(a\)–\(b\) path, and satisfies \(\mathrm{Spec}(H')\subseteq\mathrm{Spec}(H)\) and \(S(H',a,b)\subseteq S(H,a,b)\); moreover every chord of \(\mathcal C'\) has an endpoint of chord-degree 1. Hence any hypothesis of the form "avoid a prescribed set of cycle lengths **and** avoid a prescribed set of through-lengths" is inherited by \(H'\), so a search for such a pair may be restricted to chord-minimal systems without loss. **(iii) Monotone reroute.** For every family \((i_1,j_1),\dots,(i_t,j_t)\in\mathcal C\) with \(i_1<j_1\le i_2<j_2\le\cdots\le i_t<j_t\), \(H\) has an \(a\)–\(b\) path of length \(M-\sum_k(j_k-i_k-1)\); the set \(\Sigma\) of these sums is computed by \(R_0=\{0\}\), \(R_p=R_{p-1}\cup\bigcup_{(i,p)\in\mathcal C}(R_i+(p-i-1))\), \(\Sigma=R_M\), and \(R_p\subseteq\Sigma\) for every \(p\) — so \(\Sigma\cap\{M-\ell\}\ne\emptyset\) certifies \(\ell\in S\) and the certificate is monotone in \(\mathcal C\) and detectable on prefixes. **(iv) Chord-pair geometry (correction).** Two **interior-disjoint** chords close no cycle with \(P\) beyond their own; only shared-endpoint, nested and crossing pairs do, with cycle lengths \((k-j)+2\), \((k-i)+2\), \((k-i)+(j-l)+2\) and \((k-i)+(l-j)+2\) respectively. (`A026` T5's span law — no chord of span 3 or 7 in a \(\{C_4,C_8\}\)-free graph — is the one-chord case of the same computation and is unaffected.) | proved | `A027` T1/T2/T6; mechanically exercised throughout `E028` (80,131 anchor checks under CPython 3.14.2 and PyPy 7.3.23, including exact set-equality of the descent-based enumerator against an independently written brute-force enumeration, assertion that every DP-generated length is a genuine \(a\)–\(b\) path length, and — added after the `R003` audit — verification of the descent's minimality propagation and of the savings DP on the **nonempty** order-19/20 cover sets); the reduction itself was re-derived and passed by the delegated reviewer `R003` | `D001`–`D004`; independent of the (F) program — (ii) uses only heredity of cycle-length freedom and of through-sets under spanning subgraphs |
| C048 | computational observation | **E028 (S027; order-30 rung added and independently reproduced in S028): the (F) ladder on the Hamiltonian stratum — no dodging profile object exists at orders 16–30.** Decided by exhaustive enumeration after the proved reduction `L052`: at each order \(n=M+1\), enumerate every chord-minimal cover of the \(n\) path positions whose graph \(P+\mathcal C'\) is \(\{C_4,C_8,C_{16}\}\)-free and whose monotone-reroute savings set misses \(\{M-2,M-6,M-14,M-30\}\); by `L052`(ii)/(iii) every counterexample yields such a cover, so **emptiness at an order proves, at that order, that every \(\{C_4,C_8\}\)-free graph with exactly two degree-2 vertices and a Hamiltonian \(a\)–\(b\) path satisfies \(S\cap\{2,6,14,30\}\ne\emptyset\) or \(16\in\mathrm{Spec}\)** — i.e. (F) on that stratum, which is exactly what case (5b) needs there. **Run B (the (F) hypothesis, \(\{C_4,C_8,C_{16}\}\)):** survivors 0 at every order **16–30** — the last completed rung, and the last one that will be run (the ladder was stopped there in `S028` per the `S027` post-close decision); nodes 8,924 … 49,882,612, node growth \(\approx\times1.9\) per order (26,421,019 → 49,882,612 = \(\times1.888\) at the top), wall clock 0.5 s … 4,195.9 s (\(\times1.907\) at the top). The order-30 rung records `capped: false`, so it is exhaustive and not truncated by a node cap. **Two scope facts the audit `R003` established and this row records at full strength.** (i) *Open-ended, not a window closure* (F3): wall-clock growth is 1.8–2.4 per rung and rising, so orders 31–35 are days of single-core computation; nothing is claimed above the last completed rung. **`S028` closed this out**: the ladder was stopped after order 30 rather than continued, because by (ii) below it had stopped exercising (F)'s mechanism, so the remaining rungs would buy a floor and no understanding. The search *is* parallelisable if it is ever wanted again — its DFS branches at position 0 over the single chord \((0,q)\), giving \(\approx M\) independent root subtrees — so the stop is a decision about mathematical value, not a computational limit. (ii) *Above order 26 the verdict is class-emptiness, not poison forcing* (F4): the poison prune's branch kills fall 61, 33, 45, 17, 2, 1, **0, 0, 0, 0** at \(M=20\ldots29\), so from \(M=26\) the tree with the poison test is identical to the tree without it and what is proved at orders 27–30 is the **stronger**, poison-free statement — *no \(\{C_4,C_8,C_{16}\}\)-free graph of that order has exactly two degree-2 vertices, all other degrees \(\ge3\), and a Hamiltonian \(a\)–\(b\) path, whatever its through-set*. That implies (F) there a fortiori but exercises none of (F)'s forcing mechanism. **Run A (\(\{C_4,C_8\}\) only, \(C_{16}\) deferred to the exact stage):** empty at orders 16–20, and at orders 21 and 22 it returns 10 and 43 chord-minimal survivors — the **first \(\mathcal G\)-profile objects exhibited at those orders** (3 and 16 isomorphism classes; all 2-connected, girth 3, 91–186 \(C_{16}\)s) — every one of which the exact stage kills **twice**: \(14\in S\) on all 53 and \(6\in S\) on 40, and a \(C_{16}\) on all 53. New structure at the frontier: 13 of the order-22 objects have \(S\) **not** a full interval (holes at \(\{6\}\) or \(\{2,3,4,6,7\}\), one with adjacent terminals \(\min S=1\)) — the first in-window exactly-two objects breaking the `A025` T3 saturation pattern, while still carrying 14 and a \(C_{16}\). Verification: anchors **80,131** checks under both interpreters with identical histograms — including the families **a6/a7 added in response to `R003` F1/F2**, which are the checks the verdict rests on: before them, none of the three loss-capable prunes (the poison DP, the reversal symmetry break, \(C_{16}\) detection) was exercised on a positive or nonempty instance, because the \(\{C_4,C_8\}\)-free chord-minimal class is empty below order 19 and the depth-15 \(C_{16}\) branch was only ever called on graphs of order \(\le12\); a6 now drives that branch to `True` on **539 positive** and 63 negative instances across \(M=15..34\) against three detectors, and a7 verifies reversal-closure and poison-prune selection on the nonempty order-19/20 cover sets; the enumerator set-equal to an independently written brute-force reference (different traversal, cycle detector, savings routine, no symmetry breaking, no minimality propagation) at every \(M\) where the reference is feasible, including the nonempty \(C_4\)-only controls 0/1/11/80/660 at \(M=6..10\); every survivor re-certified with the whole-graph detector `has_cycle_len`, with \(S\) recomputed by a **second independent** plain-DFS path enumerator and asserted equal to the `E018` enumerator's answer, and canonical forms via nauty `labelg`. **Independent cross-check of the recorded ladder:** with the poison prune off and \(\{C_4,C_8\}\) only, the chord-minimal cover counts by order are 0 at 12–18 and 6, 65 at 19, 20; at **isomorphism level** (nauty `labelg`) those give **1** and **7** classes, set-*equal* to the canonical forms of the 1 and 7 recorded profile objects — nothing unrecorded appears and nothing recorded is missed, reproducing `C027`/`C039`/`C043`'s emptiness and first rungs from an entirely different generation principle. Scope, explicit: the stratum is **pairs carrying a Hamiltonian \(a\)–\(b\) path** (all eighteen known in-window profile objects do, but the profile is not proved to force it — `A027` T5 is the named gap, and the order-21/22 objects found here are **no evidence** on it, being constructed on a Hamiltonian path); nothing is claimed at orders above the last completed rung; run A's storage of individual survivors is capped at 200 per order (the decision stage processes all of them). | tested | `E028` `search.py` (anchors both interpreters; production PyPy 7.3.23 `nice -n 15`, single process beside the running `E024`; data in `E028/data/`). **Provenance repair, `S028`:** the production process began at 14:03 while `search.py` was edited at 15:19 (the `R003` a6/a7 repairs, made during the run) and only one revision was ever committed, so the rungs came from an instrument that could not be diffed against the recorded one. **Closed, by two from-scratch re-runs on the committed instrument.** (a) The anchor suite reproduced `anchors_search_pypy.json` **byte-identically** except its own timing field (80,131 checks, identical histograms). (b) The order-30 rung itself was re-run end to end (`search_29_29_c16.json`) and agrees with the production record on **every field**: survivors 0, nodes 49,882,612, `capped` false, targets \(\{2,6,14\}\), 0 gap-free survivors, and all seven prune counters identical to the digit (cover 44,065,721; cycle 1,334,986,730; minimal 2,890,076; poison 0; reserve 233,544,446; symmetry 22,837,229; count 0). Only the wall clock differs (2,907.2 s against 4,195.9 s — the laptop was unloaded for the re-run). A search whose traversal had been perturbed could not match seven independent counters in the billions, so the `R003` repairs are confirmed confined to the anchor suite and the rung rests on the instrument the repository actually holds. **Delegated adversarial audit `R003` PASSED at lemma-and-instrument level** (0 critical / 4 major / 4 minor / 3 notes; the reviewer reproduced run A \(M=15..21\) and run B \(M=15..23\) node counts to the last digit from the shipped instrument run outside the repository, and its own from-scratch enumerator independently returned empty for the run-B configuration at orders 19–25; all four major findings repaired in place before this row was recorded) | `L052` (the reduction the exhaustiveness rests on); `L048`(iii) for the poison set \(\{2,6,14,30\}\) and the forbidden spectrum \(\{4,8,16,32\}\); `L042`/`L046` for the residual object's window; `C043`/`C046` used only as cross-checks, never as premises; `A021`/`A026` calibration discipline; `R003` (the audit whose F1/F2 repairs are the anchor families this row cites, and whose F3/F4 fix its scope) |
| C049 | computational observation | **E024 (S029): the order-21 \(\mathcal G\) rung is EMPTY, and the exactly-two profile forces a Hamiltonian through-path 19 for 19.** Over the **complete** order-21 \(\{C_4,C_8\}\)-free class — **2,951,168** graphs, 652,935 generator cpu-seconds (181.4 core-hours), max 33 edges, generated as a 144-part `res/mod` split — the degree-profile class (exactly two degree-2 vertices, all others \(\ge3\)) has **19** members, **all 19 \(C_{16}\)-blocked**, and **zero power-free survivors**. So \(\mathcal G\) has **no order-21 member**, extending `C043`'s emptiness at 18–20: every \(\mathcal G\)-member now has order \(\ge22\) and every tight 1-atom order \(\ge23\). Class shape: degree-2 histogram 19 / 1,071 / 15,792 / 106,188 / 378,371 / 765,621 / 883,192 / 552,238 / 188,130 / 48,910 / 11,636 at counts 2…12 (mode 8); **308,098 power-free members, minimum degree-2 count 5** — none with 2, 3 or 4. **THE DECISIVE MEASUREMENT (the reason this rung was promoted, `S027` post-close review): \(\max S = 20 = n-1\) on ALL NINETEEN.** Every order-21 profile member carries a Hamiltonian \(a\)–\(b\) path. Cumulatively **27 of 27** in-window profile objects are Hamiltonian (8 at orders 19–20 via `C042`(e), 19 here) against `C047`(e)'s measured control of **22–29%** for ordinary taut degree-2 pairs — the pre-registered pivot did **not** fire, and `C048`'s ladder is therefore supported (not proved) to address case (5b) as a whole rather than a proper sub-stratum. **Scope, explicit: this is evidence, not a theorem.** 27 examples do not force Hamiltonicity, and `A027` T5 (the non-Hamiltonian stratum) is still cleared at *no* order. **Refinement:** 18 of the 19 carry both 6 and 14 in \(S\); **one does not** — `T????A?O@?B?D??oQ?_KAoG?AoB?_@OO?LA?`, \(d(a,b)=4\), \(\max S=20\), \(S\cap\{2,6,14,30\}=\{14\}\) — so the \(S\supseteq[6,n-1]\) saturation of `A025` T3 is **not universal** at order 21, though the double blocking (14 in \(S\) *and* a \(C_{16}\)) survives; this is the break `S027` first saw at order 22, now one order lower. | tested | `E024` `cloud.py` driving `E019/scan.py` through `E022/ladder.py`; produced on Railway behind the **passed** linux/amd64 anchor re-gate `O012` (amd64 pin `e8e839f6…4bb0`, recorded beside the untouched arm64 pin; all six containers confirmed at run time to carry it), instrument preserved per `O014`. Retrieval verified before use: **144/144 parts present exactly once**, every file matching the sha256 its own container recorded, each part self-describing as the order and part it is filed under, and the per-part identity `profile == c16_blocked + survivors` holding throughout. **Three parts (115, 99, 142) exceeded `scan.py`'s `SAVE_LIMIT` and have no class file** (205,928 / 351,893 / 365,671 graphs); each was checked individually and contains **zero** profile members, so all 19 were recovered and the extraction is complete — recorded because it was luck, not design. **The harvest and the measurement were run with the LOCAL, already-anchored arm64 instrument**, not in the cloud; the measurement re-derives \(\{C_4,C_8\}\)-freeness, the degree profile and the edge count per row and asserts the recorded invariant \(\min S=d(a,b)\). | `C043` (the 18–20 rungs this extends); `C048` (the ladder whose stratum this measures); `C047`(e) for the 22–29% control; `C042`(e) for the eight earlier profile objects; `L039`/`L041` for what \(\mathcal G\)-emptiness lifts; `O012`/`O014` for the instrument's provenance |

| L053 | delimitation | **No class-level Hamiltonian forcing (S030, `A028` T1).** For every \(N\) there is a graph \(H\) of order \(\ge N\) with two distinguished vertices \(a,b\) such that: \(d_H(a)=d_H(b)=2\) and every other degree is 3 (the **exactly-two profile**); \(H\) is **2-connected**; \(H\) has **girth \(\ge17\)**, hence contains no \(C_4\), \(C_8\) or \(C_{16}\); and \(H\) has **no Hamiltonian \(a\)–\(b\) path**, i.e. \(\max S(H,a,b)<\lvert V(H)\rvert-1\). Construction: take a cubic, 3-edge-connected, non-Hamiltonian graph \(F\) of girth \(\ge17\) (external existence, `X002`; independently `X003`), which is 3-connected because \(\kappa=\kappa'\) for cubic graphs, and delete one edge \(ab\); a Hamiltonian \(a\)–\(b\) path of \(F-ab\) plus \(ab\) would be a Hamiltonian cycle of \(F\). **Consequence.** The hypotheses available at class level — exactly-two profile, \(\{C_4,C_8,C_{16}\}-freeness, 2-connectivity, and hence (`L050`) vertex-tautness — **do not force a Hamiltonian through-path**. `C049`'s 27/27 measurement therefore cannot be promoted to a lemma at that strength: any Hamiltonian-forcing argument must additionally consume power-freeness, the poison condition \(S\cap(\mathbb P-2)=\emptyset\), minimum-order minimality, or an explicit order bound. \(F-ab\) is recorded as **calibration object #3**, beside Petersen\(-e\) (`C037`) and the order-14 object: any proposed proof of Hamiltonian forcing must fail on it. **Second consequence.** The ambient class of `C048`'s ladder is nonempty at arbitrarily large order, so the class-emptiness verdict that `R003` F4 showed the ladder to be delivering above order 26 is a small-order phenomenon and must fail at some order, after which only poison forcing — the mechanism that stopped firing at \(M=26\) — can carry a rung. **Scope, explicit: asymptotic.** A cubic graph of girth 17 has order in the thousands; this row exhibits **no** non-Hamiltonian profile member in the window \([18,35]\) and is fully consistent with 27/27 at orders 19–21. It closes a route, not a pattern. Not claimed: that the *Hamiltonian* sub-class is nonempty at large order (that needs Hamiltonian cubic graphs of girth \(\ge17\), expected from the standard random-cubic model but not imported). | proved | `A028` T1 (hand proof, four lines from the imports); external existence `X002` (Haythorpe 2014, arXiv:1902.10344) and independently `X003` (Kochol 1996 snarks of arbitrarily large girth, plus the three-line internal proof that a Hamiltonian cubic graph is 3-edge-colourable), both audited in `references/large-girth-non-hamiltonian-cubic-2026-07-26.md` | `D001`–`D004`; `L050` for vertex-tautness of the calibration object; refutes at class strength the alternative recorded in `S029`'s ending checkpoint; scopes `C048`/`C049` |
| L054 | interface theorem | **The bipartite exclusion dichotomy (S030, `A028` T2).** Let \(X\) be a finite **power-free** graph and \(Y\subseteq X\) a connected subgraph with at most two vertices of degree \(\le2\) *in \(Y\)*, those degrees being \(\ge1\) and summing to \(\ge3\) if there are two. If \(Y\) is **bipartite**, then statement 0.1 is **false**, and `L035` T3 constructs an explicit counterexample from \(Y\). (Proof: power-freeness is hereditary, so \(Y\) satisfies `L035` T3's hypotheses verbatim.) Three usable forms, all recorded because they differ in logical force: **(a) dichotomy** — either 0.1 is false with a witness in hand, or no power-free graph contains such a \(Y\), so a construction producing one is a *disproof route*, not a dead end; **(b) outright exclusion in range** — if \(\lvert V(Y)\rvert\le24\) then `C034` excludes \(Y\) with no appeal to 0.1 at all; **(c) inside `G015`** — that setting already assumes a counterexample exists, so (a) alone is **not** a contradiction there, and only (b), or an order comparison against \(n_0\) through `L040`, converts the dichotomy into an exclusion. Recorded explicitly so that no later session reads the bipartite transfer as unconditional. This is the matching of the power-freeness hypothesis that `A027` T5 left provisional. | proved | `A028` T2 (one line from `L035` T3, with the three logical forms separated) | `L035` T3 (the assembly criterion, at recorded strength); `C034` for form (b)'s range; `L040` for form (c); heredity of power-freeness under subgraphs |
| L055 | reduction | **The positive-savings theorem for off-path components (S030, `A028` T3–T7).** Let \((H,a,b)\) be a **power-free, 2-connected** exactly-two-profile pair, \(P=v_0\cdots v_M\) a **longest** \(a\)–\(b\) path, and \(K\) a component of \(H-V(P)\). A **bridge** \((i,j,w)\) is a \(v_i\)–\(v_j\) path of length \(w\) with interior in \(K\); its **savings** is \(\sigma=(j-i)-w\). **(i) Coverage** (`A027` T1 verbatim): every position \(0,\dots,M\) is a chord endpoint or an attachment of some component, positions \(0\) and \(M\) exactly once. **(ii) Maximality bound:** every bridge has \(w\le j-i\), i.e. \(\sigma\ge0\). **(iii) Two attachments:** if \(K\) attaches at exactly \(v_i,v_j\) then \(G_K=H[K\cup\{v_i,v_j\}]\) is vertex-taut (2-connectivity plus the Menger fan) with all \(K\)-degrees \(\ge3\), so by `L035` T2 its through-set \(S_K\) is parity-constant iff \(G_K\) is bipartite; by `L054` the bipartite case makes 0.1 false. Hence \(S_K\) contains **both parities**, and since all its members are \(\le j-i\), savings of **both parities** are realised — in particular one with \(\sigma\ge1\). (The `L035` T3 side condition is checked in all three degree cases: \(d_i+d_j\ge3\) applies to \(G_K\); \(d_i=d_j=1\) passes to \(H[K]\), whose only vertices of degree \(\le2\) are the unique attachment neighbours \(u,u'\), of degree \(\ge2\) each, or the single vertex \(u=u'\) of degree \(\ge1\).) **(iv) Three or more attachments:** for attachments \(i<j<k\) there are \(u\in K\) and three internally disjoint legs to \(v_i,v_j,v_k\) of lengths \(\alpha,\beta,\gamma\ge1\) (minimal connected subgraph of \(K\) meeting three attachment neighbours is a tree with \(\le3\) leaves), and \(\sigma(i,k)=\sigma(i,j)+\sigma(j,k)+2\beta\ge2\beta\ge2\) — **maximality alone**, no power-freeness, no \(C_4\)-freeness. **(v) Conclusion:** **every** component admits a bridge with \(\sigma\ge1\) (\(\ge2\) with \(\ge3\) attachments), unless 0.1 is false with an explicit witness built from that component by `L054`; in the window the escape clause is closed outright when \(\lvert K\rvert\le22\) (`L054` form (b)). **(vi) Bridge span law:** \((j-i)+w\notin\mathbb P\) for every bridge, so consecutive attachments of a component are \(\ge3\) apart, and \(j-i=4\) forces \(\sigma\ge1\) (as does \(j-i=8\) under \(C_{16}\)-freeness). **What this settles:** the obstruction `A027` T5 named — "a bridge with \(w=j-i\) covers two positions and yields savings 0, so covering no longer forces savings" — does not arise; zero-savings components do not exist, and `L052`(iii)'s monotone-reroute machinery transports verbatim from chords to bridges. **What it does not settle:** the descent is not yet a decision procedure on this stratum — a component cannot be thinned edge by edge, so no minimal-cover notion with a bound on the number of components is available, and the savings are per component rather than per position (`A028` T8). No order is cleared on the non-Hamiltonian stratum by this row. | proved | `A028` T3–T7 (hand proofs; every boundary case of the `L035` T3 side condition carried explicitly; the Y-identity verified on the degenerate instance \(\alpha=\beta=\gamma=1\), where it is strictly stronger than \(C_4\)-freeness plus the span law) | `L035` T2 (parity structure) and `L054`; `L049`/`L050`; Menger's fan lemma as recorded in `references/textbook-classics-2026-07-25.md`; `L042`/`L046` supply the hypotheses for the case-(5b) residual object; `A027` T1/T2 for the transported coverage and reroute steps; discharges `A027` T5's provisional first purchase |
| C050 | computational observation | **E029 (S030): the no-gap conjecture (INT) survives its first kill test, and its constant is pinned from both sides.** `A028` T9 formulates **(INT)**: for a vertex-taut \(\{C_4,C_8\}\)-free exactly-two-profile pair, \(S(H,a,b)\supseteq[8,\max S]\). Two measurements over data **already on disk** — nothing generated. **(a) Profile objects: 24 checked, 0 violations.** The 19 order-21 members (`E024`), the 4 order-20 part-14 members (`E022`) and the order-19 member (`C043`), with \(S\) recomputed from the stored graph6 by exact path enumeration rather than read from the `C043`/`C049` summaries. Every hole occurring in any of them lies in \(\{4,5,6\}\) — **none at 7 or above**; all 24 are \(C_4\)-free and Hamiltonian, re-derived per row. Only two are gapped at all: the `C049` order-21 exception \([4,20]\setminus\{6\}\) (reproduced), and — **new** — an order-20 member with \(S=[3,19]\setminus\{4,5\}\), a second non-interval profile object one order below where `S027` first saw one, consistent with `A025` T3 (which asserts \(S\supseteq[6,n-1]\), not that \(S\) is an interval). **(b) The corpus violation frontier.** Over `E027`'s complete 9,061-row near-miss corpus, the minimum degree-2 count `ndeg2` at which each hole value occurs is 4 for every hole value \(\le7\), then **5** at hole 8, 7 at 9, 6 at 10, 8 at 11 and **7** at 14; 1,920 rows have a hole \(\ge8\); no hole above 14 occurs anywhere. **Interpretation.** (1) (INT) is not close to failing on the objects it is about — their holes stop at 6, two below the threshold. (2) **The constant 8 is the smallest the data permits**: every hole value \(\le7\) is realised already at the corpus's minimum degree-2 count of 4, so (INT) with any constant \(\le7\) is false on the near-miss frontier — a sharper calibration than `A026` T7's (L-B), which fixed no constant. (3) **The operative case has a five-vertex margin**: no corpus row opens a hole at **14** below `ndeg2 = 7`, so the weakened form **(INT-14)** — a vertex-taut \(\{C_4,C_8\}\)-free profile pair with \(\max S\ge14\) has \(14\in S\) — is better supported than full (INT) and, by `L048`(iii), is all (F-S) needs. (4) The profile hypothesis stays load-bearing, as `C046`(b)/`C047`(b) predicted: 1,920 refutations of (INT) sit on disk the moment the degree-2 count rises. **Scope:** (a) is exhaustive over the *stored* profile objects — three of the eight orders 19–20 objects are not stored as graph6 and are covered only by `A025` T3's recorded saturation; (b) is exhaustive over the recorded corpus, so an absent hole value means "not in the corpus", not "impossible". Neither can prove (INT); a violation on a profile object would have disproved it, and none occurs. | tested | `E029` `nogap.py` (PyPy 7.3.x and CPython 3.14.2, identical output; primitives imported verbatim from `E019/scan.py`). Six anchors, both interpreters, each reproducing a quantity recorded independently of this experiment: Petersen\(-e\)'s \(S=\{4,5,7,8\}\) and non-Hamiltonicity, built from an explicit edge list inside the instrument rather than read from disk (`C037`); the order-14 calibration object's single hole at 6 and its Hamiltonicity; the order-19 member's \(S=[5,18]\) exactly (`C043`); the order-21 rung's 19 members, all \(\max S=20\), exactly one with \(6\notin S\) and that one with \(\min S=4\) (`C049`); the corpus's 9,061 rows (`C047`); graph6 round-trip on every object | `A028` T9 (the formulation); `C047`/`C046` (the corpus and its provenance); `C043`/`C049` (the profile objects, used here as anchors, not as premises); `L048`(iii) for why 14 is the operative value |
## Dependency notes

- `L001`–`L004` are conditional structural consequences of the negation of
  `C001`; none proves `C001`.
- `L003` is independent of `E001`. The computation is retained as a
  reproducibility and implementation check, not as the proof of `L003`.
- `L005` and `L006` are independent of `E002`; the experiment located the
  structural cases, while `A002` and `A003` prove them.
- `L002` was proved internally before source inspection. Carr (2026) identifies
  its high-degree-independence conclusion as an earlier observation of
  Markström; no novelty is claimed here.
- `C004`–`C008` are imported at exactly the strength stated. They do not imply
  `C001`, and the external computations behind `C007` were not reproduced in
  this repository.
- `C009` is used only for finite cubic large-girth existence. `L007` derives
  the induced \(P_{13}\) and the recorded structural conclusions directly; it
  does not assert that the large-girth graph avoids \(C_{32},C_{64},\ldots\).
- `L007` refutes only the proposed local \(C_{16}\)-forcing route, not `C001`.
  `C010` concerns a much smaller exact family and is not support for `L007`.
- `L008` uses edge-maximality under safe edge addition, not the edge-minimality
  in `L002`. Its minimum-order addendum controls subgraphs which omit vertices;
  it does not import the spanning-subgraph part of `C004`.
- `L009` uses a canonical bipartite double cover when the graph from `C009` is
  non-bipartite. It is a stress test for `L008`, not a counterexample to
  `C001`.
- `L010`–`L012` are consequences of the saturation witnesses. `L013` uses
  2-connectivity and the near-minimum-degree block directly. `L014` is only a
  separation example for one-ear length equations; its internal path vertices
  have degree \(2\), so it is not a counterexample to `C001` or to `L011`.
- `L015` and `L016` delimit the single-witness information content of `L012`:
  their theta and double-theta families satisfy every constraint visible to
  one witness, yet they contain internal degree-\(2\) vertices, so none is a
  counterexample to `C001` or to `L011`. Together they retire every bounded
  single-witness forcing pattern and fire the recorded pivot condition for
  the saturation-only route. They say nothing against saturation as a
  reduction, and `L008`–`L013` remain available as constraints.
- `L017` is computer-assisted, unlike the hand-proved `L005`–`L006`: its
  proof combines the proved small-order collapse and coverage arguments in
  `A006` with the exhaustive search `C011`/`E004`. At orders \(11\)–\(13\)
  the full \(\{C_4,C_8\}\)-free class was empty, so `L017` does not depend
  on `L008`. The cleared orders are below reported prior computational
  bounds in the literature; no novelty is claimed, and those prior
  computations were not reproduced or relied on here.
- `C012` and `C013` are external computations imported at reported
  strength; neither was reproduced here in full. `C012`'s reduction to the
  restricted degree structure is the elementary minimal-counterexample
  argument (the independence conclusion of `L002` plus order-minimality),
  so its search class covers all minimum counterexamples. The
  widely-circulated "at least 17 vertices" strengthening of `C012` is an
  overread with no primary-source support and must not be cited
  (`references/source-audit-2026-07-23-S007.md`). Orders \(11\)–\(16\) of
  the range covered by `C012` are independently confirmed internally
  (`E004`, `E006`); orders \(\le 24\) of `C013`'s range by `E005`.
- `C017` is a strategic import: its graphs satisfy the conjecture (or
  concern \(q\ge3\)) and none is 2-connected, so it does not intersect the
  block reduction `L011`; its value is the spectrum-confinement mechanism
  and the demonstration that 1-connectivity is what makes bounded cycle
  spectra — and hence power dodging — possible at minimum degree \(3\).
- `C015` records witness coverage, not `L008` saturation: the Markström
  graph contains a \(C_{16}\), so it is not power-cycle-free and not an
  `L008` object; the observation is that the Mersenne-witness half of the
  saturation condition is realizable by an actual extremal cubic planar
  graph at order \(24\).
- `L018` is computer-assisted like `L017`, but with a weaker internality:
  `E004`'s generation layer was internal and hand-proved, while `E006`
  delegates generation to nauty's geng, an imported tool anchored against
  OEIS A002851/A006786 counts, cross-validated against `E004` at orders
  \(11\)–\(13\), and re-checked per graph for the degree and \(C_4\)
  conditions. The connectivity and collapse lemmas of `A007` are hand
  proofs. `L018` strictly strengthens `L017`'s bound; `L017` remains the
  strongest bound with an internal generation layer.
- `L019`–`L021` are unconditional lemmas about voltage lifts (tools for
  the falsification program of `A008`), not reductions of `C001`; none
  bears on the truth of `C001` directly. `L019`'s certificate direction
  is one-sided: absence of zero-voltage walks proves absence of the
  corresponding cycle lengths, but a zero-voltage walk does not prove a
  cycle exists. `L021` is abelianization-specific: over a non-abelian
  voltage group the commutator word's net voltage is a genuine group
  commutator, generally nontrivial, so `L021` says nothing there.
- `C020` is a statement about the L019 *certificate*, not about the
  lifts' actual cycle spectra: it proves the cyclic-lift sieve can never
  output a counterexample candidate from the six bases, and nothing
  more. `C021` is truth-level (validated detectors on the explicit
  graphs) but finite: orders \(12\)–\(30\) only, six bases only. Neither
  claim covers non-abelian groups, other bases, or unbounded orders; the
  general-base effective version of the `L021` obstruction is open as
  `G012`.
- `C019` uses the `C018` graphs verbatim and the `E004` detectors plus an
  exact canonical cycle counter; it is calibration data for route
  selection, with no bearing on `C001`'s truth.
- `L022` extends `L018` by one order with an identical support
  structure: the `A007` connectivity and collapse lemmas are hand
  proofs, the computational leg is the same anchored pipeline at order
  \(18\) (`C023`), and every internality caveat recorded for `L018`
  applies verbatim. `L017` remains the strongest bound with a fully
  internal generation layer.
- `L023` and `L024` are voltage-lift program results, not reductions of
  `C001`; neither bears on the truth of `C001` directly. `L024`'s
  certificate corollary cites `L019` at its group-agnostic strength as
  proved in `A009` (the ledger row `L019` itself is stated for
  \(\mathbb Z_m\); `C022` already cites the group-agnostic form the same
  way). The corollary is per-base effective closure for
  \(\lvert\Gamma\rvert\ge\Gamma_0(B)\) plus the finite verdicts
  `C020`/`C022`; it does not cover a small fixed group on bases with
  large reach constant \(R_B\) (see `R001` F3 for a concrete such
  family). For bipartite bases the forced lengths are \(\equiv0\bmod4\)
  only (`R001` F4); powers of two are unaffected. `E009` and the `R001`
  probe verify the construction on finitely many instances; the
  universal claims rest on the `A010` proofs. Unlike `L017`–`L022`,
  `L023`/`L024` carry a delegated adversarial logic audit (`R001`,
  delegated-subagent, pass).
- `C024`'s two theorems were read in the published PDF and may be cited at
  full strength; the ring remark is a construction narrated in prose around
  their Figure 1, and our reconstruction of it (terminal identification,
  through-lengths \(\{3,5\}\)) is verified against their quoted spectrum by
  `E010` anchor A4. `C025` and `C026` are **abstract-strength imports**:
  hypotheses were matched against the arXiv abstracts only, and no proof
  was inspected; do not lean a proof step on them without a full-text
  audit. `C026` is a preprint.
- `L025` bears on the truth of `C001` only conditionally: it converts a
  hypothetical finite object (an atom) into counterexamples, so its value
  is (i) a live disproof channel with a finite search interface and (ii)
  its contrapositive — the spread-doubling necessary condition — as the
  sharp proof-side target replacing the retired unconditional
  walk-to-cycle transfer. The retirement itself is witnessed by `C024`'s
  ring plus `L024`: those graphs carry balanced nb closed walks at every
  admissible length past a threshold while their cycle spectra have
  unbounded-ratio gaps.
- `C027` is a finite exclusion for the `L025` assembly constructions
  (1-atom doublings need gadget order \(\ge16\), hence produce
  counterexamples only at order \(\ge32\); rings need gadget order
  \(\ge16\)); it says nothing about the universal statement. Its class is
  **not** contained in any earlier census (`E004`/`E006`/`C023` all
  required minimum degree 3); the coverage of atoms with a degree-\(\ge3\)
  terminal pair is inherited from `L022`/`C023`, as recorded in the row.
  The profile arm shows pinched through-spread is geometrically common
  without power-freeness, so the spread-doubling phenomenon is a genuine
  power-freeness effect, not a search-class artifact.
- `L026` proves and disproves nothing unconditionally: both directions
  consume hypothetical objects. Its value is methodological and sharp —
  the unrestricted \(s_{\min}\)-stratification of spread-doubling has no
  lemma-sized rungs (each is conjecture-complete), so rung lemmas must be
  taut-relativized — and it upgrades `L025` R5(b) from necessary
  condition to a statement *equivalent* to 0.1 (R5's "neither implication
  reverses on its face" is thereby superseded for clause (b); clause (a),
  the 1-atom question, remains potentially strictly weaker than 0.1).
- `L027`'s decomposition (taut, or lobe with the stated degrees) holds for
  every connected (D)-gadget with no power-freeness hypothesis; only its
  final clause (1-atom or min-degree-3 subgraph) consumes power-freeness.
  Lobes are induced: lobe \(=H[K\cup\{c\}]\) for a component \(K\) of the
  inessential vertices.
- `L028` is the corrected bottom-rung program: with `L027` it routes every
  2-atom at \(s_{\min}\le2\) through the 1-atom question. Its taut
  \(s_{\min}=2\) proof needs only \(C_4\)-freeness — the rung dies before
  \(C_8\) is even mentioned — while \(K_{3,3}-e\) (taut, pinched,
  \(s_{\min}=3\), spectrum \(\{4,6\}\)) shows the *next* rung is exactly
  where \(C_4\)-freeness stops sufficing by itself and the power-spectrum
  fight begins. The order bounds in the corollaries inherit the `C027`
  and `L022` caveats verbatim.
- `L029` does not reduce statement 0.1 to cubic graphs unconditionally;
  the **tight** 1-atom question is the exact price (`L036`: with D-A4's
  unrestricted 1-atoms the price is the conjecture itself, so the
  original phrasing of this note — "the 1-atom question is the exact
  price" — was empty). It re-weights the cubic literature (`C013`,
  `C014`, `C018`: no cubic counterexample below 30, extremal cubic
  \(\{C_4,C_8\}\)-free graphs start at 24) as conditionally decisive:
  modulo tight 1-atoms, those searches bound the whole problem.
  Together with `L026`–`L028` it concentrates the disproof interface on
  two objects: tight 1-atoms and taut 2-atoms with
  \(s_{\min}\ge3\).
- `C028` is instance-level verification of proved lemmas plus an exact
  finite survey; it can refute but not prove `L027`/`L028`, and refuted
  neither. Its taut-pinched finding (all five at \(s_{\min}=6\), interval
  through-sets, each with a \(C_8\)) is the concrete shape catalogue for
  the taut \(s_{\min}=3\) rung and says nothing about larger orders.
- `L030` closes the taut \(s_{\min}=3\) rung with \(C_4\)-freeness alone,
  refuting S012's expectation that the power spectrum must carry this
  rung; at \(s_{\min}=3\) it is exactly the taut form of the
  spread-doubling inequality (`L025` R5(b)). Its limits are sharp on
  three sides: \(K_{3,3}-e\) shows \(C_4\)-freeness cannot be dropped;
  `L026` shows tautness cannot be dropped (a tautness-free version would
  prove 0.1 outright); and `C028`'s five witnesses show the analogous
  \(C_4\)-only statement is **false** at \(s_{\min}=6\) — so the
  \(C_4\)-only regime of the taut ladder ends within
  \(s_{\min}\in\{4,5,6\}\), and by `L028`+`L030` the assembly disproof
  channel below \(s_{\min}=4\) routes entirely through the 1-atom
  question or an outright counterexample. The order-\(\ge19\) corollary
  inherits the `C027` and `L022` caveats verbatim.
- `C029` can refute but not prove `L030`, and did not refute it. Its
  stronger empirical finding — no (D)-pair with \(S\subseteq\{3,4,5\}\)
  even without tautness at orders \(\le14\) — is **not** implied by
  `L030` and must not be cited as a lemma: by `L026`-type constructions,
  non-taut pinched pairs at small \(s_{\min}\) exist iff counterexamples
  to 0.1 exist, so this emptiness is expected but conjecture-strength.
  The endgame-dichotomy anchor is exhaustive only at \(k\in\{2,4\}\).
  **Scope caution (S014):** `C029` certifies \(S\subseteq\{3,4,5\}\)
  only; it says nothing about taut pinched pairs with
  \(s_{\min}\in\{4,5\}\) and \(s_{\max}\ge6\) (its DFS aborted on any
  length-6 path). Earlier summaries citing it for "\(s_{\min}\in\{4,5\}\)
  empty through 14" overstated it; that emptiness is established only by
  `C030`.
- `L031` and `L032` are unconditional graph theory; neither bears on the
  truth of 0.1 directly. `L032`'s equivalence concerns *taut* 2-atoms
  only: with `L027` (non-taut gadgets contain lobes) it splits `G013`
  into the 1-atom question and the **block question** — does a
  power-free vertex-taut 2-connected core with \(s_{\max}\le2s_{\min}\)
  exist? Both directions are constructive, so a block-question witness
  disproves 0.1 outright (lift, then ring by `L025`). The T5 composite
  is a separation example only: its spectrum contains \(C_8\), so it
  disproves nothing about 0.1; what it kills is the hope that
  spread-doubling could hold for cores with cut vertices.
- `C030` supersedes the rung-emptiness content of `C029` and completes
  the strict catalogue through order 14. `C031`'s blocks all contain
  \(C_8\), consistent with `C027` (every stream graph of order \(\le15\)
  contains a \(C_8\)); hence a power-free block needs order \(\ge16\),
  the same frontier as the atom question. The interference census
  (every \(C_8\) a two-path symmetric difference) and the equality
  phenomenon (every known block at \(e=0\) exactly) are finite
  observations at orders \(\le14\), not lemmas; the block rungs at
  \(s_{\min}\ge4\) (strict impossibility, equality power-forcing) are
  open in both directions.
- `L033` is unconditional graph theory with no power-freeness content;
  it bears on 0.1 only through the `L032` interface. Its dichotomy
  (T0/T1/T1′) holds in all graphs; only the refinements consume
  \(C_4\)-freeness, and `C033`'s anchors show each refinement fails
  without it. The "pencil endgame" it induces is a *reduction target*,
  not a proved rung: band 4 of the block question is open exactly in
  the pencil case. The provisional micro-lemmas at the end of `A015`
  (middle/exit matchings; the \(t\ge3\) matched-middle exclusion) are
  sketches, not ledger claims.
- `C032` extends `E013` by one order with identical tool caveats; at
  order 15 the stream total is asserted against `C027`, and the D14
  identification of the unique strict hit's reduction is verified two
  ways (terminal-respecting isomorphism against the recorded closed-14
  hit, plus the closed-scan exhaustiveness argument). The
  pendant-type closed boundary hits reduce to excess-\(+1\) cores
  (\(e(H)=e(\mathrm{core})-1\)), so they are not block-question
  objects and their existence is consistent with the block ladder
  starting at band 4.
- `C033` can refute but not prove `L033`, and refuted nothing; its
  necessity specimens are constructive and small enough to hand-check.
  Its part-B class (2-connected, \(\le2\) sub-cubic, \(C_4\) allowed)
  is not contained in any earlier census; emptiness statements from it
  are order-bounded (\(\le9\)) and must not be cited beyond that
  range.
- `L034` corrects a framing error, not a theorem: D-A3's pinch
  condition came from R2's sufficient dyadic window, and later records
  presented it as *the* ring criterion (`A011` R5's "sharp
  walk-to-cycle form", `G013`'s "closes the entire ring/assembly
  disproof channel", `PROOF.md`'s "sharp conditional form"). Those
  glosses are retracted; every proved row stands, including `L032`'s
  equivalence for taut 2-atoms *as defined*. The assembly disproof
  interface is wider: 1-atoms plus power-free (D)-gadgets with **any**
  \(L\)-fold sumset avoiding the powers of two — pinch, parity, gcd,
  and mod-4 are the known channels. `L027` (lobes) taut-relativizes
  every channel, but `L031`/`L032` (pendant/block) are pinch-specific:
  the pendant shift \(S\mapsto1+S\) flips parity, so the parity channel
  has no block reduction yet. Coverage transfer: `C027` is
  stream-level, so **no power-free (D)-gadget of any profile exists
  through order 15** — the new channels are exactly as empty as the
  pinched one there (and through order 18 for gadgets with both
  terminal degrees \(\ge3\), via `L022`/`C023`); what was never
  scanned is their *shape* data (e.g. all-odd taut cores), invisible
  to the banded `E011`–`E013` scanners. Provenance: the gap was
  surfaced by an external agent memo
  (`references/external-agent-strategy-memo-2026-07-24.md`), audited
  and independently re-derived in `A016`; the memo's literature claims
  remain unverified leads under `G014`.
- `L035` is unconditional graph theory and does not bear on the truth of
  0.1 directly; it reshapes the *interface*. Two of its readings must be
  kept apart. T3 (the criterion) is a **disproof** device: it needs no
  tautness and no path data, so any bipartite power-free graph with
  \(\le2\) sub-cubic vertices ends the conjecture. T4 (completeness) is
  a **search-design** statement, and it is relativized exactly like
  every other rung of `G013`: "bipartite generation misses nothing"
  holds modulo the 1-atom question, because a non-taut parity gadget
  disproves 0.1 by `L027` instead of by bipartiteness. Neither reading
  extends to `L034` channel (ii): \(\Theta(3,3,3)\) is the recorded
  counterexample to any bipartite-forcing claim there. `L035` also
  supersedes the note above that "the parity channel has no block
  reduction yet": pendant shift toggles all-odd \(\leftrightarrow\)
  all-even, i.e. the two bipartite sub-cases, and T0 gives the chain
  decomposition degree-free — what is still missing is a parity
  analogue of `L032`'s *2-connected block extraction* (the excess
  arithmetic that forces a single block to carry the channel has no
  parity counterpart, since an odd chain can be carried by bridges).
- `C034` is a finite exclusion for one channel, not evidence for 0.1 in
  general: it excludes bipartite witnesses at orders \(\le22\)
  (\(\le23\) with a pendant), where the general stream stops at 15
  (`C027`). Its min-degree-\(\ge3\) column is an internal verification
  of the bipartite instance of 0.1 in that range and is the reason no
  step here needs `G014` item 2. Its margin data (minimum \(C_8\) count
  \(\ge13\) throughout) is descriptive, not a bound: nothing proved
  forbids the count from collapsing at a higher order, and
  \(C_8\)-freeness in this class is *not* impossible in general — the
  bipartite double cover of a cubic graph of girth \(g\) is cubic,
  bipartite and has girth \(\ge g\) (a cover cycle projects to a
  tailless non-backtracking closed walk of the same length), so
  `C009`'s arbitrarily-large-girth cubic graphs give bipartite
  \(\{C_4,C_8\}\)-free members of the class at large order. What
  `C034` establishes is that none is reachable at these orders.
- `C035` extends the pinched-channel catalogue by one order with the
  same tool caveats as `C030`–`C032`; its stream total is new data
  (nothing to assert against), its strict-emptiness claim rests on the
  boundary-flag subsumption argument recorded with `C032`, and its
  \(C_8\) statements are pair-level, not stream-level. The S017
  session record and the E013 README S017 addendum carry the full
  classification; the harvest ran concurrently with S016 and its
  canonical-record updates were applied after that session's close
  (see the S017 punch-list note).
- `L036` is a completeness result, i.e. a *negative* structural fact
  about this dossier's own vocabulary: it removes an object that six
  sessions listed as a live sub-question. Read it in the same register
  as `L026`. What it does **not** do is weaken any search: every scan
  ever run over the one-sub-cubic-vertex profile (`C027` through order
  15; `C034` for bipartite graphs through order 21) covers tight
  1-atoms, which is the surviving object. What it does do is relocate
  the programme's proof-side deliverable: the honest target is the
  **cubic reduction** (`G015`), whose two non-circular routes are "no
  tight 1-atom" and strengthening the imported \(4/7\) density bound
  `C006` to 1 — neither of which passes through a conjecture-complete
  object.
- `C036` is a finite exclusion at exactly one order for exactly one
  profile. It consumes the degree pattern and power-freeness only —
  never the \(S\)-condition — so its emptiness verdict covers all of
  \(\mathcal G\) at 16 a fortiori. The \(\ge18\) consequence is a
  theorem-plus-computation composite: `L041` supplies the case
  analysis, `C036` the base emptiness; it inherits every caveat of
  `C027` and `L022` and adds geng's (anchored) generation layer at one
  more order. It says nothing about order 17, nothing about profiles
  with one or zero sub-cubic vertices at 16, and nothing pair-level.
- `C037` is descriptive data plus one exact identification; the
  universal-sounding sentence it supports ("no congruence-type theorem
  at any modulus can exclude case (5b) from those hypotheses alone")
  is the logical reading of a *realization*: a consistent hypothesis
  set admits no refutation from within. Any future congruence attempt
  must add a hypothesis Petersen\(-e\) violates — in practice,
  power-freeness itself or a minimality lever — and `A021` T1 records
  why cycle-space mechanisms cannot supply mod-\(2^t\) (\(t\ge2\))
  confinement regardless. The kill retires `A019` exit item 2 (the
  mod-4 congruence hunt) as pre-registered; it does **not** touch the
  chain-cancellation tension (Minkowski-additive, not residue-based)
  or the band/interference machinery, which remain the live proof-side
  levers against case (5b).
- `L043`–`L046` are the S021 chain-case package and live inside the
  (R)-framed case-(5b) configuration of `L041`; the [min]/[R] labels in
  `A022` record which engine each row consumes *inside* that frame, not
  freedom from (R). `L045` is pure arithmetic and unconditional as an
  identity; only its corollary reads the frame. `L046` is the recorded
  falsifiable target of the S020 checkpoint delivered as a dichotomy;
  its constants are lineage-dependent (32 from `C027`+`C036`; 36 once
  `C039`'s two-order class extension is consumed) and it says nothing
  about the 2-connected branch, which after S021 is the entire
  case-(5b) frontier below order 36.
- `C038`'s negative verdict is scoped to its catalogue (blocks \(\le13\)
  any profile, order-14 two-degree-2 only, chains of \(\le3\) blocks);
  it is **not** an emptiness theorem for the kill object, and its
  abstract 15/16-frontier solutions show the obstruction it found (the
  forced 14) is order-local. Its structural halves (the chain floor and
  the cut⟺non-taut coincidence at \(\le14\)) are proved in `A022`
  W1-T10, not merely observed.
- `C039` is the dossier's first generation-layer *extension*: the
  plugin deletes subtrees of geng's canonical construction path, so its
  coverage claim = geng's coverage claim ∩ a monotone property, and the
  anchoring burden (set-equality against the old pipeline, partition
  checks, a nonzero external positive control at order 24, and the
  cross-instrument reproduction of `E018`'s independent order-16
  profile verdict) is correspondingly heavier than a filter run's. Its
  \(\ge19\)/\(\ge18\) consequences are theorem-plus-computation
  composites through `L041`, with `C027`/`L022` lineage inherited.
  The order-17 **class-level** count (2,580) has no independent full
  cross-check (the raw stream there, \(\approx6\times10^9\), is the
  instrument's whole reason to exist); the profile-level verdicts do,
  at every order where an older instrument reached.
- `L053`–`L055` and `C050` are the S030 non-Hamiltonian package. `L053` is a
  **delimitation, not an exclusion**: it kills a route (promoting `C049`'s
  27/27 to a class-level lemma) and it is asymptotic — it exhibits nothing in
  the window \([18,35]\), so it neither contradicts 27/27 nor moves any floor.
  `L054` must be cited in the right form: as a *dichotomy* it is not a
  contradiction inside `G015`, which already assumes a counterexample exists;
  only its range form (via `C034`) or an order comparison against \(n_0\)
  (via `L040`) excludes anything. `L055` inherits that caveat wherever it uses
  `L054` — its two-attachment half is conditional in exactly the same way, and
  only its three-or-more-attachment half (the Y-identity) is unconditional,
  consuming maximality alone. `L055` clears **no order** on the non-Hamiltonian
  stratum; it removes an obstruction and leaves the two gaps of `A028` T8
  (component-atomic minimality, and savings that are per component rather than
  per position). `C050` is a survived kill test for a **conjecture** — (INT) is
  not a lemma, is not citable as support for any proved row, and its measured
  margins (constant pinned at 8; five degree-2 vertices of room at the value 14)
  are properties of the recorded corpus, not of the class.
